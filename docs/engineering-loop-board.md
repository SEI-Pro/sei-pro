# Engineering Loop Board — SEI Pro PRF

> Atualize este arquivo a cada execução do loop.  
> Estados válidos: `pending_migration`, `migration_in_progress`, `migrated_pending_review`, `review_in_progress`, `review_failed_needs_fix`, `review_passed`, `blocked`.

## Board

| ID | Prioridade | Tipo | Contexto | Feature/Camada | Fatia | Estado | Commit migração | Commit verificação | Fonte | Gate | Smoke scope | Observações |
|---|---:|---|---|---|---|---|---|---|---|---|---|---|
| L0-001 | 0 | docs | global | loop | Criar `docs/engineering-loop.md` | review_passed | bootstrap-atual | bootstrap-atual | `docs/engineering-loop-architecture-evaluation.md` | `git diff --check`; `npm test` | n/a | Documento canônico criado na implantação inicial do loop. |
| L0-002 | 0 | docs | global | loop | Criar `docs/engineering-loop-board.md` | review_passed | bootstrap-atual | bootstrap-atual | `docs/engineering-loop-architecture-evaluation.md` | `git diff --check`; `npm test` | n/a | Board criado como registro auditável, não fila fechada. |
| L0-003 | 0 | map | global | loop-map | Criar `scripts/engineering-loop-map.mjs` | review_passed | bootstrap-atual | bootstrap-atual | `docs/engineering-loop-architecture-evaluation.md` | `node scripts/engineering-loop-map.mjs --check`; `npm test` | n/a | Script gerador de mapa criado sem depender do repo antigo. |
| L0-004 | 0 | map | global | loop-map | Gerar `docs/engineering-loop-map.md` e CSVs de mapa a partir de `src/` | review_passed | bootstrap-atual | bootstrap-atual | `scripts/engineering-loop-map.mjs` | `node scripts/engineering-loop-map.mjs --check`; `npm test` | n/a | Artefatos gerados pelo script local. |
| L0-005 | 0 | infra | global | hermes-cron | Criar novos cron jobs no repo correto, com migração em `openai-codex/gpt-5.5` e verificação 30min depois | review_passed | bootstrap-atual | bootstrap-atual | `docs/engineering-loop.md` | `cronjob list`; `git diff --check`; `npm test` | n/a | Jobs antigos ficam pausados; novos jobs apontam para `/home/tadeu/repos/sei-pro-prf`. |
| A1-001 | 1 | core | global | `src/core/stack.js` / `monitorados` | Tirar dependência de feature de dentro de `core/stack.js`: instalar `installMonitoradoStore` na ponte transitória `src/content/core-stack.js` | review_passed | e7471a2 | (este commit) | `DEVELOPMENT.md` linhas 291-292 e tabela de violações conhecidas | `git diff --check` ✅; `npm test` ✅ (49 arquivos, 382 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 5 (Todas as páginas); atenção a globais de monitorados antes de `sei-functions-pro`/`init*` | Core não importa mais `../features/monitorados/store.js`; compat legada preservada porque `core-stack.bundle.js` ainda instala os aliases globais antes dos scripts legados. Teste estrutural novo em `tests/structure/no-duplicate-core.test.js` guarda que `src/core/stack.js` não importe `../features/*`. Verificação: diff revisado, gates executados com output real, `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace). Próxima fatia sugerida: mapear e iniciar separação domain/io/view de `src/features/monitorados/` ou próxima dívida explícita do `DEVELOPMENT.md`. |
| A1-002 | 2 | feature | lista-processos | `controlar-prazos` / `gerenciarprazos` | Mover a ponte `aliasGlobal` de `src/features/controlar-prazos/index.js` para `legacy-api.js`, preservando a superfície global legada | review_passed | 4c42c47 | (este commit) | `DEVELOPMENT.md` linhas 123, 156-159 e violação `src/features/*/index.js` com `aliasGlobal` | `git diff --check` ✅; `npm test` ✅ (50 arquivos, 384 testes); `node scripts/engineering-loop-map.mjs --check` ✅ após regenerar `docs/engineering-loop-map.md`; `dist/js/init_visualizacao.js` revertido por mudança incidental do build | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco em `initControlePrazo`, coluna de prazos e handlers de adicionar/alterar prazo | Compatibilidade preservada: `index.js` continua importando `./legacy-api.js`, e o novo arquivo reexpõe todas as funções de `domain/io/view` com os mesmos nomes globais. Saída gerada correspondente incluída em `dist/js/sei-pro-controle-prazo.js`; mapa atualizado para 129 arquivos JS analisados. Teste estrutural novo impede regressão de `aliasGlobal` direto em `features/*/index.js` e verifica a ponte de controlar-prazos. Próxima fatia sugerida: aplicar o mesmo padrão de `legacy-api.js` a outra feature/index com aliases ou reduzir gradualmente handlers inline de `controlar-prazos`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real, `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace). Aprovado. |
| A1-003 | 3 | legacy-bridge | global | `monitorados` / store | Mover os aliases globais do store de `src/features/monitorados/store.js` para a ponte antecipada `store-legacy-api.js`, mantendo instalação via `core-stack` | review_passed | ca70a89 | (este commit) | `DEVELOPMENT.md` linhas 123, 156-159 e violação conhecida de `aliasGlobal` fora de `legacy-api.js`; board A1-001/A1-002 | `git diff --check` ✅; `npm test` ✅ (50 arquivos, 386 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace) | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 5 (Todas as páginas), com foco em globais `getStoreMonitoradoPro`/`saveConfigMonitorado` disponíveis antes dos scripts legados e persistência de monitorados | Compatibilidade preservada: `src/content/core-stack.js` agora chama `installMonitoradoStoreLegacyApi()`, que instala o namespace do store e reexpõe exatamente os mesmos aliases globais antes de `sei-functions-pro`/`init*`; `store.js` fica como IO sem `aliasGlobal`. Saída gerada correspondente incluída apenas em `dist/js/core-stack.bundle.js`; mapa atualizado para 130 arquivos JS analisados. Teste estrutural ampliado garante que chamadas reais a `aliasGlobal()` em `src/features/**` fiquem em pontes `legacy-api` e cobre a ponte antecipada de monitorados. Próxima fatia sugerida: reduzir a exceção transitória de `core-stack` criando entry/contexto específico para os aliases de monitorados ou atacar a próxima dívida explícita do background monolítico. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real, `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace). Aprovado. |
| A1-004 | 4 | background | background | `background.js` / storage | Extrair o roteamento das ações `storageGet`/`storageSet`/`storageRemove` para `src/background/storage-handler.js`, mantendo a API de mensagens legada | review_passed | b0b7c6f | 82ba840 | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e seleção dinâmica após A1-003 | `git diff --check` ✅; `npm test` ✅ (51 arquivos, 387 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace) | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar fluxos que usam storage/background (options, lista de processos/notificação, features com persistência) | Compatibilidade preservada: o service worker clássico continua em `dist/js/background.js` e carrega `storage-handler.js` por `importScripts`; as ações e payloads legados não mudam, apenas delegam para `SeiProBackgroundStorage.handleStorageMessage`. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/storage-handler.js`; `scripts/build.mjs` copia o novo adapter; mapa regenerado para 131 arquivos JS analisados. Teste estrutural novo cobre carregamento/delegação do adapter. Próxima fatia sugerida: extrair uma segunda parte pequena do background monolítico, preferencialmente o roteamento de `fetch`/bug report ou um adapter de notificações, mantendo ações legadas. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado e confirmado idêntico ao build), gates executados com output real, `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace). Aprovado. |
| A1-005 | 5 | background | background | `background.js` / fetch | Extrair o roteamento da ação legada `fetch` para `src/background/fetch-handler.js`, mantendo validação de remetente/host e resposta assíncrona | review_passed | 5e07dbb | (este commit) | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e sugestão do board A1-004 | `git diff --check` ✅; `npm test` ✅ (52 arquivos, 388 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace) | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar fluxos que usam background/fetch, especialmente chamada `fetch` delegada ao service worker (Gemini/resolveCaptchaAI) e console sem erros de runtime | Compatibilidade preservada: o service worker clássico continua em `dist/js/background.js` e carrega `storage-handler.js` + `fetch-handler.js` por `importScripts`; a ação `fetch` mantém os mesmos payloads (`url`, `options`) e respostas (`ok`, `status`, `body`/`error`), apenas delegando para `SeiProBackgroundFetch.handleFetchMessage`. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/fetch-handler.js`; `scripts/build.mjs` copia o novo adapter; mapa regenerado para 132 arquivos JS analisados. Teste estrutural novo cobre carregamento/delegação do adapter de fetch, e o teste do storage foi relaxado para aceitar múltiplos scripts carregados no mesmo `importScripts`. Próxima fatia sugerida: extrair uma próxima parte pequena do background monolítico, preferencialmente o envio de `enviarRelatorioBug` ou um adapter de notificações, mantendo ações legadas. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado e confirmado idêntico ao build), gates executados com output real, `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace). Aprovado. |
| A1-006 | 6 | background | background | `background.js` / bug report | Extrair o roteamento da ação legada `enviarRelatorioBug` para `src/background/bug-report-handler.js`, mantendo validação de host e fallback POST→GET | review_passed | 64c1ebf | (este commit) | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e sugestão do board A1-005 | `git diff --check` ✅; `npm test` ✅ (54 arquivos, 390 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar fluxo de envio de relatório de bug/logs via `src/platform/report.js`, verificando console sem erro novo no service worker | Compatibilidade preservada: o service worker clássico continua em `dist/js/background.js` e carrega `storage-handler.js` + `fetch-handler.js` + `bug-report-handler.js` por `importScripts`; a ação `enviarRelatorioBug` mantém os mesmos payloads (`url`, `payload`) e respostas (`ok`/`erro`), apenas delegando para `SeiProBackgroundBugReport.handleBugReportMessage`. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/bug-report-handler.js`; `scripts/build.mjs` copia o novo adapter; mapa regenerado para 133 arquivos JS analisados. Teste estrutural novo cobre carregamento/delegação do adapter de bug report e garante que `isAllowedBugReportSender` saiu do monólito. Próxima fatia sugerida: extrair uma próxima parte pequena do background monolítico, preferencialmente notificações de processos (`syncNotificacaoProcessos`/badge) para um adapter dedicado, mantendo ações legadas. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado e confirmado idêntico ao build atual), gates executados com output real (54 arquivos, 390 testes passaram), `dist/js/init_visualizacao.js` sem churn incidental (árvore limpa após `npm test`). Aprovado. |
| A1-007 | 7 | background | background | `background.js` / notificações de processos | Extrair o roteamento das ações legadas `syncNotificacaoProcessos`/`syncNotificacaoProcessosConfig` para `src/background/process-notification-handler.js`, mantendo badge e notifications | review_passed | 3e98c0e | (este commit) | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e sugestão do board A1-006 | `git diff --check` ✅; `npm test` ✅ (55 arquivos, 391 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar notificações de processos/badge na lista de processos, verificando console sem erro novo no service worker | Compatibilidade preservada: o service worker clássico continua em `dist/js/background.js` e carrega `storage-handler.js` + `fetch-handler.js` + `bug-report-handler.js` + `process-notification-handler.js` por `importScripts`; as ações `syncNotificacaoProcessos` e `syncNotificacaoProcessosConfig` preservam os mesmos payloads/respostas e delegam para `SeiProBackgroundProcessNotification.handleProcessNotificationMessage`. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/process-notification-handler.js`; `scripts/build.mjs` copia o novo adapter; mapa regenerado para 134 arquivos JS analisados. Teste estrutural novo cobre carregamento/delegação do adapter de notificações e garante que as funções de sync/notificação saíram do monólito. Próxima fatia sugerida: checker revisar A1-008; depois, se aprovado, avaliar se ainda resta roteamento no background ou escolher a próxima dívida explícita (`aliasGlobal` em features ou CSS sem prefixo). Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via `diff`), gates executados com output real (55 arquivos, 391 testes passaram), árvore limpa sem churn incidental. Aprovado. |
| A1-008 | 8 | background | background | `background.js` / instalação-atualização | Extrair o listener legado `runtime.onInstalled` para `src/background/install-handler.js`, mantendo abertura da página de boas-vindas e flag `InstallOrUpdate` | review_passed | 4e9add6 | (este commit) | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e sugestão do board A1-007 | `git diff --check` ✅; `npm test` ✅ (56 arquivos, 392 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos; em install/update real, validar página de boas-vindas/flag `InstallOrUpdate` e console sem erro novo no service worker | Compatibilidade preservada: `browser.runtime.onInstalled.addListener(handleInstalled)` continua no service worker clássico, mas `handleInstalled` delega para `SeiProBackgroundInstall.handleInstalled(details, browser)` carregado via `importScripts`; os comportamentos de `install` e `update` foram movidos sem alterar payloads, storage ou URLs. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/install-handler.js`; `scripts/build.mjs` copia o adapter; mapa regenerado para 135 arquivos JS analisados. Teste estrutural novo cobre carregamento/delegação do adapter de instalação. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild), gates executados com output real (56 arquivos, 392 testes passaram), árvore limpa sem churn incidental. A1-009 (router) já submetida como próxima fatia; checker revisar A1-009 na próxima execução. Aprovado. |
| A1-009 | 9 | background | background | `background.js` / router | Extrair o roteamento de mensagens de `background.js` para `src/background/router.js`, mantendo o service worker como fachada fina | review_passed | 5ffc5e9 | (este commit) | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e sugestão do board A1-008 | `git diff --check` ✅; `npm test` ✅ (57 arquivos, 393 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar fluxos de background/storage/fetch/relatório de bug/notificações, verificando console sem erro novo no service worker | Compatibilidade preservada: `background.js` continua clássico e registrado no manifest, carrega todos os adapters e o novo `router.js` por `importScripts`, mantém `runtime.onMessage.addListener(handleMessage)` e delega para `SeiProBackgroundRouter.handleMessage(message, sender, sendResponse, browser)`; as ações legadas e respostas de indisponibilidade permanecem iguais. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/router.js`; `scripts/build.mjs` copia o adapter; mapa regenerado para 136 arquivos JS analisados. Teste estrutural novo cobre a fachada fina e os testes dos adapters agora verificam a delegação no router. Próxima fatia sugerida: iniciar separação domain/io/view de outra feature (`controlar-prazos`, `monitorados`) ou atacar dívida de `aliasGlobal` em features/CSS sem prefixo. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via `diff`), gates executados com output real (57 arquivos, 393 testes passaram), árvore limpa sem churn incidental. Aprovado. |
| A1-010 | 10 | shared-legacy | global | `shared/ui/prazo-preview` / legacy bridge | Mover os aliases globais `getDatesPreview`/`getProgressPreview`/`configDatesPreview` para `src/shared/ui/prazo-preview-legacy-api.js`, preservando instalação no `core-stack` | review_passed | 4e91d26 | (este commit) | `DEVELOPMENT.md` linhas 123, 156-159 e 287-304; seleção dinâmica após A1-009 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 394 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar seções 1 (Lista de processos) e 5 (Todas as páginas), com foco em etiquetas/prévia de prazo (`getDatesPreview`) e console sem erro novo durante renderização de prazos | Compatibilidade preservada: `installCoreStack()` continua instalando `SeiPro.shared.prazoPreview` e, logo depois, chama `installPrazoPreviewLegacyApi()` para reexpor os mesmos três globais usados por scripts legados; o módulo compartilhado `prazo-preview.js` deixa de importar/chamar `aliasGlobal`. Saída gerada correspondente incluída em `dist/js/core-stack.bundle.js`, `dist/js/db.bundle.js` e `dist/js/login.bundle.js`; mapa regenerado para 137 arquivos JS analisados. Teste estrutural novo garante que os aliases globais de prazo fiquem apenas na ponte `prazo-preview-legacy-api.js`. Próxima fatia sugerida: checker revisar A1-011; depois escolher outra ponte global pequena ou iniciar fatias de CSS prefixado quando houver escopo visível controlado. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild), gates executados com output real (58 arquivos, 394 testes passaram), árvore limpa sem churn incidental. Aprovado. |
| A1-011 | 11 | feature | lista-processos | `controlar-prazos` / CSS prefixado | Prefixar as classes próprias da coluna de prazos (`prazoBoxDisplay`/`addControlePrazo`) para `.seipro-*`, preservando seletores consumidores | review_passed | d090fa3 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de CSS sem prefixo em `src/features/*/*.css`; seleção dinâmica após A1-010 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 394 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar seção 1 (Lista de processos), com foco na coluna “Prazos”, botão “Adicionar prazo”, agrupamento por prazo e espelho de prazo em Monitorados; console sem erro novo | Compatibilidade preservada: a função/global legada `addControlePrazo` e os marcadores `data-seipro-add-prazo` não mudam; apenas as classes DOM/CSS próprias passaram para `seipro-prazo-box-display` e `seipro-add-controle-prazo`. Consumidores em `lista-processos/sei-pro.js` e `monitorados/panel.js` foram atualizados para o novo seletor. Saída gerada correspondente incluída em `dist/css/controlar-prazos.css`, `dist/js/sei-pro-controle-prazo.js`, `dist/js/sei-pro.js` e `dist/js/monitorados.bundle.js`; teste estrutural novo trava o prefixo e ausência das classes antigas em `src/`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 394 testes passaram), sem churn incidental. Resíduos de `addControlePrazo` em `src/` são apenas nomes de função global legado e strings de replace de onclick→data-attr, não classes CSS — preservação correta. Aprovado. Próxima fatia sugerida: continuar CSS prefixado pequeno em outra feature visível ou revisar exceções compartilhadas remanescentes. |
| A1-012 | 12 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar a classe própria da tabela de monitorados (`tableMonitorados`) para `.seipro-table-monitorados`, preservando os seletores consumidores | review_passed | 565bed5 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de CSS sem prefixo em `src/features/*/*.css`; sugestão A1-011 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 395 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, edição de etiquetas/cores/ícones e console sem erro novo | Compatibilidade preservada: `panel.js` continua gerando a mesma tabela/IDs legados (`monitoradosProDiv`, `monitoradoTablePro`) e mantém classes compartilhadas (`tableInfo`, `tableZebra`, `infraTable`, `tableFollow`, `tabelaControle`); só a classe própria de styling da feature foi prefixada. O seletor legado em `sei-functions-pro.js` que atualiza ícones de etiquetas agora procura `.seipro-table-monitorados tbody`. Saída gerada correspondente incluída em `dist/css/monitorados.css`, `dist/js/monitorados.bundle.js` e `dist/js/sei-functions-pro.js`; teste estrutural ampliado garante ausência de `.tableMonitorados` em `src/`. Próxima fatia sugerida: continuar CSS prefixado pequeno em outra feature visível (ex.: outra classe própria de monitorados ou de `controlar-prazos`) ou atacar exceção `aliasGlobal`/legacy-bridge remanescente explícita no `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 395 testes passaram), sem churn incidental. Aprovado. |
| A1-013 | 13 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook DOM próprio de remoção em lote (`iconMonitorados_remove`) para `.seipro-monitorados-remove-selected`, preservando o `data-act` legado | review_passed | 42a9cd4 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-012 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 396 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, seleção de linhas e exibição/acionamento da remoção em lote; console sem erro novo | Compatibilidade preservada: o contrato comportamental continua por `data-act="remove-selected"` e pelo listener delegado de `panel-lifecycle.js`; apenas o hook CSS/DOM próprio da feature foi prefixado. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `iconMonitorados_remove` em `panel.js`/`panel-lifecycle.js`. Próxima fatia sugerida: continuar prefixando hooks próprios pequenos de monitorados (`iconMonitorados_update`/`iconMonitorados_config`) ou outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 396 testes passaram), sem churn incidental. Aprovado. |
| A1-014 | 14 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook DOM próprio de atualização (`iconMonitorados_update`) para `.seipro-monitorados-update`, preservando o `data-act` legado | review_passed | 1efb9d2 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-013 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 397 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, botão “Atualizar Informações” e console sem erro novo | Compatibilidade preservada: o contrato comportamental continua por `data-act="update"` e pelo listener delegado existente; apenas o hook CSS/DOM próprio da feature foi prefixado. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `iconMonitorados_update` em `src/`. Próxima fatia sugerida: continuar prefixando o hook próprio remanescente de monitorados (`iconMonitorados_config`) ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 397 testes passaram), sem churn incidental. Aprovado. |
| A1-015 | 15 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook DOM próprio de configuração (`iconMonitorados_config`) para `.seipro-monitorados-config`, preservando o `data-act` legado | review_passed | 6a72612 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-014 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 398 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa após `npm test` sem churn incidental | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, botão “Configurações” e console sem erro novo | Compatibilidade preservada: o contrato comportamental continua por `data-act="config"` e pelo listener delegado existente; apenas o hook CSS/DOM próprio da feature foi prefixado. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `iconMonitorados_config` em `src/`. Próxima fatia sugerida: revisar se restam classes próprias de monitorados sem prefixo ou escolher outra dívida explícita do `DEVELOPMENT.md` (`aliasGlobal` fora de `legacy-api.js` ou CSS sem prefixo em outra feature). Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 398 testes passaram), sem churn incidental. Aprovado. |
| A1-016 | 16 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar o hook DOM próprio de configuração de prazo (`monitoradoConfigDates`) para `.seipro-monitorado-config-dates`, preservando o `data-act` legado | review_passed | a308517 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-015 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 399 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco nas opções de prazo de processos monitorados e console sem erro novo | Compatibilidade preservada: o contrato comportamental continua por `data-act="dates-config"` e pelo listener delegado existente; apenas o hook CSS/DOM próprio da feature foi prefixado e o consumidor em `prazo-row.js` passou a consultar o seletor novo. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `monitoradoConfigDates` em `src/features/monitorados`. Próxima fatia sugerida: revisar outras classes próprias remanescentes de monitorados sem prefixo (`tdmonitorado_*`, `info_*`) com cautela por serem compartilhadas com legado, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real (58 arquivos, 399 testes passaram), árvore limpa sem churn incidental. Aprovado. |
| A1-017 | 17 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar a classe própria das células espelho nativas (`tdmonitorado_native`) para `.seipro-monitorado-native-cell`, preservando o contrato `data-native` | review_passed | 1e9d6c2 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-016 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 400 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, espelhos de prazo/marcador/anotação e console sem erro novo | Compatibilidade preservada: o comportamento continua guiado por `data-native` e pelos IDs/estruturas legadas do painel; apenas a classe DOM própria da feature usada nas células espelho foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `tdmonitorado_native` em `panel.js`. Próxima fatia sugerida: avaliar outras classes próprias remanescentes de monitorados (`tdmonitorado_tipo`, `td_monitorado_category`, `info_category*`) com cautela por consumidores locais/legados, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 400 testes passaram), sem churn incidental. Aprovado. |
| A1-018 | 18 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar a classe própria da célula de tipo (`tdmonitorado_tipo`) para `.seipro-monitorado-type-cell`, preservando o contrato `data-native="tipo"` | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-017 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 401 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, coluna “Tipo de Processo” e console sem erro novo | Compatibilidade preservada: o conteúdo da célula continua guiado por `data-native="tipo"` e pelo mesmo link `data-act="remove-row"`; apenas a classe DOM própria da feature foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `tdmonitorado_tipo` em `src/`. Próxima fatia sugerida: checker revisar A1-018; depois avaliar a classe própria de categoria (`td_monitorado_category`/`info_category*`) com cautela por consumidores locais, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real (58 arquivos, 401 testes passaram), árvore limpa sem churn incidental. Aprovado. |
| A1-019 | 19 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar a classe própria da célula de categoria (`td_monitorado_category`) para `.seipro-monitorado-category-cell`, preservando os hooks internos de edição | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-018 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 402 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, coluna “Categoria”, edição/filtro de categoria e console sem erro novo | Compatibilidade preservada: os hooks locais `info_category_txt`, `info_category` e `data-act="category-edit"` continuam iguais para o dispatcher de categorias; apenas a classe DOM própria da célula foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `td_monitorado_category` em `src/`. Próxima fatia sugerida: checker revisar A1-019; depois avaliar `info_category*` com cautela por serem hooks internos de edição, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real (58 arquivos, 402 testes passaram), árvore limpa sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-020 | 20 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar os hooks internos de edição de categoria (`info_category_txt`/`info_category`) para classes `.seipro-*`, preservando o dispatcher de categoria | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-019 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 402 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, coluna “Categoria”, editar/salvar/nova categoria e console sem erro novo | Compatibilidade preservada: o contrato comportamental segue pelos atributos `data-act` de categoria e pelo dispatcher de `categorias.js`; apenas os hooks DOM internos da célula foram prefixados para `seipro-monitorado-category-text` e `seipro-monitorado-category-editor`. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava os novos hooks e ausência de `info_category*` em `panel.js`/`categorias.js`. Próxima fatia sugerida: avaliar a próxima classe própria remanescente de monitorados (`tdmonitorado_tags`) com cautela por consumidores compartilhados/atividades, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado e confirmado idêntico ao build), gates executados com output real (58 arquivos, 402 testes passaram), árvore limpa sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-021 | 21 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a classe própria da célula de etiquetas de monitorado (`tdmonitorado_tags`) para `.seipro-monitorado-tags-cell`, preservando o modo de etiquetas | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-020 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 403 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco nas etiquetas de processos monitorados, botão “Adicionar etiqueta” e console sem erro novo | Compatibilidade preservada: o contrato comportamental continua pelo atributo `data-etiqueta-mode` e pelo dispatcher de etiquetas existente; apenas a classe DOM/CSS própria da célula de etiquetas em `visualizacao.js` foi prefixada para `seipro-monitorado-tags-cell`. Saída gerada correspondente incluída em `dist/css/monitorados.css` e `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `tdmonitorado_tags` nos arquivos da fatia. Próxima fatia sugerida: checker revisar A1-021; depois avaliar classes próprias remanescentes de monitorados ou atacar outra dívida explícita do `DEVELOPMENT.md`. |
| A1-022 | 22 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a classe própria do box de opções de monitorado (`monitoradosLabelOptions`) para `.seipro-monitorados-label-options`, preservando seleção legada de etiquetas | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-021 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 404 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no checkbox “Manter processo em Processos Monitorados”, edição de categoria/prazo/etiquetas e console sem erro novo | Compatibilidade preservada: o box continua dentro de `divSinAdicionarMonitorados` e mantém `seiProForm`, ids e inputs legados; consumidores em `datas.js` e `sei-functions-pro.js` passaram a consultar a classe prefixada. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js` e `dist/js/sei-functions-pro.js`; teste estrutural ampliado trava o novo hook e ausência dos seletores antigos nos consumidores da fatia. Próxima fatia sugerida: avaliar hooks próprios remanescentes de monitorados, como `monitoradoTagsPro`/`info_dates_*`, com cautela por classes compartilhadas, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 404 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-023 | 23 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a classe própria do input de etiquetas de monitorado (`monitoradoTagsPro`) para `.seipro-monitorado-tags-input`, preservando o `name` legado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-022 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 405 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no input de etiquetas de processos monitorados, checkbox “Manter processo em Processos Monitorados” e console sem erro novo | Compatibilidade preservada: o campo mantém `name="monitoradoTagsPro"` e o fluxo de `createTagsInput`/salvamento de etiquetas; apenas o hook DOM de classe foi prefixado e o reset em `datas.js` passou a consultar a classe nova. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga nos consumidores da fatia. Próxima fatia sugerida: avaliar os hooks de prazo remanescentes (`tdmonitorado_dates`/`info_dates_*`) com cautela por serem compartilhados com `controlar-prazos`/`atividades`, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 405 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-024 | 24 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar a classe própria do contêiner de ícones nativos copiados (`info_icons_monitorado`) para `.seipro-monitorado-native-icons`, preservando os links/ícones clonados | review_passed | 9846d2b | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-023 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 405 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, ícones nativos copiados ao lado do número do processo e console sem erro novo | Compatibilidade preservada: os links/ícones nativos filtrados continuam clonados de `#P{id}` e inseridos no mesmo ponto da célula de processo; apenas a classe DOM própria da feature foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga em `panel.js`. Próxima fatia sugerida: avaliar hooks de prazo remanescentes (`tdmonitorado_dates`/`info_dates_*`) com cautela por classes compartilhadas com `controlar-prazos`/`atividades`, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 405 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-025 | 25 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar o hook interno do editor de prazo de monitorado (`info_dates_monitorado_txt`) para `.seipro-monitorado-dates-editor`, preservando o `name` legado do input | review_passed | 8764b2c | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-024 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 406 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no editor de prazo de processos monitorados, checkbox “Manter processo em Processos Monitorados” e console sem erro novo | Compatibilidade preservada: o campo mantém `name="monitoradoPrazoSend"`, id `monitoradoPrazoSend`, `data-act="dates-hide-blur"` e classe de input `.seipro-monitorado-dates`; apenas o hook DOM do span editor foi prefixado e consumidores em `datas.js`/`prazo-row.js` passaram a consultar a classe nova. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga nos consumidores da fatia. Próxima fatia sugerida: avaliar `tdmonitorado_dates`/`info_dates_follow_empty` com cautela por acoplamento com CSS legado compartilhado, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 406 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-026 | 26 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook de estado vazio de prazo (`info_dates_follow_empty`) para `.seipro-monitorado-dates-empty`, preservando a prévia compartilhada de prazo | review_passed | 5f1cce1 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-025 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 407 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, coluna “Prazo”, hover “Adicionar prazo” em prazo vazio e console sem erro novo | Compatibilidade preservada: a prévia compartilhada `.info_dates_monitorado` e o fluxo `getDatesPreview` continuam iguais; apenas a classe de estado vazia controlada por `prazo-row.js` foi prefixada e o CSS passou a escopar pelo painel `.seipro-table-monitorados`. Saída gerada correspondente incluída em `dist/css/monitorados.css` e `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `tdmonitorado_dates`/`info_dates_follow_empty` nos arquivos da fatia. Próxima fatia sugerida: checker revisar A1-026; depois avaliar outros hooks próprios remanescentes de monitorados com cautela por classes compartilhadas, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 407 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-027 | 27 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar o hook visual de adicionar etiquetas (`followLinkTagsAdd_send`) para `.seipro-monitorado-tags-add`, preservando o dispatcher por `data-act` | review_passed | 8bc1090 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-026 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 407 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco em adicionar etiqueta em processos monitorados e console sem erro novo | Compatibilidade preservada: o clique continua roteado pelo atributo `data-act` de etiquetas e pelo dispatcher legado `showFollowEtiqueta`; apenas a classe DOM própria do link foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `followLinkTagsAdd_send` em `visualizacao.js`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real (58 arquivos, 407 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. Próxima fatia sugerida: avaliar `info_tags_follow_empty`/`info_tags_follow*` com cautela por serem compartilhados com atividades/legado, ou atacar outra dívida explícita do `DEVELOPMENT.md`. |
| A1-028 | 28 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook visual de remover linha de monitorado (`followLinkMonitoradoRemove`) para `.seipro-monitorado-remove-row`, preservando o dispatcher por `data-act` | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-027 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 407 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel "Processos Monitorados", botão de remover linha individual e console sem erro novo | Compatibilidade preservada: o clique continua roteado pelo atributo de remover linha e pelo dispatcher delegado do painel; apenas a classe DOM própria do link foi prefixada. Classes compartilhadas `.newLink`, `.followLink`, `.followLinkTags` permanecem intactas. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`, confirmado idêntico ao build via rebuild. Teste estrutural ampliado trava o novo hook e ausência da classe antiga em `panel.js`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 407 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. Próxima fatia sugerida: avaliar hooks próprios remanescentes de monitorados (`info_tags_follow*` compartilhados com atividades/legado) com cautela, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. |
| A1-029 | 29 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook visual de editar categoria (`followLinkMonitoradoCategory`) para `.seipro-monitorado-category-edit`, preservando o dispatcher por `data-act` | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-028 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 407 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel Processos Monitorados, botão de editar categoria e console sem erro novo | Compatibilidade preservada: o clique continua roteado pelo atributo de edição de categoria e pelo dispatcher delegado do painel; apenas a classe DOM própria do link foi prefixada. Classes compartilhadas `.newLink`, `.followLink` e `.followLinkTags` permanecem intactas. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga em `panel.js`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 407 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. Aprovado. Próxima fatia sugerida: avaliar hooks próprios remanescentes de monitorados (`info_tags_follow*` compartilhados com atividades/legado) com cautela, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. |
| A1-030 | 30 | feature | lista-processos | `controlar-prazos` / CSS prefixado | Prefixar a classe remanescente da coluna de prazos (`prazosBoxDisplay`) para `.seipro-prazo-box-display`, alinhando DOM com CSS já prefixado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; seleção dinâmica após A1-029 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 407 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco na coluna Prazos, ordenação/renderização do prazo por marcador e console sem erro novo | Compatibilidade preservada: funções/globais legados de controle de prazo não mudaram; apenas a classe DOM da coluna passou para o prefixo já usado por seletores e CSS da feature. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-controle-prazo.js`; teste estrutural ampliado trava a ausência de `prazosBoxDisplay` em `src/`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 407 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. Ausência de `prazosBoxDisplay`/`prazoBoxDisplay` confirmada em `src/` e `dist/`. Aprovado. Próxima fatia sugerida: avaliar outra classe própria sem prefixo em feature visível ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. |
| A1-031 | 31 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar o hook de estado vazio de etiquetas de monitorado (`info_tags_follow_empty`) para `.seipro-monitorado-tags-empty`, preservando atividades/legado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-030 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 408 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco em etiquetas de processos monitorados sem etiquetas e console sem erro novo | Compatibilidade preservada: `showFollowEtiqueta` continua usando `info_tags_follow_empty` para atividades/legado, mas aplica `seipro-monitorado-tags-empty` quando o alvo é monitorados; `info_tags_follow`/`info_tags_follow_txt` e dispatcher de etiquetas permanecem intactos. Saída gerada correspondente incluída em `dist/css/monitorados.css` e `dist/js/sei-functions-pro.js`; mapa regenerado porque `src/shared/legacy/sei-functions-pro.js` mudou. Teste estrutural ampliado trava o novo hook em CSS/legado e ausência do hook antigo no CSS de monitorados. Próxima fatia sugerida: checker revisar A1-031; depois avaliar outros hooks próprios remanescentes de monitorados com cautela por compartilhamento com atividades/legado, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. |
| A1-032 | 32 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o wrapper de rolagem do painel (`tabelaPanelScroll`) para `.seipro-monitorados-scroll`, preservando o painel legado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-031 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 409 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel Processos Monitorados, rolagem/redimensionamento da tabela e console sem erro novo | Compatibilidade preservada: ids/containers legados `monitoradosPro` e `monitoradosProDiv` permanecem, assim como as classes compartilhadas `panelHomePro` e `panelHome`; apenas o wrapper DOM próprio do painel foi prefixado. Saída gerada correspondente incluída em `dist/css/monitorados.css` e `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `tabelaPanelScroll` nos arquivos da fatia. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 409 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. `tabelaPanelScroll` confirmada amplamente usada em outras features/shared legacy — apenas a instância emitida por `panel.js` e seus seletores CSS escopados sob `#monitoradosProDiv` foram prefixados. Aprovado. Próxima fatia sugerida: avaliar outros wrappers/classes próprios de monitorados com cautela por compartilhamento com legado, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. |
| A1-033 | 33 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook visual do link do processo monitorado (`followLinkProcesso`) para `.seipro-monitorado-process-link`, preservando navegação e ícone nativo | review_passed | 2fa6287 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-032 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 409 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel Processos Monitorados, clique no link do processo e abertura em nova aba; console sem erro novo | Compatibilidade preservada: o href do processo, classe compartilhada `bLink`, ícone/título do tipo de processo e link `followLinkNewtab` permanecem intactos; apenas a classe DOM própria do link principal foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `followLinkProcesso` em `src/`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 409 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. `followLinkProcesso` confirmada ausente em `src/`. Aprovado. |
| A1-034 | 34 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar as linhas de seleção de documento do modal de prazo (`configDates_selectdoc`) para `.seipro-monitorado-dates-selectdoc`, preservando alternância por tipo | review_passed | 4082ccc | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-033 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, alternância “data de assinatura de documento”/“data específica” e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_*`, `data-act`, nomes globais de `legacyApi` e o contrato de alternância por `data-type` não mudaram; apenas a classe DOM própria das linhas de seleção de documento foi prefixada e o seletor dinâmico passou por `configDateClass()`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado porque `src/features/monitorados/datas.js` ganhou o helper `configDateClass`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida. Próxima fatia sugerida: checker revisar A1-034; depois continuar com uma única classe própria remanescente do modal de prazo (`configDates_setdate`, `configDates_countdays`, etc.) ou escolher outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 410 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. `configDates_selectdoc` confirmada ausente em `src/` via busca; demais `configDates_*` legados preservados intocados. Aprovado. |
| A1-035 | 35 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar as linhas de data específica do modal de prazo (`configDates_setdate`) para `.seipro-monitorado-dates-setdate`, preservando alternância por tipo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-034 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, alternância “data específica”/“data de assinatura de documento” e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_*`, atributos `data-act`/`data-type`, `legacyApi` e classes legadas irmãs do modal continuam iguais; apenas a classe DOM própria das linhas de data específica foi prefixada e a ponte dinâmica `configDateClass()` passou a mapear `setdate`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado por mudança de linhas em `src/features/monitorados/datas.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida em `datas.js`. Próxima fatia sugerida: checker revisar A1-035; depois continuar com uma única classe própria remanescente do modal de prazo (`configDates_countdays`, `configDates_newdoc`, etc.) ou escolher outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 410 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental, `dist/js/init_visualizacao.js` limpo. `configDates_setdate` confirmada ausente em `src/features/monitorados/`; remanescente em `controlar-prazos/view.js` é contexto legado fora do slice (mesma regra de A1-034). Aprovado. |
| A1-036 | 36 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a linha de opção de dias úteis do modal de prazo (`configDates_countdays`) para `.seipro-monitorado-dates-countdays`, preservando alternância por tipo | review_passed | 4cca614 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-035 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, alternância “Em número de dias”/“Em tempo relativo”, checkbox “Calcular em dias úteis” e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_*`, atributos `data-act`/`data-type`, `legacyApi` e classes legadas irmãs do modal continuam iguais; apenas a classe DOM própria da linha de dias úteis foi prefixada e a ponte dinâmica `configDateClass()` passou a mapear `countdays`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado por mudança de linhas em `src/features/monitorados/datas.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida em `datas.js`. Próxima fatia sugerida: checker revisar A1-036; depois continuar com uma única classe própria remanescente do modal de prazo (`configDates_newdoc`, `configDates_duedate`, etc.) ou escolher outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 410 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. `configDates_countdays` confirmada ausente em `src/`. Contrato `configDatesBox_workday`, `data-act`, `data-type` preservado. Aprovado. |
| A1-037 | 37 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar as linhas de novo documento do modal de prazo (`configDates_newdoc`) para `.seipro-monitorado-dates-newdoc`, preservando alternância por tipo | review_passed | ad93808 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-036 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, opção “Da assinatura de um novo documento”, lista/tipo de documento e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_newdoclist`/`configDatesBox_listnewdoc`, atributos `data-act`/`data-type`, `legacyApi` e classes legadas irmãs do modal continuam iguais; apenas a classe DOM própria das linhas de novo documento foi prefixada e a ponte dinâmica `configDateClass()` passou a mapear `newdoc`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado por mudança de linhas em `src/features/monitorados/datas.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida em `datas.js`. Verificação checker: diff do commit ad93808 revisado; `src/` é fonte da verdade, `dist/js/monitorados.bundle.js` coincide com o build, escopo limitado ao hook `newdoc` e sua ponte dinâmica; `git status --short` limpo antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 454 testes), `node scripts/engineering-loop-map.mjs --check` ✅; nenhum churn incidental em `dist/js/init_visualizacao.js`. Aprovado. Próxima fatia: A1-038. |
| A1-038 | 38 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar as linhas de dias decorridos do modal de prazo (`configDates_duedate`) para `.seipro-monitorado-dates-duedate`, preservando alternância por tipo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-037 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, opção “Do número de dias decorridos”, contador/modo de vencimento e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_duenumber`/`configDatesBox_duecounter`/`configDatesBox_duemode`, atributos `data-act`/`data-type`, `legacyApi` e classes legadas irmãs do modal continuam iguais; apenas a classe DOM própria das linhas de dias decorridos foi prefixada e a ponte dinâmica `configDateClass()` passou a mapear `duedate`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado por mudança de linhas em `src/features/monitorados/datas.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida em `datas.js`. Verificação checker: diff do commit 03facc2 revisado; `src/` é fonte da verdade, `dist/js/monitorados.bundle.js` coincide com rebuild, escopo limitado ao hook `duedate` e sua ponte dinâmica; `git status --short` limpo antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 455 testes; 455 testes passaram), `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. Próxima fatia: A1-039. |
| A1-039 | 39 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a linha de data de vencimento específica do modal de prazo (`configDates_duesetdate`) para `.seipro-monitorado-dates-duesetdate`, preservando alternância por tipo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-038 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, opção “De uma data de vencimento específica”, campo de data de vencimento e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_duesetdt`, atributos `data-act`/`data-type`, `legacyApi` e classes legadas irmãs fora de Monitorados continuam iguais; apenas a classe DOM própria da linha de data de vencimento específica foi prefixada e a ponte dinâmica `configDateClass()` passou a mapear `duesetdate`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado por mudança de linhas em `src/features/monitorados/datas.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida em `datas.js`. Revisão aprovada: `a95eabf` altera uma única linha DOM e o mapeamento dinâmico `duesetdate`; `src/` é a fonte da verdade e `dist/js/monitorados.bundle.js` coincide com o build. Gates reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 455 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Próxima fatia: A1-040. |
| A1-040 | 40 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a tabela de opções avançadas do modal de prazo (`configDates_advanced`) para `.seipro-monitorado-dates-advanced`, preservando alternância avançada | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-039 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, botão “Opções avançadas”, visualização de opções avançadas e console sem erro novo | Compatibilidade preservada: o botão continua usando `data-act` advanced e `newLink_active`; apenas a classe DOM própria da tabela avançada foi prefixada e o seletor de `advanced()` passou a consultar `.seipro-monitorado-dates-advanced`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `configDates_advanced` em `datas.js`. Revisão checker aprovada: commit `6e6d9a8` altera somente a classe da tabela avançada e o seletor consumidor, mantendo `data-act="advanced"`, `newLink_active` e demais contratos; `src/` é a fonte da verdade e `dist/js/monitorados.bundle.js` foi reproduzido pelo build. Gates reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 455 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Próxima fatia: A1-041. |

| A1-041 | 41 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar o hook visual da mensagem de sucesso ao adicionar monitorado (`iframeSucessPro`) para `.seipro-monitorado-success-message`, preservando o modal de opções | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-040 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 411 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco em adicionar processo aos Monitorados pela visualização/processo e abertura do modal de opções sem erro novo | Compatibilidade preservada: o modal `openModal` continua com o mesmo título, conteúdo textual, ícone e `monitoradosLabelOptions`; apenas a classe DOM própria do `<strong>` de sucesso foi prefixada. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `iframeSucessPro` em `commands.js`. Revisão checker aprovada: `fb697f4` altera somente o hook visual emitido em `commands.js` e o bundle correspondente; `src/` é a fonte da verdade, a classe antiga permanece ausente no produtor e o modal/texto/ícone/`monitoradosLabelOptions` foram preservados. `dist/js/monitorados.bundle.js` coincidiu com a saída reproduzida pelo build. Gates reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 456 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Próxima fatia: A1-042. |

| A1-042 | 42 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar as linhas de tipo/especificação de processo do painel de cruzamento (`containerTipoProcessoSelect`) para `.seipro-doclote-process-type-fields`, preservando checkbox e campos legados | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; seleção dinâmica após A1-041 | `git status --short` ✅ (limpo antes/depois); `git diff --check` ✅; `npm test` ✅ (60 arquivos, 457 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, modal de cruzamento CSV/modelo, checkbox “Criar cada documento em um novo processo”, seleção de tipo/especificação e console sem erro novo | Revisão aprovada: o commit `15654e8` altera uma única classe própria nas duas linhas e o seletor show/hide correspondente; ids `newProcs`, `tipoProcessoSelect` e `txtEspecificacaoProcesso` permanecem iguais. `src/` é a fonte da verdade; `dist/js/docs-lote.bundle.js` foi confirmado coincidente com a saída do build, sem churn incidental em `dist/js/init_visualizacao.js`; teste estrutural cobre o novo hook e a ausência do antigo em `templates.js`/`view.js`. Próxima fatia: A1-043. |
| A1-043 | 43 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar a classe de texto de análise do wizard (`textAnalysis`) para `.seipro-doclote-analysis-text`, preservando campos e fluxo do modal | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-042 | `git diff --check` ✅; `npm test` ✅ (59 arquivos, 413 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, etapas de análise de documento modelo/texto padrão e CSV, seleção de tipo de documento e console sem erro novo | Compatibilidade preservada: ids `fieldList`, `fieldListCSV`, `tipoDocumentoSelect`, `btnConfirmAnalysis` e `btnConfirmAnalysisCSV` continuam iguais; apenas a classe DOM própria aplicada aos textos/listas de análise foi prefixada. Saída gerada correspondente incluída em `dist/js/docs-lote.bundle.js`; teste estrutural ampliado trava o hook prefixado e ausência da classe antiga emitida em `view.js`. Revisão checker aprovada: commit `64ceb5e` limita-se ao produtor `src/features/docs-lote/view.js`, ao teste estrutural, ao board e ao bundle gerado; todos os produtores de análise foram atualizados, IDs/fluxo foram preservados e não há `textAnalysis` remanescente em `src/`. `dist/js/docs-lote.bundle.js` foi confirmado como saída do build. Gates checker: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 458 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. Próxima fatia: A1-044. |
| A1-044 | 44 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar o wrapper da opção “Forçar atribuição de nomes” (`divInputForceNames`) para `.seipro-doclote-force-names`, preservando checkbox legado | review_passed | c594942 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-043 | `git status --short` ✅ (limpo); `git diff --check` ✅; `npm test` ✅ (60 arquivos, 459 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, painel de cruzamento em SEI 4, opção “Forçar atribuição de nomes na Árvore” e console sem erro novo | Revisão aprovada: o commit c594942 altera apenas o wrapper da opção em `src/features/docs-lote/templates.js`, o teste estrutural, o bundle gerado e o registro do board; preserva `checkForceNames`, `name`, `data-type`, label e comportamento do checkbox. `src/` é a fonte da verdade; `dist/js/docs-lote.bundle.js` foi confirmado como saída do build, sem churn incidental em `dist/js/init_visualizacao.js`. Gates checker executados com output real. Próxima fatia: A1-045. |
| A1-045 | 45 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar as mensagens de validação do wizard (`noFieldsError`) para `.seipro-doclote-field-error`, preservando o legado compartilhado | review_passed | 85ab95e | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-044 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 460 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, validação de campos dinâmicos/CSV inválido e mensagens de erro do cruzamento | Aprovado pelo checker: revisão do commit 85ab95e confirmou escopo restrito aos quatro produtores de mensagens em `src/features/docs-lote/view.js`, teste estrutural, bundle gerado e board; a ocorrência compartilhada `noFieldsError` em `src/shared/legacy/sei-functions-pro.js` foi preservada. `src/` permanece fonte da verdade e `dist/js/docs-lote.bundle.js` coincide com a saída do build; não houve churn incidental em `dist/js/init_visualizacao.js`. Gates reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 460 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Próxima ação: revisar A1-046. |
| A1-046 | 46 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar o hook de título dos campos detectados do wizard (`dFielTitle`) para `.seipro-doclote-field-title`, preservando o texto de análise | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-045 | `git diff --check` ✅; `npm test` ✅ (59 arquivos, 415 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, etapa de análise de documento/CSV e títulos de campos detectados | Compatibilidade preservada: a classe base `seipro-doclote-analysis-text`, ids `fieldList`/`fieldListCSV` e textos do wizard continuam iguais; apenas o hook visual específico dos títulos dos campos detectados foi prefixado. Saída gerada correspondente incluída em `dist/js/docs-lote.bundle.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga em `view.js`. Verificação checker aprovada: o commit `2efa0f8` altera somente os três produtores de títulos em `src/features/docs-lote/view.js`, o teste estrutural, o bundle gerado e o board; preserva `seipro-doclote-analysis-text`, ids `fieldList`/`fieldListCSV` e os textos do wizard. `src/` é a fonte da verdade, e a saída em `dist/js/docs-lote.bundle.js` corresponde ao build atual; não houve churn em `dist/js/init_visualizacao.js`. Gates reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 461 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Próxima ação: checker revisar A1-047. |
| A1-047 | 47 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar o wrapper da barra de ações do resultado (`filterTablePro`) para `.seipro-doclote-result-actions`, preservando botões de baixar/copiar | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-046 | `git diff --check` ✅; `npm test` ✅ (59 arquivos, 416 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, conclusão da geração em lote e botões Baixar/Copiar da tabela de resultados | Compatibilidade preservada: os botões continuam usando classes `seipro-doclote-download`/`seipro-doclote-copy` e a delegação existente; apenas o wrapper visual emitido por `templates.js` deixou de reutilizar a classe legada compartilhada `filterTablePro`. Usos de `filterTablePro` em `shared/legacy`, atividades e lista-processos permanecem intocados fora desta fatia. Saída gerada correspondente incluída em `dist/js/docs-lote.bundle.js`; teste estrutural ampliado trava o novo hook e ausência do wrapper antigo em `templates.js`. Revisão checker aprovada: o commit `63c6e85` limita-se ao wrapper em `src/features/docs-lote/templates.js`, teste estrutural, bundle gerado e board; preserva classes dos botões, `data-value`, `data-icon`, `tableDataResult` e delegação, sem renomear usos compartilhados de `filterTablePro`. `dist/js/docs-lote.bundle.js` coincide com a saída reproduzida pelo build atual. Gates checker executados com output real: árvore limpa antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 462 testes), `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: revisar A1-048. |

| A1-048 | 48 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao wrapper do diálogo (`seipro-doclote-dialog`), preservando a classe compartilhada `dialogBoxDiv` | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-047 | `git status --short` ✅ (limpo antes/depois); `git diff --check` ✅; `npm test` ✅ (60 arquivos, 463 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, abertura dos diálogos do wizard e manutenção da aparência/modal legado | Compatibilidade preservada: o wrapper continua com id `dialogBoxDocLote` e classe compartilhada `dialogBoxDiv`; foi adicionado apenas o hook próprio `seipro-doclote-dialog` para a feature sem alterar ids, botões, campos ou fluxo do wizard. Saída gerada correspondente incluída em `dist/js/docs-lote.bundle.js`; teste estrutural ampliado trava o novo hook e a preservação da classe compartilhada. Revisão checker aprovada: b20562b altera somente o wrapper-fonte, o teste estrutural, o bundle gerado e o registro do board; `src/` é a fonte da verdade, `dist/` coincide com o build e não houve churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: revisar A1-049. |

| A1-049 | 49 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao wrapper de rolagem da tabela de resultado (`seipro-doclote-result-scroll`), preservando a tabela de resultado legada | review_passed | 322df79 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-048 | `git status --short` ✅ (limpo antes/depois); `git diff --check` ✅; `npm test` ✅ (60 arquivos, 464 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, conclusão da geração em lote, rolagem da tabela de resultados e botões Baixar/Copiar | Revisão checker aprovada: o commit 322df79 altera somente o wrapper em `src/features/docs-lote/templates.js`, o teste estrutural, o bundle gerado e o registro do board; preserva `tableDataResult`, as classes compartilhadas da tabela, botões e delegação. `src/` permanece fonte da verdade; `dist/js/docs-lote.bundle.js` coincide com a saída do build, sem churn incidental em `dist/js/init_visualizacao.js`. Gates reais executados após o commit de migração. Próxima ação: revisar A1-050. |

| A1-050 | 50 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao wrapper de rolagem da tabela de cruzamento (`seipro-doclote-crossing-scroll`), preservando a tabela de cruzamento legada | review_passed | 49fb583 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-049 | `git diff --check` ✅; `npm test` ✅ (59 arquivos, 418 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, etapa de cruzamento CSV/modelo, rolagem da tabela de campos relacionados e manutenção dos campos/seletores do painel | Revisão checker aprovada: o commit 49fb583 altera somente o wrapper em `src/features/docs-lote/templates.js`, o teste estrutural, o bundle gerado e o registro do board; preserva `tableDataCrossing`, as classes compartilhadas da tabela, ids, selects, checkbox e fluxo do wizard. `src/` permanece fonte da verdade; `dist/js/docs-lote.bundle.js` coincide com a saída reproduzida pelo build, sem churn incidental em `dist/js/init_visualizacao.js`. Gates reais executados: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 465 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Próxima ação: revisar A1-051. |
| A1-051 | 51 | feature | editor | `ai` / CSS prefixado | Prefixar as linhas de configurações avançadas do modal de IA (`configAI_advancedconfigs`) para `.seipro-ai-advanced-config-row`, preservando checkbox/config legados | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; seleção dinâmica após A1-050 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 465 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de configurações de IA, alternância “Configurações avançadas” e console sem erro novo | Compatibilidade preservada: ids `configAI_advancedconfigs`, `configAI_system_instruction`, `configAI_temperature`, `configAI_max_tokens`, `configAI_top_p`, `configAI_frequency_penalty`, `configAI_presence_penalty` continuam iguais; apenas a classe DOM própria das linhas avançadas foi prefixada e o seletor show/hide passou a consultar `.seipro-ai-advanced-config-row`. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural novo trava o hook prefixado e ausência da classe antiga como classe emitida/seletor. Revisão checker: commit `38a8d04` limitado a `src/`, teste estrutural e bundle gerado; `dist/` coincide com o build e não houve churn incidental em `dist/js/init_visualizacao.js`. Gates reais executados: `git status --short` limpo antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 465 testes), `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. Próxima ação: revisar A1-052. |

| A1-052 | 52 | feature | editor | `ai` / CSS prefixado | Prefixar o hook dos chips de seleção múltipla de documentos (`doc-ai-tag`) para `.seipro-ai-doc-tag`, preservando remoção dos chips | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-051 | `git status --short` ✅ (limpo antes/depois); `git diff --check` ✅; `npm test` ✅ (60 arquivos, 465 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, seleção múltipla de documentos, remoção de chips e envio do prompt sem erro novo | Compatibilidade preservada: o container `docAIMultiList`, dados do chip e o ícone de remoção `doc-ai-tag-remove` continuam iguais; apenas a classe DOM própria do chip foi prefixada e os seletores locais passaram a consultar `.seipro-ai-doc-tag`. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida/seletor. Revisão checker aprovada: o commit `150010a` altera somente a fonte em `src/features/ai/sei-pro-ai.js`, o teste estrutural, o bundle gerado e o registro do board; preserva `docAIMultiList`, os payloads jQuery dos chips e `doc-ai-tag-remove`, sem edição manual de `dist/`. A saída gerada coincide com o build atual e não houve churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: revisar A1-053. |

| A1-053 | 53 | feature | editor | `ai` / CSS prefixado | Prefixar o hook das respostas do usuário no chat de IA (`response_user`) para `.seipro-ai-user-response`, preservando restauração de histórico | review_passed | f8d8713 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-052 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 421 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, envio/restauração de prompts no chat e console sem erro novo | Compatibilidade preservada: o histórico continua salvo com `getHistoryDialog` e restaurado antes de `initFunctionsChat`; apenas a classe DOM própria da mensagem do usuário foi prefixada e o seletor de limpeza em `#response_ai` passou a usar `.seipro-ai-user-response`, mantendo `.response_bot` para fatia futura. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida/seletor. Revisão checker aprovada: o commit `f8d8713` altera somente a fonte em `src/features/ai/sei-pro-ai.js`, o teste estrutural, o bundle gerado e o registro do board; `src/` permanece fonte da verdade, a saída coincide com o build e não houve churn incidental em `dist/js/init_visualizacao.js`. Gates reais executados após a migração: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 465 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Próxima ação: revisar A1-054. |

| A1-054 | 54 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado às respostas do bot no chat de IA (`seipro-ai-bot-response`), preservando a classe legada de estilo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-053 | `git status --short` ✅ (limpo antes/depois); `git diff --check` ✅; `npm test` ✅ (60 arquivos, 465 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração/restauração de respostas do bot, copiar resposta e leitura em voz alta | Revisão aprovada: o commit de migração `035c33f` altera somente `src/features/ai/sei-pro-ai.js`, `tests/structure/ai-css-prefix.test.js`, `dist/js/sei-pro-ai.js` gerado e este registro; preserva `response_bot`/`response_bot_content`, adiciona `seipro-ai-bot-response` nas duas caixas emitidas e move os seletores locais de restauração e leitura para o hook prefixado. O teste estrutural cobre o contrato e a saída gerada coincide com a fonte; não houve churn incidental em `dist/js/init_visualizacao.js`. Gates reais do checker: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 465 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Próxima ação: revisar A1-055. |

| A1-055 | 55 | feature | editor | `ai` / CSS prefixado | Prefixar o hook de adicionar resposta da IA ao editor (`add_doc_response_ai`) para `.seipro-ai-add-doc-response`, preservando o `data-response` | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-054 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 423 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta e ação “Adicionar texto no documento” no editor CKEditor | Compatibilidade preservada: o wrapper da ação mantém `data-response` e chama `addDocResponseAI(this)` via delegação; apenas a classe DOM própria do botão foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida/seletor. Revisão checker aprovada no commit `a632ed6`: `src/` é a fonte da verdade, `dist/js/sei-pro-ai.js` coincide com a saída do build e o escopo permanece restrito ao hook de adicionar resposta. Gates reais do checker: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Próxima ação: revisar A1-056. |

| A1-056 | 56 | feature | editor | `ai` / CSS prefixado | Prefixar o hook de criar documento SEI a partir da resposta da IA (`create_doc_response_ai`) para `.seipro-ai-create-doc-response`, preservando o `data-response` | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-055 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 424 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA fora do editor CKEditor, geração de resposta e ação “Criar documento SEI” | Compatibilidade preservada: o wrapper da ação mantém `data-response` e chama `createDocResponseAI(this)` por delegação; apenas a classe DOM própria do botão foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida/seletor. Revisão checker aprovada: o commit `891ef47` limita-se ao hook de criação de documento, teste estrutural, bundle gerado e registro do board; `src/` é a fonte da verdade, `dist/` coincide com o build e o contrato legado permanece preservado. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima fatia: A1-057. |

| A1-057 | 57 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado à ação de copiar texto simples da resposta da IA (`seipro-ai-copy-response`), preservando a classe legada de estilo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-056 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 425 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta, ação “Copiar texto simples” e duplo clique para cancelar leitura em voz alta | Compatibilidade preservada: o wrapper da ação mantém `copy_response_ai` para o CSS legado de `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-copy-response`; os seletores locais de clique e duplo clique passaram a usar o hook prefixado, mantendo `data-response` e `copyResponseAI(this)`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novos seletores locais pela classe legada. Revisão checker aprovada: o commit de migração `ce74f07` altera somente a fonte em `src/features/ai/sei-pro-ai.js`, o teste estrutural, o bundle gerado e o registro do board; `src/` permanece fonte da verdade, a classe legada `copy_response_ai`, `data-response`, `copyResponseAI(this)` e os fluxos click/dblclick foram preservados, e o bundle coincide com o build sem edição manual de `dist/`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Próxima ação: revisar A1-058. |

| A1-058 | 58 | feature | editor | `ai` / CSS prefixado | Prefixar o hook de copiar texto formatado da resposta da IA (`copy_html_response_ai`) para `.seipro-ai-copy-html-response`, preservando o `data-response` | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-057 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 426 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta e ação “Copiar texto formatado” para colar em documentos | Compatibilidade preservada: o wrapper da ação mantém `data-response` e chama `copyHtmlResponseAI(this)` por delegação; apenas a classe DOM própria do botão foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida/seletor. Próxima ação: A1-059 aguarda revisão checker. Revisão checker aprovada: o commit de migração `6b5f6d4` limita-se ao produtor em `src/features/ai/sei-pro-ai.js`, teste estrutural, bundle gerado e registro do board; preserva `data-response` e `copyHtmlResponseAI(this)`, e remove apenas o hook legado de classe/seletor local. `src/` permanece fonte da verdade; `dist/js/sei-pro-ai.js` coincide com o build reproduzido. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. |

| A1-059 | 59 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado à ação de leitura em voz alta da resposta da IA (`seipro-ai-speech-response`), preservando a classe legada de estilo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-058 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 427 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta, ação “Ouvir em voz alta” e interrupção/convivência com copiar texto simples | Compatibilidade preservada: o wrapper da ação mantém a classe legada `speech_response_ai` para o CSS legado de `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-speech-response`; o seletor local de clique passou a usar o hook prefixado, mantendo `data-response` e leitura a partir da resposta do bot. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novo seletor local pela classe legada. Próxima ação: checker revisar A1-059. Revisão checker aprovada: o commit de migração `62dd04c` altera somente a fonte em `src/features/ai/sei-pro-ai.js`, o teste estrutural, o bundle gerado e o registro do board; preserva `speech_response_ai` para estilo legado, `seipro-ai-speech-response` para delegação, `data-response` e leitura via `.seipro-ai-bot-response`. `src/` permanece fonte da verdade; `dist/js/sei-pro-ai.js` coincide com o build reproduzido, sem edição manual. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Próxima fatia: A1-060 aguarda revisão checker. |

| A1-060 | 60 | feature | editor | `ai` / CSS prefixado | Adicionar hooks prefixados à área/cartões de sugestões do chat de IA (`seipro-ai-suggestions`/`seipro-ai-suggestion-action`), preservando classes legadas de estilo | review_passed | e26ff4f | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-059 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 428 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, exibição de sugestões iniciais, clique em sugestões padrão e favoritos de prompt | Compatibilidade preservada: as classes legadas `suggestions` e `suggestion_actions` permanecem para o CSS legado de `dist/css/sei-pro.css`, mas os seletores locais passaram a usar os hooks próprios `seipro-ai-suggestions` e `seipro-ai-suggestion-action`; atributos `data-send`, `data-type` e dados de documento/processo continuam iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava os novos hooks e evita nova delegação pela classe legada. Checker independente: diff de `e26ff4f` revisado, `src/` confirmado como fonte da verdade, bundle reproduzido pelo build sem churn incidental em `dist/js/init_visualizacao.js`; gates atuais `git status --short` limpo, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 466 testes), `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário. Aprovado. Próxima ação: revisar A1-061. |

| A1-061 | 61 | feature | editor | `ai` / CSS prefixado | Prefixar o hook de remover sugestão favorita da IA (`suggestion_actions_remove`) para `.seipro-ai-remove-suggestion`, preservando índice do favorito | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-060 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 429 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, sugestões favoritas/personalizadas, remoção pelo ícone de lixeira e persistência da lista | Compatibilidade preservada: cartões de sugestão continuam com `suggestion_actions` para CSS legado e `seipro-ai-suggestion-action` para a delegação principal; apenas o hook local do ícone de remoção de favorito foi prefixado. O atributo `data-index` e a chamada `removeFavoritePromptByIndex` permanecem iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência do seletor antigo. Revisão checker do commit `9a5f27f`: diff restrito ao produtor em `src/features/ai/sei-pro-ai.js`, teste estrutural e bundle gerado; `src/` confirmado como fonte da verdade, sem edição manual de `dist/`, e contrato de remoção preservado. Gates checker atuais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. Próxima ação: seguir para A1-062, mantendo uma revisão por execução. |
| A1-062 | 62 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado às referências de documento do prompt (`seipro-ai-prompt-ref-doc`), preservando classe legada de estilo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-061 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 430 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, prompt personalizado com referências de documento/processo, seleção de documentos e envio do prompt | Revisão aprovada: diff pequeno e coerente em `src/features/ai/sei-pro-ai.js`, teste estrutural e saída gerada correspondente em `dist/js/sei-pro-ai.js`; classe legada, atributos de referência e classes dinâmicas foram preservados, e o seletor local passou a usar apenas o hook prefixado. Gate do maker preservado como histórico (430 testes); checker rerodou os gates no estado atual (466 testes), sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: migration job selecionar a próxima fatia, A1-063. |

| A1-063 | 63 | feature | editor | `ai` / CSS prefixado | Prefixar o hook de remoção dos chips de documento (`doc-ai-tag-remove`) para `.seipro-ai-doc-tag-remove`, preservando o chip selecionado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-062 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 430 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, seleção múltipla de documentos, remoção de chips e envio do prompt | Compatibilidade preservada: os chips continuam usando `seipro-ai-doc-tag`, dados jQuery (`id_documento`, `num_processo`, `id_procedimento`, `name`) e container `docAIMultiList`; apenas o ícone/delegação de remoção foi prefixado para `seipro-ai-doc-tag-remove`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência do seletor antigo. Revisão checker do commit `545941b`: diff restrito ao produtor em `src/features/ai/sei-pro-ai.js`, teste estrutural e bundle gerado; `src/` confirmado como fonte da verdade, sem edição manual de `dist/`, e contrato de remoção preservado. Gates checker atuais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. Próxima ação: revisar A1-064. |
| A1-064 | 64 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao conteúdo textual das respostas da IA (`seipro-ai-response-content`), preservando a classe legada de estilo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-063 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 431 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta, copiar texto simples/formatado, adicionar ao editor e criar documento SEI | Compatibilidade preservada: spans de conteúdo continuam com `response_bot_content` para o CSS legado em `dist/css/sei-pro.css`, mas recebem o hook próprio `seipro-ai-response-content`; seletores locais de copiar/adicionar/criar documento passaram a consultar o hook prefixado. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novos seletores locais pela classe legada. Próxima ação: checker revisar A1-064. Revisão checker aprovada: commit de migração `f77bd75` revisado; a mudança é uma única fatia, altera `src/` como fonte da verdade, inclui apenas o bundle gerado correspondente e preserva `response_bot_content` para o CSS legado, adicionando o hook prefixado a todos os produtores de conteúdo. Os seletores locais de copiar/adicionar/criar documento usam o novo hook. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima ação: revisar A1-065.

| A1-065 | 65 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao alvo da animação BotPro (`seipro-ai-icon`), preservando a classe legada de estilo | review_passed | 1eecff3 | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-064 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 432 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, abertura do chat e animação/ícone BotPro inicial | Compatibilidade preservada: o contêiner continua com `icon_ia` para o CSS legado em `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-icon`; chamadas de `adicionarVideoWebM` passaram a mirar o hook prefixado. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novos alvos pela classe legada. Checker independente do commit `1eecff3`: diff restrito a uma única fatia em `src/features/ai/sei-pro-ai.js`, teste estrutural e bundle correspondente; `src/` confirmado como fonte da verdade, `dist/js/sei-pro-ai.js` confirmado como cópia verbatim da fonte no commit, e contratos `icon_ia`, alvo da animação e chamadas `adicionarVideoWebM` preservados. Gates reais no checkout atual: `git status --short` limpo antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 466 testes), `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima ação: checker revisar A1-066. |

| A1-066 | 66 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao wrapper do diálogo de IA (`seipro-ai-dialog`), preservando a classe legada de estilo | review_passed | 5043fe4 | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-065 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 433 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA e manutenção da aparência/fluxo do diálogo | Compatibilidade preservada: o wrapper continua com `dialogBoxAI` para o CSS legado de `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-dialog`; ids, botões, seletores internos e fluxo do chat não mudaram. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e a preservação da classe legada. Revisão independente: diff do commit `5043fe4` restrito a uma fatia em `src/features/ai/sei-pro-ai.js`, teste estrutural e bundle gerado correspondente; o wrapper preserva `dialogBoxAI`, ids, botões, seletores internos e fluxo. `src/` confirmado como fonte da verdade. O build atual contém fatias posteriores no mesmo bundle, portanto a comparação do artefato foi feita contra o estado atual e não é usada para reescrever a evidência histórica do maker. Gates reais no checkout atual: `git status --short` limpo antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 466 testes), `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima ação: checker revisar A1-067. |
| A1-067 | 67 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao bloco de boas-vindas do chat de IA (`seipro-ai-welcome`), preservando a classe legada de estilo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-066 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 434 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA e visualização da mensagem inicial/ícone BotPro | Compatibilidade preservada: o bloco inicial continua com a classe legada `welcome` para o CSS legado em `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-welcome`; ícone `icon_ia seipro-ai-icon`, ids e fluxo do chat não mudaram. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e a preservação da classe legada. Checker independente do commit `6550692`: diff restrito a uma única fatia em `src/features/ai/sei-pro-ai.js`, teste estrutural e bundle correspondente; `src/` confirmado como fonte da verdade, `dist/js/sei-pro-ai.js` confirmado como cópia verbatim da fonte após o build, e contratos `welcome`, `icon_ia seipro-ai-icon`, ids e fluxo do chat preservados. Gates reais no checkout atual: `git status --short` limpo antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 466 testes), `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima ação: checker revisar A1-068. |

| A1-068 | 68 | feature | editor | `ai` / CSS prefixado | Prefixar o wrapper da barra de prompt do chat de IA (`input_prompt`) para `.seipro-ai-prompt-bar`, preservando campos de prompt/documento | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-067 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 435 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA, seleção de prompt/documento e digitação de prompt personalizado | Compatibilidade preservada: os ids `promptAISelect`, `docAISelect`, `docAIMultiList`, `promptAIPersonal` e o fluxo de envio do prompt continuam iguais; apenas a classe DOM própria do wrapper da barra de prompt foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência de `input_prompt` como classe emitida. Revisão checker independente do commit `86be4b7`: diff restrito a uma única fatia em `src/features/ai/sei-pro-ai.js`, teste estrutural e bundle gerado correspondente; `src/` confirmado como fonte da verdade, `dist/js/sei-pro-ai.js` confirmado como cópia verbatim da fonte após o build, e os ids de prompt/documento foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 466 testes), `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima ação: checker revisar A1-069. |

| A1-069 | 69 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do seletor de prompt do chat de IA (`prompt_type`) para `.seipro-ai-prompt-select`, preservando o id do select | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-068 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 436 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA, seleção de prompts padrão/personalizados e envio do prompt | Compatibilidade preservada: o select continua com id `promptAISelect`, opções de `buildPromptOptions()` e fluxo Chosen/delegação por id; apenas a classe DOM própria do seletor de prompt foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência de `prompt_type` como classe emitida. Revisão checker aprovada: o commit de migração `f30b4fe` limita-se à fonte em `src/features/ai/sei-pro-ai.js`, ao teste estrutural, ao bundle gerado correspondente e ao registro do board; `src/` é a fonte da verdade, o `dist/` foi regenerado pelo build e o contrato do select permanece preservado. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-070 aguarda revisão checker. |
| A1-070 | 70 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do seletor de documentos do chat de IA (`prompt_doc`) para `.seipro-ai-doc-select`, preservando o id do select | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-069 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 437 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA, seleção de documento/processo e envio do prompt | Compatibilidade preservada: o select continua com id `docAISelect`, carregamento via `getDocsArvore_fillSelect()` e fluxos Chosen/delegação por id; apenas a classe DOM própria do seletor de documentos foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência de `prompt_doc` como classe emitida. Revisão checker aprovada: diff de `4166d9a` restrito ao hook do seletor de documentos, teste estrutural, bundle gerado e registro do board; `src/` é a fonte da verdade, `dist/js/sei-pro-ai.js` coincide com a fonte após o build, o id `docAISelect`, `getDocsArvore_fillSelect()` e os fluxos Chosen/delegação por id permanecem preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-071 aguarda revisão checker. |
| A1-071 | 71 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao modo de prompt personalizado do seletor de documento (`seipro-ai-personal-prompt-mode`), preservando a classe legada de CSS | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-070 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 438 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA, prompt personalizado, seleção de documento para inserir referência no prompt e retorno ao seletor padrão | Compatibilidade preservada: o estado escolhido do `docAISelect_chosen` mantém `prompt_personal` para o CSS legado de `dist/css/sei-pro.css`, mas passa a receber também o hook próprio `seipro-ai-personal-prompt-mode`; ids `promptAIPersonal`, `docAISelect`, fluxo Chosen e opções de seleção permanecem iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e a preservação da classe legada. Próxima ação: checker revisar A1-071. Revisão checker aprovada: o commit de migração `b18de40` altera somente os dois pontos de estado Chosen em `src/features/ai/sei-pro-ai.js`, o teste estrutural e o bundle gerado; preserva `prompt_personal`, ids, fluxo de seleção e compatibilidade de CSS legado. `src/` é a fonte da verdade e `dist/js/sei-pro-ai.js` coincide com a fonte após o build. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-072 aguarda revisão checker. |

| A1-072 | 72 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao modo de seletor de documentos no editor (`seipro-ai-editor-doc-mode`), preservando a classe legada de CSS | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-071 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 439 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA no editor CKEditor, seletor de documento/processo e envio de prompt | Compatibilidade preservada: o estado Chosen de `docAISelect_chosen` mantém `prompt_editor_sei` para o CSS legado de `dist/css/sei-pro.css`, mas recebe também o hook próprio `seipro-ai-editor-doc-mode`; ids, opções, Chosen e fluxo de prompt/documento permanecem iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e a preservação da classe legada. Próxima ação: checker revisar A1-072. Revisão checker aprovada: commit `18674e0` revisado; a fatia altera apenas o hook adicional no estado Chosen do editor, o teste estrutural, o bundle gerado e o registro do board. `src/` é a fonte da verdade; `prompt_editor_sei`, ids, opções e fluxo Chosen foram preservados, e `dist/js/sei-pro-ai.js` coincide com `src/features/ai/sei-pro-ai.js` após o build. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-073 aguarda revisão checker. |

| A1-073 | 73 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do botão de salvar credencial Gemini (`gemini_token`) para `.seipro-ai-gemini-token`, preservando detecção da plataforma | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-072 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 440 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações de IA, selecionar Gemini, salvar credencial e confirmar que a plataforma detectada continua Gemini | Compatibilidade preservada: o botão mantém id `plataformAI_uiElement`, rótulo/inputs e classes compartilhadas do CKEditor; apenas o hook próprio `gemini_token` foi prefixado e a detecção em `saveTokenOpenAI` passou a consultar `.seipro-ai-gemini-token`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência do seletor antigo. Próxima ação: checker revisar A1-073. Revisão checker aprovada: commit `d757a50` revisado; a fatia altera somente o hook do botão Gemini, a detecção correspondente, o teste estrutural e o bundle gerado. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` coincide com `src/features/ai/sei-pro-ai.js` após o build, sem edição manual. O id `plataformAI_uiElement`, o campo secreto, classes compartilhadas do CKEditor e a detecção Gemini foram preservados. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-074. |

| A1-074 | 74 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do botão de salvar credencial OpenAI (`openai_token`) para `.seipro-ai-openai-token`, preservando detecção da plataforma | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-073 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 441 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações de IA, selecionar OpenAI, salvar credencial e confirmar que a plataforma detectada continua OpenAI | Compatibilidade preservada: o botão mantém id `plataformAI_uiElement`, rótulo/inputs e classes compartilhadas do CKEditor; apenas o hook próprio `openai_token` foi prefixado e a detecção em `saveTokenOpenAI` passou a consultar `.seipro-ai-openai-token`, mantendo fallback OpenAI. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência do seletor antigo. Próxima ação: checker revisar A1-074. Revisão checker aprovada: commit `8a2f8b0` revisado; a fatia altera somente o hook do botão OpenAI, a detecção correspondente, o teste estrutural e o bundle gerado. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` coincide com `src/features/ai/sei-pro-ai.js` após o build, sem edição manual. O id `plataformAI_uiElement`, o campo secreto, classes compartilhadas do CKEditor e o fallback OpenAI foram preservados. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-075 aguarda revisão checker. |

| A1-075 | 75 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do botão de salvar credencial Ollama (`ollama_token`) para `.seipro-ai-ollama-token`, preservando detecção da plataforma | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-074 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 442 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações de IA, selecionar/configurar Ollama ou LiteLLM, salvar credencial/URL/modelo e confirmar que a plataforma detectada continua Ollama | Compatibilidade preservada: o botão mantém id `plataformAI_uiElement`, rótulo, campos `cke_inputOllamaUrl_textInput`/`cke_inputOllamaKey_textInput`/`cke_inputOllamaModel_textInput` e classes compartilhadas do CKEditor; apenas o hook próprio `ollama_token` foi prefixado e a detecção em `saveTokenOpenAI` passou a consultar `.seipro-ai-ollama-token`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência do seletor antigo. Próxima ação: checker revisar A1-075. Revisão checker aprovada: commit `cd37272` revisado; a fatia altera somente o hook do botão Ollama, a detecção correspondente, o teste estrutural e o bundle gerado. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` coincide com `src/features/ai/sei-pro-ai.js` após o build, sem edição manual. O id `plataformAI_uiElement`, os campos de URL/chave/modelo e as classes compartilhadas do CKEditor foram preservados. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-076 aguarda revisão checker. |

| A1-076 | 76 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do checkbox de animação de digitação (`resume_doc`) para `.seipro-ai-typing-toggle`, preservando id/config e estilo legado do switch | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-075 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 443 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, alternar “Animação de texto digitando”, salvar e confirmar que a opção continua persistindo e afetando a digitação | Compatibilidade preservada: o checkbox mantém id `configAI_typing_box`, `name="infraAncoraSigla"`, label e persistência via `setTypingAI`; apenas a classe própria `resume_doc` foi substituída por `seipro-ai-typing-toggle`, preservando a classe compartilhada `infraLinkOrgao`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga. Próxima ação: checker revisar A1-076. Revisão checker aprovada: commit `e118f0e` revisado; a fatia altera apenas o hook do checkbox em `src/features/ai/sei-pro-ai.js`, o teste estrutural, o bundle gerado e o registro do board. `src/` permanece fonte da verdade; `dist/js/sei-pro-ai.js` coincide com a fonte após o build; ids, label, `name`, `setTypingAI` e a classe compartilhada `infraLinkOrgao` foram preservados, sem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-077. |

| A1-077 | 77 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao checkbox de modelos beta (`seipro-ai-beta-models-toggle`), preservando id/config e estilo legado do switch | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-076 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 444 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, alternar “Listar modelos beta”, salvar e confirmar que a lista/modelos beta continuam persistindo | Compatibilidade preservada: o checkbox mantém id `configAI_betamodels`, `name` legado, label e persistência via `setBetaModelsAI`; apenas foi adicionado o hook próprio `seipro-ai-beta-models-toggle`, preservando a classe compartilhada `infraLinkOrgao`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-077. Revisão checker aprovada: commit `1b451f0` revisado; a fatia altera somente o hook do checkbox em `src/features/ai/sei-pro-ai.js`, o teste estrutural, o bundle gerado e o registro do board. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` coincide byte a byte com a fonte após o build, sem edição manual e sem churn incidental em `dist/js/init_visualizacao.js`. O id `configAI_betamodels`, `name`, label, `setBetaModelsAI` e a classe compartilhada `infraLinkOrgao` foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-078. |

| A1-078 | 78 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao checkbox de configurações avançadas (`seipro-ai-advanced-config-toggle`), preservando id/config e estilo legado do switch | review_passed | 67582e3 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-077 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 445 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, alternar “Configurações avançadas”, salvar e confirmar exibição/persistência dos campos avançados | Compatibilidade preservada: o checkbox mantém id `configAI_advancedconfigs`, `name` legado, label e persistência via `setAdvancedConfigs`; apenas foi adicionado o hook próprio `seipro-ai-advanced-config-toggle`, preservando a classe compartilhada `infraLinkOrgao`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-078. Revisão checker aprovada: commit `67582e3` revisado; a fatia limita-se ao hook adicional do checkbox em `src/features/ai/sei-pro-ai.js`, ao teste estrutural, ao bundle gerado e ao registro do board. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` coincide byte a byte com a fonte após o build, sem edição manual. O id `configAI_advancedconfigs`, `name`, label, `setAdvancedConfigs` e a classe compartilhada `infraLinkOrgao` foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-079. |

| A1-079 | 79 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado à linha de modelo manual Ollama (`seipro-ai-manual-model-row`), preservando id/campo de modelo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-078 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 446 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA na plataforma Ollama/LiteLLM, informar modelo manual, salvar e confirmar persistência/uso do modelo | Compatibilidade preservada: a linha mantém id `configAI_ollama_model_row`, campo `configAI_manual_model`, label e persistência via `setModelOllama`; apenas foi adicionado o hook próprio `seipro-ai-manual-model-row`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-079. Revisão checker aprovada: commit `fb5652a` revisado; a fatia altera somente o hook da linha de modelo manual em `src/features/ai/sei-pro-ai.js`, o teste estrutural, o bundle gerado e o registro do board. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` coincide byte a byte com a fonte após o build, sem edição manual e sem churn incidental em `dist/js/init_visualizacao.js`. O id `configAI_ollama_model_row`, o campo `configAI_manual_model`, label e persistência via `setModelOllama` foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-080. |
| A1-080 | 80 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao seletor de modelo da IA (`seipro-ai-model-select`), preservando id e persistência | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-079 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 447 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, escolher modelo por plataforma, salvar e confirmar persistência/uso do modelo | Compatibilidade preservada: o select mantém id `configAI_model`, opções de `optionsModels()` e persistência por `setModelOpenAI`/`setModelGemini`/fallback Ollama; apenas foi adicionado o hook próprio `seipro-ai-model-select`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-080. Revisão checker aprovada: commit `e9c168a` revisado; a fatia limita-se ao hook adicional do select em `src/features/ai/sei-pro-ai.js`, teste estrutural, bundle gerado e registro do board. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` coincide byte a byte com `src/features/ai/sei-pro-ai.js` após o build, sem edição manual. O id `configAI_model`, `for`, `optionsModels()` e os fluxos de persistência OpenAI/Gemini/Ollama foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-081 aguarda revisão checker. |

| A1-081 | 81 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao aviso de consentimento de IA (`seipro-ai-consent-disclaimer`), preservando a classe legada de estilo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-080 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 448 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir o modal de IA, visualizar o aviso de consentimento e marcar o checkbox antes de prosseguir | Compatibilidade preservada: o bloco de consentimento mantém `editorTextDisclaimer` para o CSS legado em `dist/css/sei-slim.css`, mas recebe o hook próprio `seipro-ai-consent-disclaimer`; id `cke_382_uiElement`, checkbox `ciente_disclaimer`, label e fluxo de consentimento permanecem iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/checkbox. Próxima ação: checker revisar A1-081. Revisão checker aprovada: commit `2205126` revisado; a fatia limita-se ao hook adicional do aviso de consentimento em `src/features/ai/sei-pro-ai.js`, teste estrutural e bundle gerado. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` coincide byte a byte com a fonte após o build, sem edição manual. `editorTextDisclaimer`, `cke_382_uiElement`, `ciente_disclaimer`, label e fluxo de consentimento foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-082.

| A1-082 | 82 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao link de restauração do histórico da IA (`seipro-ai-history-link`), preservando o id legado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-081 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 449 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir o modal de IA, gerar/restaurar histórico do diálogo e confirmar que o link Ver Histórico continua funcionando | Compatibilidade preservada: o link mantém o id `historyDialogAI` e o storage `historyDialogAI`; apenas recebe o hook próprio `seipro-ai-history-link`, e a restauração/click delegado passam a usar a classe prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novos seletores locais pelo id legado. Próxima ação: checker revisar A1-082. Revisão checker aprovada: commit `2ee63e0` revisado; a fatia é pequena e limitada ao hook do link de histórico, teste estrutural, bundle gerado e registro do board. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` coincide byte a byte com a fonte após o build, sem edição manual. O id/storage `historyDialogAI`, restauração, delegação e fluxo de `getHistoryDialog('restore')` foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-083.

| A1-083 | 83 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de instrução de sistema da IA (`seipro-ai-system-instruction`), preservando id/config e persistência | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-082 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 450 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar instrução de sistema, salvar e confirmar persistência/uso no chat | Compatibilidade preservada: o textarea mantém id `configAI_system_instruction`, label e persistência via `setSystemInstructionAI`; apenas recebeu o hook próprio `seipro-ai-system-instruction`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-083. Revisão checker aprovada: commit de migração `afeae1b` revisado por diff exato; a fatia adiciona somente o hook do textarea, seu teste estrutural, o bundle gerado e o registro do board. `src/` é a fonte da verdade, `dist/js/sei-pro-ai.js` coincide byte a byte com `src/features/ai/sei-pro-ai.js` após o build, e id, label e persistência foram preservados. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-084.

| A1-084 | 84 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de temperatura da IA (`seipro-ai-temperature-input`), preservando id/config e persistência | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-083 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 451 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar temperatura, salvar e confirmar persistência/uso na geração | Compatibilidade preservada: o input mantém id `configAI_temperature`, label e persistência via `setTemperatureAI`; apenas recebeu o hook próprio `seipro-ai-temperature-input`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-084. Revisão checker aprovada: commit de migração `8cd2f5f` revisado por diff exato; a fatia é pequena e limitada ao hook do campo de temperatura, teste estrutural, bundle gerado e registro do board. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` coincide byte a byte com `src/features/ai/sei-pro-ai.js` após o build, sem edição manual. O id `configAI_temperature`, label, faixa/tipo do input e persistência via `setTemperatureAI` foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-085. |

| A1-085 | 85 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de comprimento máximo da IA (`seipro-ai-max-tokens-input`), preservando id/config e persistência | review_passed | 8d18556 | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-084 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 452 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar comprimento máximo, salvar e confirmar persistência/uso na geração | Compatibilidade preservada: o input mantém id `configAI_max_tokens`, label e persistência via `setMaxTokenAI`; apenas recebeu o hook próprio `seipro-ai-max-tokens-input`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-085. Revisão checker aprovada: commit `8d18556` revisado por diff exato; a fatia é pequena e limitada ao hook do campo de comprimento máximo, teste estrutural e bundle gerado. `src/` é a fonte da verdade; o bundle coincide com a saída do build atual, sem edição manual e sem churn incidental em `dist/js/init_visualizacao.js`. O id `configAI_max_tokens`, label, tipo/faixa/step e persistência via `setMaxTokenAI` foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-086. |

| A1-086 | 86 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo Top P da IA (`seipro-ai-top-p-input`), preservando id/config e persistência | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-085 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 453 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar Parte superior P/Top P, salvar e confirmar persistência/uso na geração | Compatibilidade preservada: o input mantém id `configAI_top_p`, label e persistência via `setTopPAI`; apenas recebeu o hook próprio `seipro-ai-top-p-input`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-086. Revisão checker aprovada: commit de migração `ed81368` revisado por diff exato; a fatia é pequena e limitada ao hook do campo Top P, teste estrutural, bundle gerado e registro do board. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` coincide byte a byte com `src/features/ai/sei-pro-ai.js` após o build, sem edição manual. O id `configAI_top_p`, label, tipo/faixa/step e persistência via `setTopPAI` foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-087. |

| A1-087 | 87 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de penalidade de frequência da IA (`seipro-ai-frequency-penalty-input`), preservando id/config e persistência | review_passed | b92debe | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-086 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 454 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar penalidade de frequência, salvar e confirmar persistência/uso na geração | Compatibilidade preservada: o input mantém id `configAI_frequency_penalty`, label e persistência via `setFrequencyPenaltyAI`; apenas recebeu o hook próprio `seipro-ai-frequency-penalty-input`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-087. Revisão checker: commit `b92debe` revisado por diff exato; a fatia adiciona somente o hook do campo de penalidade de frequência, teste estrutural, bundle gerado e registro do board. `src/` é a fonte da verdade; o contrato `configAI_frequency_penalty`, label, faixa/tipo/step e persistência via `setFrequencyPenaltyAI` foi preservado. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-088. |

| A1-088 | 88 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de penalidade de presença da IA (`seipro-ai-presence-penalty-input`), preservando id/config e persistência | review_passed | 4c5b24a | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-087 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 455 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar penalidade de presença, salvar e confirmar persistência/uso na geração | Compatibilidade preservada: o input mantém id `configAI_presence_penalty`, label, tipo/range/step e persistência via `setPresencePenaltyAI`; apenas recebeu o hook próprio `seipro-ai-presence-penalty-input`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-088. Revisão checker aprovada: commit `4c5b24a` revisado por diff exato; a fatia é pequena e limitada ao hook do campo de penalidade de presença, teste estrutural, bundle gerado e registro do board. `src/` é a fonte da verdade; o id `configAI_presence_penalty`, label, tipo/range/step e persistência via `setPresencePenaltyAI` foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build, sem edição manual de `dist/`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-089. |

| A1-089 | 89 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de modelo manual Ollama (`seipro-ai-manual-model-input`), preservando id/config e persistência | review_passed | 7861222 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-088 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 456 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em configurar modelo manual Ollama/LiteLLM, salvar e confirmar persistência/uso | Compatibilidade preservada: o input mantém id `configAI_manual_model`, placeholder, valor e persistência via `setModelOllama`; apenas recebe o hook próprio da feature. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o hook e os contratos de id/persistência. Revisão checker: commit `7861222` revisado por diff exato; a fatia é pequena e limitada ao hook do input de modelo manual, teste estrutural, bundle gerado e registro do board. `src/` é a fonte da verdade; o id `configAI_manual_model`, label, placeholder, valor e persistência via `setModelOllama` foram preservados. `npm test` reconstruiu os artefatos sem churn incidental em `dist/js/init_visualizacao.js`; não houve edição manual de `dist/`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-090. |

| A1-090 | 90 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao cursor de digitação da resposta (`seipro-ai-typing-cursor`), preservando a classe legada de estilo | review_passed | b59c8ef | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-089 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 457 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta com animação de digitação e cursor piscante | Compatibilidade preservada: o cursor mantém `blinker` para o CSS legado em `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-typing-cursor`; o fluxo de renderização incremental e a resposta final permanecem iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava os dois hooks. Próxima ação: checker revisar A1-090. Revisão checker: commit `b59c8ef` revisado por diff exato; a fatia adiciona somente o hook de cursor, teste estrutural, bundle gerado e registro do board. `src/` é a fonte da verdade; a classe legada `blinker`, a renderização incremental e a resposta final foram preservadas. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build, sem edição manual de `dist/`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-091. |

| A1-091 | 91 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao botão de adicionar documento (`seipro-ai-add-document`), preservando o id legado | review_passed | e186ebe | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-090 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 458 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, adicionar documento à seleção múltipla, remover chip e enviar prompt | Compatibilidade preservada: o link mantém id `btnAddDocAI`, classe compartilhada `newLink`, tooltip e chamada `addDocToMultiList`; o listener delegado local usa o novo hook da feature. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava hook, id e delegação. Próxima ação: checker revisar A1-091. Revisão checker: commit `e186ebe` revisado pelo diff exato; a fatia é pequena e limitada ao hook do botão de adicionar documento, teste estrutural, cópia gerada e registro no board. `src/` é a fonte da verdade; o id `btnAddDocAI`, a classe compartilhada `newLink`, tooltip e chamada `addDocToMultiList` foram preservados, e apenas a delegação local passou ao hook prefixado. `cmp` confirmou `src/features/ai/sei-pro-ai.js` idêntico a `dist/js/sei-pro-ai.js` após o build, sem edição manual de `dist/`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-092. |

| A1-092 | 92 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao botão de configurações de IA (`seipro-ai-config-button`), preservando o id legado | review_passed | 1c882c1 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-091 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 459 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir o modal de IA, acionar Configurações Gerais e salvar/fechar sem erro novo | Compatibilidade preservada: o link mantém id `btnConfigAI`, classe compartilhada `newLink`, tooltip e chamada `configAI(this)`; somente a delegação local passou para o novo hook da feature. Saída gerada correspondente: `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava hook, id e delegação. Próxima ação: checker revisar A1-092. Revisão checker aprovada: o commit `1c882c1` foi revisado pelo diff exato; a fatia é pequena e limitada ao hook do botão, teste estrutural, bundle gerado e registro do board. `src/` é a fonte da verdade; `btnConfigAI`, `newLink`, tooltip e `configAI(this)` permanecem preservados, e a delegação local usa apenas `.seipro-ai-config-button`. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-093. |

| A1-093 | 93 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao botão de envio de prompt (`seipro-ai-send-button`), preservando o id legado | review_passed | 20b060f | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-092 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 460 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, envio de prompt por botão e atalho, e ausência de erro novo | Compatibilidade preservada: o link mantém id `btnSendAI`, classe compartilhada `newLink`, ícone/texto e chamada `initAI(this)`; somente a delegação local usa o novo hook da feature. Saída gerada esperada: `dist/js/sei-pro-ai.js`; teste estrutural cobre hook, id e delegação. Revisão checker aprovada: o commit `20b060f` foi revisado pelo diff exato; a fatia altera apenas o hook do botão de envio, sua delegação local, o teste estrutural, a cópia gerada e o registro no board. `src/` é a fonte da verdade; `id="btnSendAI"`, classe `newLink`, ícone/texto, `initAI(this)`, o atalho de teclado e os usos internos do id foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` idêntico a `dist/js/sei-pro-ai.js` após o build; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-094. |
| A1-094 | 94 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao botão de alternar plataforma da IA (`seipro-ai-switch-platform`), preservando o id legado | review_passed | 6934d5a | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-093 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 461 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA com duas ou mais plataformas configuradas, alternância de plataforma e persistência da plataforma ativa | Compatibilidade preservada: o link mantém id `btnChangeSelectedAI`, classe compartilhada `newLink`, tooltip e persistência por `setOptionsPro`; somente a delegação local passa a usar o novo hook da feature. Saída gerada correspondente: `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava hook, id, delegação e persistência. Revisão checker aprovada: o commit `6934d5a` foi revisado pelo diff exato; a fatia é pequena e limitada ao hook do botão, sua delegação local, teste estrutural, cópia gerada e registro do board. `src/` é a fonte da verdade; o id `btnChangeSelectedAI`, a classe compartilhada `newLink`, tooltip e persistência via `setOptionsPro` foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` idêntico a `dist/js/sei-pro-ai.js` após o build, sem edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-095. |

| A1-095 | 95 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de chave Ollama (`seipro-ai-ollama-key-input`), preservando o comportamento legado de senha | review_passed | 023f3f4 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-094 | `git diff --check` ✅; `npm test` ✅; `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em configurações de IA para Ollama/LiteLLM, preenchimento da chave e salvamento da credencial | Compatibilidade preservada: o input mantém id `cke_inputOllamaKey_textInput`, tipo `password` e classe legada `passReveal`; foi adicionado apenas o hook próprio `seipro-ai-ollama-key-input`. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de senha. Próxima ação: checker revisar A1-047 e, depois, esta fatia na ordem do board. Revisão checker: commit `023f3f4` revisado pelo diff exato; a fatia é pequena e limitada ao hook do campo de chave Ollama, teste estrutural, cópia gerada e registro no board. `src/` é a fonte da verdade; o id `cke_inputOllamaKey_textInput`, tipo `password`, classe legada `passReveal` e fluxo de credencial foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-096. |
| A1-096 | 96 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de URL do servidor Ollama (`seipro-ai-ollama-url-input`), preservando id e endpoint padrão | review_passed | f7f0fa1 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-095 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 463 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em configurações de IA para Ollama/LiteLLM, editar a URL do servidor, salvar e confirmar persistência/conexão | Compatibilidade preservada: o input mantém id `cke_inputOllamaUrl_textInput`, tipo `text`, endpoint padrão e leitura por `saveTokenOpenAI`; foi adicionado apenas o hook próprio `seipro-ai-ollama-url-input`. Saída gerada esperada: `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o hook e os contratos de id/endpoint. Próxima ação: checker revisar A1-096. Revisão checker aprovada: commit `f7f0fa1` revisado pelo diff exato; a fatia adiciona somente o hook do campo de URL Ollama, o teste estrutural e o bundle gerado. `src/` é a fonte da verdade, o input preserva id, tipo, endpoint padrão e leitura por `saveTokenOpenAI`, e não houve edição manual de `dist/`. `cmp` confirmou a fonte e a cópia gerada idênticas após o build; `dist/js/init_visualizacao.js` não teve churn incidental. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-097. |

| A1-097 | 97 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de modelo do servidor Ollama (`seipro-ai-ollama-model-input`), preservando id e fluxo de modelo | review_passed | edea6dc | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-096 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 464 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em configurações de IA para Ollama/LiteLLM, informar o modelo padrão, salvar e confirmar persistência/uso | Compatibilidade preservada: o input mantém id `cke_inputOllamaModel_textInput`, tipo `text`, placeholder e leitura por `saveTokenOpenAI`; foi adicionado apenas o hook próprio `seipro-ai-ollama-model-input`. Saída gerada correspondente esperada: `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o hook e os contratos de id/modelo. Próxima ação: checker revisar A1-097. Revisão checker aprovada: commit `edea6dc` revisado pelo diff exato; a fatia adiciona somente o hook do input de modelo Ollama, o teste estrutural, o bundle gerado e o registro no board. `src/` é a fonte da verdade; o id `cke_inputOllamaModel_textInput`, tipo, placeholder, leitura por `saveTokenOpenAI` e persistência via `setModelOllama` foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build, sem edição manual de `dist/` e sem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-098.

| A1-098 | 98 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado aos campos de chave secreta OpenAI/Gemini (`seipro-ai-secret-key-input`), preservando o contrato de senha | review_passed | 7842d09 | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-097 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 465 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações de IA OpenAI/Gemini, inserir/salvar credencial e confirmar que os campos continuam mascarados | Compatibilidade preservada: os dois campos mantêm id `cke_inputSecretKey_textInput`, tipo `password`, rótulo ARIA e o fluxo existente de salvar credencial; foi adicionado apenas o hook próprio `seipro-ai-secret-key-input`. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural cobre as duas ocorrências e o contrato de senha. Próxima ação: checker revisar A1-050 primeiro e, depois, esta fatia na ordem do board. Revisão checker: commit `7842d09` revisado pelo diff exato; fatia única e pequena, limitada ao hook dos dois campos de chave secreta, teste estrutural, cópia gerada e registro no board. `src/` é a fonte da verdade; ids, tipo `password`, rótulo ARIA e fluxo de salvamento foram preservados. Rebuild atual deixou a árvore limpa e `dist/js/sei-pro-ai.js` coincide com o artefato versionado; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-099. |

| A1-099 | 99 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao retorno do prompt personalizado (`seipro-ai-return-prompt-select`), preservando o id legado | review_passed | 4c7b789 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-098 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no prompt personalizado, retorno ao seletor padrão e restauração dos controles de documento | Compatibilidade preservada: o link mantém id `btnReturnSelectPromptAI`, classe compartilhada `newLink`, ícone e fluxo que restaura os seletores/oculta prompt personalizado; somente a delegação local passou para o novo hook da feature. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural cobre hook, id, delegação e fluxo de retorno. Próxima ação: revisão concluída pelo checker. Diff focado do commit `4c7b789` confirmou uma única fatia; `src/` é a fonte da verdade, `dist/js/sei-pro-ai.js` coincide com a cópia gerada por build e não houve edição manual nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 466 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-100 | 100 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao indicador de plataforma ativa (`seipro-ai-platform-status`), preservando animação/cores legadas | review_passed | 12e68e2 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-099 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 467 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, indicador da plataforma ativa e animação visual sem erro novo | Compatibilidade preservada: o ícone de status no botão da plataforma principal mantém `animate-flicker`, `verdeColor`, ícone Font Awesome e posicionamento inline; foi adicionado apenas o hook próprio `seipro-ai-platform-status`. Teste estrutural ampliado cobre o novo hook e a saída gerada correspondente está em `dist/js/sei-pro-ai.js`. Próxima ação: checker revisar esta fatia. Revisão checker aprovada: diff de `12e68e2` restrito ao hook aditivo em `src/features/ai/sei-pro-ai.js`, teste estrutural, cópia gerada e registro do board; `src/` é a fonte da verdade, `animate-flicker`/`verdeColor`/ícone/posicionamento foram preservados e `cmp` confirmou fonte e artefato `dist/js/sei-pro-ai.js` idênticos após o build. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 467 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-101 | 101 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao botão da plataforma alternativa (`seipro-ai-secondary-platform-button`), preservando id e rotação | review_passed | 1638925 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-100 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 468 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA com plataforma alternativa, alternância/configuração e persistência da plataforma ativa | Compatibilidade preservada: o link mantém id `btnSecondPlataform`, classe compartilhada `newLink`, `data-plataform`, tooltip condicional e atualização interna do id; somente a delegação de clique local passou ao hook da feature. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural cobre hook, id, delegação e rotação. Revisão checker aprovada: diff de `1638925` restrito ao hook aditivo em `src/features/ai/sei-pro-ai.js`, teste estrutural e cópia gerada; `src/` é a fonte da verdade, contratos legados e rotação foram preservados, e `cmp` confirmou `src/features/ai/sei-pro-ai.js` e `dist/js/sei-pro-ai.js` idênticos após o build. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 468 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-102 | 102 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao botão da plataforma principal (`seipro-ai-primary-platform-button`), preservando id e indicador ativo | review_passed | cba8876 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-101 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 469 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, indicador da plataforma principal e troca de plataforma sem erro novo | Compatibilidade preservada: o botão mantém id `btnMainPlataform`, classe compartilhada `newLink`, ícone da plataforma ativa e indicador `seipro-ai-platform-status`; foi adicionado apenas o hook próprio da feature. O teste estrutural cobre o hook, o id e a atualização local do ícone após rotação. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`. Próxima ação: revisão concluída pelo checker. Revisão independente do commit `cba8876`: diff focado restrito ao hook aditivo do botão principal, teste estrutural, cópia gerada e registro do board; `src/` é a fonte da verdade, `dist/js/sei-pro-ai.js` coincide byte a byte com `src/features/ai/sei-pro-ai.js` após o build, e id, classe compartilhada, ícone, indicador e atualização local do ícone foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 469 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. |
| A1-103 | 103 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado às escolhas de plataforma da IA (`seipro-ai-platform-selector`), preservando ids e fluxos de token | review_passed | 3bcf32b | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-102 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 470 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir a seleção de plataforma, escolher OpenAI/Gemini/Ollama e abrir o formulário de credencial correspondente | Compatibilidade preservada: as três escolhas mantêm ids legados, classes compartilhadas CKEditor/newLink/newLink_confirm e cada chamada a `boxAIStoreToken`; receberam o hook próprio e `data-platform`, enquanto a delegação local passou a usar o hook prefixado. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural cobre os três ids, atributos, delegações e fluxos de token. Próxima ação: checker revisar A1-103. Revisão checker aprovada: diff focado do commit `3bcf32b` confirma uma única fatia; `src/features/ai/sei-pro-ai.js` é a fonte da verdade, os três ids legados, classes compartilhadas, `data-platform`, delegações por hook e chamadas `boxAIStoreToken` foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` idêntico a `dist/js/sei-pro-ai.js` após o build. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 470 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: A1-104. |
| A1-104 | 104 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao indicador de carregamento da resposta (`seipro-ai-response-loading`), preservando o spinner legado | review_passed | 82485b1 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-103 e inventário de classes emitidas em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 471 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, envio de prompt e indicador de carregamento até a resposta ou erro | Compatibilidade preservada: o wrapper do spinner mantém `loadingio-spinner-pulse` e o filho `ldio`, necessários ao CSS legado em `dist/css/sei-pro.css`; recebeu apenas o hook próprio `seipro-ai-response-loading`. Teste estrutural ampliado trava os dois contratos; saída gerada correspondente em `dist/js/sei-pro-ai.js`. Próxima ação: checker revisar A1-104. Revisão checker aprovada: diff focado do commit `82485b1` restrito ao hook aditivo no wrapper do spinner, teste estrutural, cópia gerada e registro do board; `src/` é a fonte da verdade, `loadingio-spinner-pulse`/`ldio` foram preservados e `cmp` confirmou `src/features/ai/sei-pro-ai.js` idêntico a `dist/js/sei-pro-ai.js` após o build. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 471 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: seleção dinâmica após A1-104.

| A1-105 | 105 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado à lista de respostas do chat (`seipro-ai-response-list`), preservando o id legado | review_passed | bac2b8d | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-104 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 472 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, histórico de respostas, redimensionamento do diálogo e console sem erro novo | Compatibilidade preservada: o contêiner mantém id `response_ai`, usado pelos fluxos de inserção de respostas e ajuste de altura; foi adicionado somente o hook próprio `seipro-ai-response-list`. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural cobre hook e atualização de layout por id. Aguardando revisão independente do checker. Revisão checker aprovada: diff focado do commit `bac2b8d` restrito ao hook aditivo em `src/features/ai/sei-pro-ai.js`, teste estrutural, cópia gerada e registro do board; `src/` é a fonte da verdade, o id `response_ai` e os seletores legados de layout foram preservados, e `cmp` confirmou fonte e artefato `dist/js/sei-pro-ai.js` idênticos após o build. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 472 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-106 | 106 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao wrapper do diálogo de Configurações Gerais (`seipro-ai-config-dialog`), preservando a classe legada | review_passed | a0de279 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-105 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 473 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir Configurações Gerais de IA, alternar campos avançados, salvar/fechar e verificar aparência sem erro novo | Compatibilidade preservada: o wrapper do diálogo de Configurações Gerais mantém `alertBoxDiv` para a aparência legada e recebe o hook próprio `seipro-ai-config-dialog`; título, largura, ids dos campos e o fluxo de salvar permanecem iguais. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural cobre ambos os contratos. Revisão checker aprovada: diff de `a0de279` restrito ao hook aditivo em `src/features/ai/sei-pro-ai.js`, teste estrutural, cópia gerada e registro do board; `src/` é a fonte da verdade, `alertBoxDiv`, título, largura, ids dos campos e fluxo de salvar foram preservados, e `cmp` confirmou fonte e artefato `dist/js/sei-pro-ai.js` idênticos após o build. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 473 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-107 | 107 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao controle de favoritar prompt (`seipro-ai-favorite-prompt`), preservando id e fluxo de favoritos | review_passed | b84bf17 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-106 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 474 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no prompt personalizado, favoritar prompt, restaurar favoritos e atualizar o ícone de estrela | Compatibilidade preservada: o controle mantém id `favoritePromptAI`, os fluxos internos de show/hide/ícone e o armazenamento `favoritePromptAI`; apenas a delegação de clique passou para o hook próprio `seipro-ai-favorite-prompt`. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural cobre hook, id, delegação e persistência. Próxima ação: checker revisar A1-107. Revisão checker aprovada: diff de `b84bf17` restrito ao hook aditivo em `src/features/ai/sei-pro-ai.js`, teste estrutural, cópia gerada e registro do board; `src/` é a fonte da verdade, o id `favoritePromptAI`, operações internas de show/hide/ícone e armazenamento `favoritePromptAI` foram preservados, e `cmp` confirmou fonte e artefato `dist/js/sei-pro-ai.js` idênticos após o build. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 474 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. |
| A1-108 | 108 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao contêiner de ações da IA (`seipro-ai-actions-container`), preservando o id e o layout legado | review_passed | fc67900 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-107 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 475 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, barra de prompt, redimensionamento ao alternar prompt personalizado e fechamento do diálogo | Compatibilidade preservada: o contêiner mantém id `boxAIActions`, usado pelos fluxos legados de remoção de estilo, fechamento e cálculo de altura; recebeu apenas o hook próprio `seipro-ai-actions-container`. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural cobre o hook e os ajustes de layout por id. Revisão checker aprovada: diff do commit `fc67900` restrito ao hook aditivo do contêiner, teste estrutural, bundle gerado e registro do board; `src/` é a fonte da verdade, o id `boxAIActions` e os ajustes legados de layout foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build, sem edição manual de `dist/` e sem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 475 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima fatia: seleção dinâmica após A1-108. |
| A1-109 | 109 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado aos links informativos das credenciais de plataforma (`seipro-ai-platform-info-link`), preservando estilo legado | review_passed | e76560c | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-108 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 476 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir credenciais OpenAI/Gemini e confirmar os links informativos/estilo no diálogo | Compatibilidade preservada: os cinco links emitidos nas instruções de credenciais OpenAI/Gemini mantêm `linkDialog`, URLs, destino em nova aba e conteúdo; foi adicionado apenas `seipro-ai-platform-info-link` porque o CSS de `linkDialog` permanece legado em `dist/css/sei-pro.css`. Teste estrutural cobre os cinco hooks e os links de ambas plataformas. Saída gerada incluída em `dist/js/sei-pro-ai.js`, confirmada idêntica à fonte após o build. Aguardando revisão independente do checker. Revisão checker aprovada: diff do commit `e76560c` restrito aos cinco links informativos OpenAI/Gemini, teste estrutural, bundle gerado e board; `src/` é a fonte da verdade, `linkDialog`, URLs, `target="_blank"` e conteúdo foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build, sem edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 476 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-110 | 110 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao editor de prompt personalizado (`seipro-ai-personal-prompt-editor`), preservando id e persistência | review_passed | d02a870 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-109 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 477 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir prompt personalizado, inserir referência de documento, salvar/restaurar texto e retornar aos seletores padrão | Compatibilidade preservada: o editor mantém id `promptAIPersonal`, `contenteditable`, inserção de referências, armazenamento `promptAIPersonal` e retorno ao seletor padrão; recebeu apenas o hook próprio da feature. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural cobre hook, id e fluxo de persistência. Próxima ação: checker revisar A1-110. Revisão checker aprovada: diff de `d02a870` restrito ao hook aditivo no editor de prompt personalizado, teste estrutural, cópia gerada e registro do board; `src/` é a fonte da verdade, o id `promptAIPersonal`, `contenteditable`, inserção de referências, persistência `promptAIPersonal` e retorno aos seletores padrão foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 477 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-111 | 111 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao estado pendente de respostas da IA (`seipro-ai-response-pending`), preservando a classe legada de carregamento | review_passed | 8e062f4 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-110 e inventário de classes emitidas em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 478 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no envio de prompt, indicador de carregamento e resposta de erro no modal de IA | Compatibilidade preservada: os dois produtores de resposta pendente mantêm `response_bot`, `loading`, plataforma e contratos de resposta; foi adicionado apenas o hook `seipro-ai-response-pending`. O CSS legado ainda depende de `.dialogBoxAI .response_bot.loading`, por isso `loading` não foi renomeada. Teste estrutural cobre as duas emissões e a remoção normal de `loading`; saída gerada em `dist/js/sei-pro-ai.js`. Smoke manual permanece pendente. Próxima ação: checker revisar esta fatia. Revisão checker aprovada: diff de `8e062f4` restrito aos dois produtores de resposta pendente, teste estrutural, cópia gerada e registro do board; `src/` é a fonte da verdade, `response_bot`, `loading`, plataforma e os contratos de resposta foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build, sem edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 478 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. |
| A1-112 | 112 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao diálogo de seleção de plataforma (`seipro-ai-platform-picker-dialog`), preservando a classe legada | review_passed | 3950d2b | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-111 e inventário de classes emitidas em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 479 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o seletor de plataforma e escolher OpenAI/Gemini/Ollama, verificando aparência e diálogo de credenciais | Compatibilidade preservada: o wrapper do seletor mantém `dialogBoxDiv` para o CSS legado e recebe apenas `seipro-ai-platform-picker-dialog`; as escolhas de plataforma continuam com ids, hooks e fluxos de token existentes. Saída gerada incluída em `dist/js/sei-pro-ai.js`; teste estrutural cobre os contratos do diálogo. Smoke manual permanece pendente. Revisão checker aprovada: diff de `3950d2b` restrito ao hook aditivo do wrapper do seletor, teste estrutural e cópia gerada; `src/` é a fonte da verdade, `dialogBoxDiv`, ids, hooks e fluxos de token foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 479 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-113 | 113 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao diálogo de sucesso de credenciais (`seipro-ai-token-success-dialog`), preservando confirmação e recarga legadas | review_passed | 627d8bb | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-112 e inventário de classes emitidas em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 480 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), salvar credencial OpenAI/Gemini/Ollama, confirmar mensagem de sucesso e acionar “Recarregar” | Compatibilidade preservada: o diálogo mantém `alertaAttencionPro`, `dialogBoxDiv`, texto de confirmação, id `plataformAI_uiElement` e recarga inline; foi adicionado apenas o hook `seipro-ai-token-success-dialog`. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural cobre hook e contratos de confirmação. Próxima ação: checker revisar A1-113. Revisão checker aprovada: diff de `627d8bb` restrito ao hook aditivo do diálogo de sucesso, teste estrutural, cópia gerada e registro do board; `src/` é a fonte da verdade, `alertaAttencionPro`, `dialogBoxDiv`, texto de confirmação, id `plataformAI_uiElement` e recarga inline foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 480 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-114 | 114 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado aos avisos de credencial das plataformas (`seipro-ai-platform-credential-notice`), preservando diálogos legados | review_passed | 0210974 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-113 e inventário de templates em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 481 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir credenciais OpenAI/Gemini/Ollama e confirmar avisos, estilo e campos sem erro novo | Compatibilidade preservada: os oito blocos de aviso mantêm as classes legadas `alertaAttencionPro`/`dialogBoxDiv`; os três alertas mantêm o id `plataformAI_alert`, e textos/links/campos de credenciais não mudam. O hook próprio foi adicionado a todos os avisos emitidos pelos três templates; teste estrutural cobre as oito emissões e contratos das plataformas. Saída gerada incluída apenas em `dist/js/sei-pro-ai.js`; smoke manual pendente. Próxima ação: checker revisar esta fatia. Revisão checker aprovada: diff de `0210974` restrito às oito adições do hook nos avisos de credencial, teste estrutural e cópia gerada; `src/` é a fonte da verdade, as classes legadas `alertaAttencionPro`/`dialogBoxDiv`, os três ids `plataformAI_alert`, textos, links e campos foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 481 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-115 | 115 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao diálogo de criar documento SEI (`seipro-ai-create-document-dialog`), preservando estilo e seletor legados | review_passed | 3b92ae4 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-114 e inventário de templates em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 482 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), gerar resposta, acionar “Criar documento SEI”, selecionar tipo no diálogo e confirmar criação sem erro novo | Compatibilidade preservada: o wrapper externo do diálogo mantém `dialogBoxDiv` e `seiProForm` para o CSS legado, e recebe apenas `seipro-ai-create-document-dialog`; o seletor `docTipoSelect`, Chosen e o fluxo de criação permanecem iguais. Teste estrutural cobre o hook e os contratos preservados; `cmp` confirmou `src/features/ai/sei-pro-ai.js` idêntico a `dist/js/sei-pro-ai.js` após o build. Smoke manual pendente. Revisão checker aprovada: diff de `3b92ae4` restrito ao hook aditivo no produtor, teste estrutural e cópia gerada; `src/` é a fonte da verdade, `dialogBoxDiv`, `seiProForm`, `docTipoSelect`, Chosen e o fluxo de criação foram preservados. `cmp` confirmou `src/features/ai/sei-pro-ai.js` igual a `dist/js/sei-pro-ai.js` após o build; não houve churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 482 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-116 | 116 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao wrapper externo do diálogo de criar documento SEI (`seipro-ai-create-document-alert`), preservando o estilo legado | review_passed | 1e5c6ac | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-115 e inventário de templates em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 482 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), gerar resposta, acionar Criar documento SEI, conferir abertura/estilo do diálogo, seletor de tipo e criação sem erro novo | Compatibilidade preservada: o wrapper externo mantém `alertBoxDiv` para o CSS legado; recebeu somente o hook da feature. O diálogo interno mantém `dialogBoxDiv`, `seiProForm`, `docTipoSelect`, Chosen e o fluxo de criação. Teste estrutural cobre os hooks e contratos. A saída gerada `dist/js/sei-pro-ai.js` veio do build; não houve churn em `dist/js/init_visualizacao.js`. Smoke manual pendente. Revisão checker aprovada: diff de `1e5c6ac` restrito a um hook aditivo no wrapper externo, teste estrutural e cópia gerada; `src/` é a fonte da verdade, `alertBoxDiv`, diálogo interno `dialogBoxDiv`, `seiProForm`, `docTipoSelect`, Chosen e fluxo de criação preservados. `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0 após o build; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 482 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece pendente conforme `SMOKE_TEST.md` seção 3. Aprovado. |

| A1-117 | 117 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao diálogo de pesquisa de processo (`seipro-ai-process-search-dialog`), preservando formulário e autocomplete legados | review_passed | 4d76721 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-116 e inventário de diálogos em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 483 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o seletor de documento da IA, pesquisar processo, validar autocomplete, busca e retorno ao seletor sem erro novo | Compatibilidade preservada: o diálogo mantém `dialogBoxDiv`, `seiProForm`, campo `dialogBoxProcessoAI`, autocomplete, título, busca e retorno ao seletor; recebeu apenas o hook da feature. Teste estrutural cobre hook e contratos. A saída em `dist/js/sei-pro-ai.js` foi gerada pelo build; sem churn em `dist/js/init_visualizacao.js`. Smoke manual pendente; checker revisou o commit `4d76721`: uma única adição de hook no produtor, teste estrutural e cópia gerada; `src/` é a fonte da verdade, contratos de formulário/autocomplete preservados, `dist/` não foi editado manualmente. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 483 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. |

| A1-118 | 118 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao wrapper externo da pesquisa de processo (`seipro-ai-process-search-alert`), preservando formulário e autocomplete legados | review_passed | 7a757a6 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-117 e inventário de diálogos em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 484 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o seletor de documento da IA, pesquisar processo e validar autocomplete, busca e retorno ao seletor sem erro novo | Compatibilidade preservada: o wrapper externo mantém `alertBoxDiv` para o CSS legado e recebe somente `seipro-ai-process-search-alert`; o diálogo interno mantém `dialogBoxDiv`, `seiProForm`, `dialogBoxProcessoAI`, autocomplete, título, busca e retorno ao seletor. Teste estrutural cobre os dois wrappers e os contratos. `dist/js/sei-pro-ai.js` foi gerado pelo build; smoke manual pendente. Próxima ação: checker revisar A1-118. Revisão checker aprovada: `7a757a6` contém uma única adição do hook no wrapper externo, teste estrutural e cópia gerada; `src/` é a fonte da verdade, `alertBoxDiv`, diálogo interno `dialogBoxDiv`, `seiProForm`, `dialogBoxProcessoAI`, autocomplete, título, busca e retorno foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 484 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-119 | 119 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao seletor interno de tipo de documento (`seipro-ai-create-document-type-selector`), preservando o formulário legado | review_passed | 93eefce | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-118 e inventário de wrappers em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 485 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), gerar resposta, acionar Criar documento SEI, conferir seletor de tipo com Chosen e criação sem erro novo | Compatibilidade preservada: o seletor interno mantém `dialogBoxDiv`, `seiProForm`, id `docTipoSelect`, inicialização Chosen e fluxo de criação; recebeu apenas o hook da feature. Teste estrutural cobre o hook e os contratos preservados; `dist/js/sei-pro-ai.js` foi gerado pelo build. Smoke manual pendente. Próxima ação: checker revisar A1-119. Revisão checker aprovada: `93eefce` contém uma única adição de hook no produtor, teste estrutural e cópia gerada; `src/` é a fonte da verdade, os contratos `dialogBoxDiv`, `seiProForm`, `docTipoSelect`, Chosen e criação foram preservados, e não houve edição manual em `dist/`. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 485 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme `SMOKE_TEST.md` seção 3. Aprovado. |
| A1-120 | 120 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao botão de recarregar após salvar credencial (`seipro-ai-token-reload-button`), preservando o contrato legado | review_passed | d49df3a | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-119 e inventário do template de sucesso de credenciais em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 486 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), salvar credencial de IA, confirmar mensagem de sucesso e botão Recarregar sem erro novo | Compatibilidade preservada: o botão mantém `id="plataformAI_uiElement"`, classes CKEditor `cke_dialog_ui_button cke_dialog_ui_button_cancel`, `onclick="window.location.reload()"` e o realce em árvore; recebeu somente o hook de feature. Teste estrutural cobre hook e contratos; `dist/js/sei-pro-ai.js` foi gerado pelo build. Smoke manual pendente; aguarda revisão checker. Revisão checker aprovada: `d49df3a` contém uma única adição de hook no botão de sucesso, teste estrutural e cópia gerada; `src/` é a fonte da verdade, o ID `plataformAI_uiElement`, classes CKEditor, `onclick` de recarga e realce em árvore foram preservados, sem edição manual em `dist/`. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 486 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme `SMOKE_TEST.md` seção 3. Aprovado. |

| A1-121 | 121 | feature | editor | `ai` / CSS prefixado | Adicionar hook compartilhado aos botões de salvar credencial (`seipro-ai-token-save-button`), preservando identificadores e detecção de plataforma | review_passed | 17c03ae | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-120 e inventário dos três templates de credencial em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 487 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), salvar credenciais OpenAI/Gemini/Ollama e confirmar o respectivo diálogo/fluxo sem erro novo | Compatibilidade preservada: os três botões mantêm o identificador legado, classes CKEditor e hooks específicos de OpenAI/Gemini/Ollama; o listener delegado agora usa o hook compartilhado da feature e a detecção por hook específico permanece igual. Inclui somente a cópia gerada `dist/js/sei-pro-ai.js` e teste estrutural para três produtores/um consumidor. Smoke manual pendente. Checker aprovou após revisar o diff focado de `17c03ae`: uma única fatia aditiva nos três produtores e no listener compartilhado; `src/` é a fonte da verdade, os identificadores legados, classes CKEditor, hooks específicos, `data-platform`/detecção e chamadas `boxAIStoreToken` permanecem preservados. `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0 após o build; não houve edição manual em `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 487 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Próxima ação: seleção dinâmica da próxima fatia após A1-121. |

| A1-122 | 122 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao checkbox de consentimento (`seipro-ai-consent-toggle`), preservando o aceite legado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-121 e inventário do template de consentimento em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 488 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o consentimento de IA, marcar “Estou ciente”, confirmar e verificar a abertura normal das ações de IA | Compatibilidade preservada: o input mantém a classe CKEditor `cke_dialog_ui_checkbox_input`, id `ciente_disclaimer`, label associado e o fluxo de aceite que grava `consentimentoIA`; recebeu somente o hook da feature. Teste estrutural cobre hook e contratos; somente `dist/js/sei-pro-ai.js` foi gerado pelo build, sem churn incidental em `dist/js/init_visualizacao.js`. Revisão checker aprovada: o commit `556bd5f` altera uma única classe aditiva no produtor, o teste estrutural, a cópia gerada e o registro do board; `src/` é a fonte da verdade, os contratos de checkbox/label e o fluxo de aceite foram preservados, e `dist/` não foi editado manualmente. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 488 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. |
| A1-123 | 123 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao formulário de Configurações Gerais (`seipro-ai-config-form`), preservando o estilo compartilhado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-122 e inventário do template de configurações em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 489 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir Configurações Gerais de IA, ajustar campos e salvar/fechar sem erro novo | Compatibilidade preservada: a tabela mantém a classe compartilhada `seiProForm`, os campos de modelo/configurações avançadas e o fluxo de persistência; recebeu somente o hook `seipro-ai-config-form`. Teste estrutural cobre o hook, a classe compartilhada e os campos legados. Saída gerada incluída somente em `dist/js/sei-pro-ai.js`; smoke manual pendente. Revisão checker aprovada: o commit `64563ed` altera somente o hook aditivo no produtor de Configurações Gerais, o teste estrutural, a cópia gerada e o registro do board; `src/` é a fonte da verdade, `seiProForm`, os campos `configAI_model`/`configAI_advancedconfigs` e o fluxo de persistência foram preservados. `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0 após o build; não houve edição manual em `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 489 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Aprovado. Próxima ação: seleção dinâmica da próxima fatia após A1-123. |

| A1-124 | 124 | feature | editor | `ai` / seletor delegado | Migrar a delegação da alternância de configurações avançadas para `.seipro-ai-advanced-config-toggle`, preservando o id e a persistência legados | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-123 e inventário de listeners em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 489 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir Configurações Gerais de IA, alternar “Configurações avançadas”, salvar e confirmar persistência/visibilidade dos campos | Compatibilidade preservada: o input mantém id, label, classe de switch e persistência por `setAdvancedConfigs`; apenas o listener delegado passa a usar o hook já prefixado da feature. Teste estrutural verifica o novo listener e rejeita o listener delegado pelo id legado. Saída gerada incluída somente em `dist/js/sei-pro-ai.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: revisão concluída pelo checker. Diff focado de `7461572` confirma uma única migração do listener delegado; `src/` é a fonte da verdade, os contratos de id/label/classe/persistência permanecem, e `dist/` foi apenas regenerado. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 489 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Aprovado. |

| A1-125 | 125 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado aos ícones informativos das Configurações Gerais (`seipro-ai-config-info-icon`), preservando tooltips e estilo legado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-124 e inventário de classes emitidas em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 490 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir Configurações Gerais de IA e validar tooltips de modelo, modelo manual e parâmetros avançados | Compatibilidade preservada: os sete ícones mantêm as classes compartilhadas `iconPopup`/`iconSwitch`, Font Awesome, cor e atributos `data-tooltip`; foi adicionado somente o hook da feature aos produtores de configuração. Teste estrutural registra as sete emissões e contratos de tooltips. Saída gerada incluída somente em `dist/js/sei-pro-ai.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-125. Revisão checker aprovada: diff focado de `a736ae0` confirma uma única fatia aditiva nos sete ícones de configuração; `src/` é a fonte da verdade, classes compartilhadas, Font Awesome, cor e `data-tooltip` foram preservados, e `dist/` foi apenas regenerado. Gates checker no checkout atual: `git status --short` limpo; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 490 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme SMOKE_TEST.md seção 3. Aprovado. |

| A1-126 | 126 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao wrapper do checkbox de consentimento (`seipro-ai-consent-checkbox-wrapper`), preservando o contrato CKEditor | review_passed | 776bd8e | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-125 e inventário de classes emitidas em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 491 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o diálogo de consentimento de IA, marcar o checkbox, confirmar e validar a abertura das ações de IA sem erro novo | Compatibilidade preservada: o wrapper mantém id `cke_383_uiElement` e a classe CKEditor `cke_dialog_ui_checkbox`; o input interno conserva id, label, hook de consentimento e persistência `consentimentoIA`. Foi adicionado somente o hook de feature ao wrapper. Teste estrutural cobre o hook e os contratos preservados; somente `dist/js/sei-pro-ai.js` foi gerado pelo build, com cópia verbatim confirmada. Smoke manual pendente; checker aprovou após revisar o diff focado de `776bd8e`: uma única adição de hook no wrapper, teste estrutural e cópia gerada; `src/` é a fonte da verdade, os contratos CKEditor, id, label, hook do input e persistência `consentimentoIA` foram preservados, sem edição manual em `dist/`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 491 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. |

| A1-127 | 127 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado aos indicadores de carregamento de credencial (`seipro-ai-token-loading`), preservando id e spinner legados | review_passed | 6a38aa7 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-126 e inventário dos templates de credencial em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 492 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), salvar credencial OpenAI/Gemini/Ollama e confirmar o indicador de carregamento até sucesso ou erro | Compatibilidade preservada: os três indicadores mantêm id `plataformAI_load`, classes Font Awesome `fas fa-sync-alt fa-spin` e controle existente de exibição; receberam somente o hook de feature. Teste estrutural cobre as três emissões, o id e o controle de exibição. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual pendente. Revisão checker independente: diff do commit `6a38aa7` confirma exatamente três produtores (OpenAI/Gemini/Ollama), preserva id `plataformAI_load`, classes `fas fa-sync-alt fa-spin` e controle de exibição, e mantém `src/` como fonte da verdade; `dist/` não foi editado manualmente. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 492 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. Próxima fatia: seleção dinâmica após A1-127. |
| A1-128 | 128 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao rótulo do seletor de tipo de documento (`seipro-ai-create-document-type-label`), preservando o diálogo legado | review_passed | eb57e86 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-127 e inventário do diálogo de criação em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 493 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), gerar resposta, acionar Criar documento SEI, conferir rótulo e seletor Chosen sem erro novo | Compatibilidade preservada: o novo hook é aditivo no rótulo do diálogo; texto “Selecione o tipo de documento”, seletor `docTipoSelect`, inicialização Chosen e fluxo de criação continuam iguais. Teste estrutural cobre o hook e esses contratos; somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build, sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual pendente; aguarda revisão checker. Revisão checker aprovada: commit `eb57e86` revisado por diff exato; a fatia é uma única adição aditiva no rótulo, preservando texto, `docTipoSelect`, Chosen e fluxo de criação. `src/` é a fonte da verdade; `dist/js/sei-pro-ai.js` foi regenerado e `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 493 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. |

| A1-129 | 129 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado compartilhado aos formulários de credencial (`seipro-ai-token-form`), preservando o id e fluxo legados | review_passed | 2aadf3b | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-128 e inventário dos templates de credencial em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 494 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir e salvar credenciais OpenAI/Gemini/Ollama, validando os formulários, indicador de carregamento e mensagens sem erro novo | Compatibilidade preservada: os três produtores mantêm o id `plataformAI_info`, estilos inline e fluxos existentes de token; receberam somente o hook de feature compartilhado. O teste estrutural confirma os três formulários, templates e consumidores legados por id. Somente `dist/js/sei-pro-ai.js` foi gerado pelo build; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual pendente. Revisão checker independente: o commit `2aadf3b` contém somente a adição do hook compartilhado nos três templates de credencial, o teste estrutural, a cópia gerada e o registro do board; preserva `plataformAI_info`, estilos inline, consumidores por id e fluxos de token. `src/` permanece a fonte da verdade e `dist/` não foi editado manualmente. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 494 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. |
| A1-130 | 130 | feature | editor | `ai` / seletor delegado | Migrar a delegação de mudança de prompt para `.seipro-ai-prompt-select`, preservando o id e os fluxos internos legados | review_passed | c466008 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-129 e inventário de listeners em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 495 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o modal de IA, alternar prompt padrão/personalizado, retornar ao seletor e confirmar seleção de documento sem erro novo | Compatibilidade preservada: o seletor mantém id `promptAISelect`; apenas seu listener delegado passa a usar o hook próprio já existente `.seipro-ai-prompt-select`. Operações internas por id, Chosen, prompt personalizado, retorno e persistência permanecem intactos. Teste estrutural cobre novo listener e preservação do id; somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build. Smoke manual pendente. Revisão checker aprovada: o diff de `c466008` é restrito à troca do seletor delegado, teste estrutural, cópia gerada e board; `src/` é a fonte da verdade, o id `promptAISelect`, operações internas por id, Chosen, prompt personalizado, retorno e persistência foram preservados. No checkout atual: `git status --short` limpo antes dos gates e após `npm test`; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 495 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. Próxima ação: seleção dinâmica após A1-130. |

| A1-131 | 131 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado compartilhado às tabelas de campos de credencial (`seipro-ai-token-fields`), preservando layout CKEditor e ações de token | review_passed | 8bee21d | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-130 e inventário dos templates de credencial em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 496 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir credenciais OpenAI/Gemini/Ollama, conferir layout dos campos e salvar cada credencial sem erro novo | Compatibilidade preservada: as três tabelas mantêm `cke_dialog_ui_hbox` e os controles existentes de salvar/carregamento; receberam somente o hook compartilhado. Teste estrutural cobre três produtores e os botões de token. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build, sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual pendente; aguarda revisão checker. Revisão checker independente: o commit `8bee21d` foi revisado e contém apenas o hook aditivo nas três tabelas de campos, teste estrutural, cópia gerada e board; preserva `cke_dialog_ui_hbox`, o estilo inline do template Ollama, os três botões de token e o template separado do seletor de plataforma. `src/` é a fonte da verdade; `dist/` não foi editado manualmente. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 496 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. |

| A1-132 | 132 | feature | editor | `ai` / seletor delegado | Migrar a delegação de mudança do seletor de documento para `.seipro-ai-doc-select`, preservando id e fluxo de prompt personalizado | review_passed | 759966d | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-131 e inventário de listeners em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 497 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir prompt personalizado, selecionar documento/processo, inserir referência e retornar ao seletor sem erro novo | Compatibilidade preservada: o select mantém id `docAISelect`; apenas o listener delegado de mudança passa a usar o hook já existente `.seipro-ai-doc-select`. Operações internas por id, Chosen, busca de processo, inserção de referência e reset para adicionar documento permanecem intactos. Teste estrutural cobre listener, id e fluxos locais; somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build, sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual pendente; aguarda revisão checker. Revisão checker aprovada: diff focado de `759966d` restrito à troca do seletor delegado, teste estrutural, cópia gerada e board; `src/` é a fonte da verdade, o id `docAISelect`, operações internas por id, Chosen, busca de processo, inserção de referência e reset foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 497 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. |
| A1-133 | 133 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao seletor de tipo de documento para criação (`seipro-ai-create-document-type-select`), preservando id e Chosen | review_passed | dc3529e | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-132 e inventário do diálogo de criação em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 498 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), gerar resposta, acionar Criar documento SEI, conferir o seletor de tipo com Chosen e concluir a criação sem erro novo | Compatibilidade preservada: o seletor mantém id `docTipoSelect`, opções dinâmicas `tiposDocumentos`, inicialização Chosen e fluxo de criação; recebeu somente o hook aditivo da feature. Teste estrutural cobre hook, id, opções, Chosen e título do diálogo. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build; smoke manual pendente. Próxima ação: checker revisar A1-133. Revisão checker independente: diff do commit `dc3529e` restrito ao hook aditivo do seletor de tipo, teste estrutural, cópia gerada e board; `src/` é a fonte da verdade, `docTipoSelect`, opções `tiposDocumentos`, Chosen e fluxo de criação foram preservados. `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0 após o build; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 498 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece pendente conforme seção 3. Aprovado. |

| A1-134 | 134 | feature | editor | `ai` / seletor delegado | Migrar a delegação de eventos do editor de prompt personalizado para `.seipro-ai-personal-prompt-editor`, preservando id e persistência legados | review_passed | 66f0861 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-133 e inventário de listeners em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 499 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir prompt personalizado, digitar/editar, inserir referência de documento, enviar com Ctrl/Cmd+Enter e retornar ao seletor sem erro novo | Compatibilidade preservada: o elemento mantém id `promptAIPersonal`; apenas a delegação de eventos passou ao hook já existente da feature. Persistência, redimensionamento, posição do cursor, favorito e envio permanecem no mesmo handler. Saída gerada incluída somente em `dist/js/sei-pro-ai.js`; teste estrutural cobre listener, id, persistência, cursor e envio. Smoke manual pendente. Revisão checker independente: diff focado do commit `66f0861` confirma uma única troca do seletor delegado; `src/` é a fonte da verdade, o id `promptAIPersonal`, persistência, cursor e envio foram preservados, e `dist/` foi apenas regenerado. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 499 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. |

| A1-135 | 135 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de pesquisa de processo (`seipro-ai-process-search-input`), preservando id, autocomplete e busca legados | review_passed | c245c02 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-134 e inventário do diálogo de pesquisa em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 499 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o seletor de documento da IA, pesquisar processo, validar autocomplete, busca e retorno ao seletor sem erro novo | Compatibilidade preservada: o input mantém id `dialogBoxProcessoAI`, tipo text, autocomplete, confirmação por Enter e busca por `appendDocAISelect`; recebeu apenas o hook aditivo da feature. Teste estrutural cobre o hook e os contratos do diálogo. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-135. Revisão checker aprovada: diff focado de `c245c02` confirma uma única adição do hook no input de pesquisa; `src/` é a fonte da verdade, o id `dialogBoxProcessoAI`, autocomplete, confirmação por Enter e busca por `appendDocAISelect` foram preservados, e `dist/` foi apenas regenerado. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 499 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme seção 3. Aprovado. |

| A1-136 | 136 | feature | editor | `ai` / seletor delegado | Migrar a delegação de Enter da pesquisa de processo para `.seipro-ai-process-search-input`, preservando id, autocomplete e busca legados | review_passed | cb7330b | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-135 e listener do diálogo de pesquisa em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 81 testes); `npm test` ✅ (60 arquivos, 499 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o seletor de documento da IA, pesquisar processo, confirmar por Enter e validar autocomplete, busca e retorno sem erro novo | Compatibilidade preservada: o input mantém id `dialogBoxProcessoAI`, autocomplete, confirmação por Enter e busca por `appendDocAISelect`; somente o listener delegado passa a usar o hook existente da feature. Teste estrutural rejeita apenas o listener delegado pelo id. Inclui a cópia verbatim gerada `dist/js/sei-pro-ai.js`; smoke manual pendente. Revisão checker independente: diff focado do commit `cb7330b` confirma uma única troca do seletor delegado de `keypress`; o input preserva id `dialogBoxProcessoAI`, hook, autocomplete, confirmação por Enter e busca por `appendDocAISelect`, sem edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes da decisão e após o build; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 499 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. |

| A1-137 | 137 | feature | editor | `ai` / seletor delegado | Migrar a preservação de cursor do seletor de documento para `.seipro-ai-doc-select`, preservando id e fluxo de prompt personalizado | review_passed | 9560883 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-136 e listener de cursor em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 82 testes); `npm test` ✅ (60 arquivos, 500 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir prompt personalizado, selecionar documento/processo, inserir referência e confirmar que o cursor é preservado sem erro novo | Compatibilidade preservada: o select mantém id `docAISelect`; somente o listener delegado de `mousedown` usa o hook existente da feature. A preservação de cursor por `saveCursorPosition()`, Chosen, busca de processo, inserção de referência e reset permanecem intactos. Teste estrutural cobre novo listener, id e efeito local. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-137. Revisão checker independente: diff focado do commit `9560883` restrito à troca do seletor delegado de `mousedown`, teste estrutural, cópia gerada e board; `src/` é a fonte da verdade, o id `docAISelect`, Chosen, busca de processo, inserção de referência, reset e `saveCursorPosition()` foram preservados. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 500 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. |

| A1-138 | 138 | feature | editor | `ai` / seletor delegado | Migrar a delegação de Enter do seletor Chosen de tipo de documento para `.seipro-ai-create-document-type-chosen`, preservando id e criação legados | review_passed | 0e4c605 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-137 e listener do diálogo de criação em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 83 testes); `npm test` ✅ (60 arquivos, 501 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), gerar resposta, acionar Criar documento SEI, usar Enter no seletor Chosen de tipo e confirmar a criação sem erro novo | Compatibilidade preservada: `docTipoSelect` e o contêiner Chosen legado `docTipoSelect_chosen` continuam disponíveis; após a inicialização Chosen, só o listener delegado de Enter usa o hook da feature, mantendo a confirmação e leitura do tipo selecionado. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada; mapa regenerado após alteração de linhas, sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-138. Revisão checker independente: diff focado do commit `0e4c605` confirma uma única troca do listener delegado de `keypress` e a adição do hook após a inicialização Chosen; `src/` é a fonte da verdade, o id `docTipoSelect`, o contêiner `docTipoSelect_chosen`, a leitura do valor e o clique de confirmação foram preservados. `dist/js/sei-pro-ai.js` foi apenas regenerado e coincidiu byte a byte com `src/features/ai/sei-pro-ai.js`. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates e após `npm test`; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 501 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. |

| A1-139 | 139 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado à lista de documentos múltiplos (`seipro-ai-document-list`), preservando id e fluxo de tags | review_passed | 56dcc2b | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-138 e inventário do prompt de IA em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 84 testes); `npm test` ✅ (60 arquivos, 502 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o modal de IA, selecionar múltiplos documentos, remover um chip e enviar o prompt sem erro novo | Compatibilidade preservada: o contêiner mantém id `docAIMultiList`; os fluxos existentes de criação, remoção e ocultação dos chips continuam usando o id e os hooks de chip já migrados. Foi adicionado apenas o hook próprio da feature. Teste estrutural cobre hook, id e fluxo de tags. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi regenerada; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-139. Revisão checker aprovada: diff de `56dcc2b` confirmado como uma única adição do hook no contêiner `docAIMultiList`; `src/` é a fonte da verdade, o id e o fluxo de tags foram preservados, e `dist/` foi somente regenerado. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 502 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. Próxima fatia: seleção dinâmica após A1-139. |

| A1-140 | 140 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado compartilhado aos ícones informativos de IA (`seipro-ai-info-icon`), preservando estilo legado e templates de plataforma | review_passed | 125a359 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-139 e inventário de produtores em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 85 testes); `npm test` ✅ (60 arquivos, 503 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco nos diálogos de credenciais OpenAI/Gemini/Ollama e no seletor de plataforma, confirmando ícones informativos e fluxos sem erro novo | Compatibilidade preservada: os quatro produtores mantêm as classes legadas `fas fa-info-circle azulColor`, o texto/links dos templates OpenAI, Gemini e Ollama e o diálogo de seleção de plataforma; receberam somente o hook aditivo `seipro-ai-info-icon`. O teste estrutural exige os quatro produtores e os contratos dos templates. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build; `manifest.base.json` e `dist/manifest.json` coincidem; sem churn incidental em `dist/js/init_visualizacao.js`. Revisão checker independente: diff do commit `125a359` confirma quatro adições de um único hook aditivo em três templates de credenciais (OpenAI/Gemini/Ollama) e no seletor de plataforma; classes legadas, textos/links e contratos dos templates foram preservados. `src/` é a fonte da verdade e `dist/js/sei-pro-ai.js` foi somente regenerado. Gates checker no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 503 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. Próxima ação: seleção dinâmica após A1-140. |
| A1-141 | 141 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado à tabela do seletor de plataforma (`seipro-ai-platform-picker-table`), preservando layout CKEditor e escolhas de plataforma | review_passed | c722274 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-140 e inventário do template em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 86 testes); `npm test` ✅ (60 arquivos, 504 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o seletor de plataforma da IA, conferir o layout CKEditor e selecionar OpenAI, Gemini e Ollama sem erro novo | Compatibilidade preservada: a tabela mantém a classe CKEditor `cke_dialog_ui_hbox`, os três IDs legados de plataforma e os atributos `data-platform`; recebeu somente o hook aditivo da feature. O teste estrutural cobre o hook, os IDs e as escolhas. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual pendente; checker deve revisar a fatia. Revisão checker independente: diff de `c722274` confirma uma única adição do hook na tabela do seletor; `src/` é a fonte da verdade, `dist/js/sei-pro-ai.js` foi somente regenerado, e a classe CKEditor, IDs `selectPlataformAI_*`, atributos `data-platform` e escolhas OpenAI/Gemini/Ollama foram preservados. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 504 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. |

| A1-142 | 142 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado à linha de opções do seletor de plataforma (`seipro-ai-platform-picker-options`), preservando layout CKEditor e escolhas de plataforma | review_passed | 4abe367 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-141 e inventário do template em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 87 testes); `npm test` ✅ (60 arquivos, 505 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o seletor de plataforma da IA, conferir o layout CKEditor da linha de opções e selecionar OpenAI, Gemini e Ollama sem erro novo | Compatibilidade preservada: a linha mantém a classe CKEditor `cke_dialog_ui_hbox`, e os três botões preservam IDs legados, atributos `data-platform` e delegação por `seipro-ai-platform-selector`; recebeu somente o hook aditivo da feature. Teste estrutural cobre o hook, layout e as três escolhas. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-142. Revisão checker independente: diff focado do commit `4abe367` confirma uma única adição do hook na linha de opções; `src/` é a fonte da verdade, a classe CKEditor, IDs `selectPlataformAI_*`, atributos `data-platform` e delegação por `seipro-ai-platform-selector` foram preservados, e `dist/js/sei-pro-ai.js` foi somente regenerado. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 505 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. |

| A1-143 | 143 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao seletor de documento-modelo (`seipro-doclote-model-select`), preservando id e carregamento legado | review_passed | dadf442 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-142 e inventário em `src/features/docs-lote/templates.js`/`view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 8 testes); `npm test` ✅ (60 arquivos, 506 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, selecionar documento modelo, confirmar carregamento Chosen/análise e console sem erro novo | Compatibilidade preservada: o select mantém id `docLoteSelect`, label associado e carregamento por `getDocsArvore`/`getDocsArvore_fillSelect`; recebeu somente o hook da feature. Teste estrutural cobre hook, id, label e consumidor. Somente `dist/js/docs-lote.bundle.js` foi gerado pelo build; não houve churn em `dist/js/init_visualizacao.js`. Smoke manual pendente; aguarda revisão checker. Revisão checker aprovada: diff focado de `dadf442` confirma uma única adição de hook em `src/features/docs-lote/templates.js`, preservando `docLoteSelect`, label associado e carregamento/consumidor em `view.js`; `src/` é a fonte da verdade e `dist/js/docs-lote.bundle.js` foi apenas regenerado. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 506 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 2. Aprovado. |

| A1-144 | 144 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao seletor de texto padrão (`seipro-doclote-template-select`), preservando id e reset mútuo legado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-143 e inventário em `src/features/docs-lote/templates.js`/`view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 9 testes); `npm test` ✅ (60 arquivos, 507 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, alternar documento modelo/texto padrão, confirmar Chosen, reset mútuo e análise sem erro novo | Compatibilidade preservada: o seletor mantém id `textoPadraoSelect`, label associado, carregamento por `txtPadrao_getList`, Chosen e reset do documento modelo por id; recebeu somente o hook próprio da feature. Teste estrutural cobre hook, id, label e reset mútuo. Somente `dist/js/docs-lote.bundle.js` foi gerado pelo build; sem churn em `dist/js/init_visualizacao.js`. Smoke manual pendente; checker revisou o diff focado de `a98c295`: uma única adição de hook no produtor, teste estrutural e bundle gerado; `src/` é a fonte da verdade, `textoPadraoSelect`, label, `txtPadrao_getList`, Chosen e reset mútuo por id foram preservados, sem edição manual em `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 507 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 2. Aprovado. |

| A1-145 | 145 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao input de upload CSV (`seipro-doclote-csv-input`), preservando id, tipo e análise legados | review_passed | d53c0f5 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-144 e inventário em `src/features/docs-lote/templates.js`/`view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 10 testes); `npm test` ✅ (60 arquivos, 508 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, enviar CSV válido/inválido, avançar na análise e confirmar leitura/validação sem erro novo | Compatibilidade preservada: o input mantém id `inputBD`, tipo `file`, aceitação CSV, label associado e handlers existentes de leitura/validação por id; recebeu apenas o hook aditivo da feature. Teste estrutural cobre hook, id, tipo, aceitação e acesso ao arquivo. Somente `dist/js/docs-lote.bundle.js` foi gerado pelo build; `manifest.base.json` coincide com `dist/manifest.json`; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual pendente; checker revisou o diff focado de `d53c0f5`: uma única adição de hook no produtor, teste estrutural e bundle gerado; `src/` é a fonte da verdade, `inputBD`, tipo/aceitação CSV, label, handler `change` e acesso a `files[0]` foram preservados, sem edição manual em `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 508 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 2. Aprovado. |

| A1-146 | 146 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao wrapper do toggle de animação de texto (`seipro-ai-typing-toggle-wrapper`), preservando o estilo e o controle legado | review_passed | e997fed | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-145 e inventário do template de configurações em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 87 testes); `npm test` ✅ (60 arquivos, 508 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir Configurações Gerais de IA, alternar Animação de texto digitando, salvar e confirmar persistência/efeito sem erro novo | Compatibilidade preservada: o wrapper mantém `infraAncoraSigla`; o input mantém id `configAI_typing_box`, `name`, label, classe `infraLinkOrgao` e persistência `setTypingAI`. Foi adicionado apenas o hook de feature ao wrapper. Teste estrutural estende o contrato do toggle. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada; sem churn incidental em `dist/js/init_visualizacao.js`. Checker independente: commit `e997fed` revisado; `src/` é a fonte da verdade, escopo limitado ao hook aditivo/teste/cópia gerada, `cmp` fonte→cópia ✅, `cmp` manifest ✅; gates no checkout atual: `git status --short` limpo antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 508 testes), `node scripts/engineering-loop-map.mjs --check` ✅, sem churn em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. |

| A1-147 | 147 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao wrapper do toggle de modelos beta (`seipro-ai-beta-models-toggle-wrapper`), preservando estilo e controle legados | review_passed | 8a86a1e | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-146 e inventário do template de configurações em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 88 testes); `npm test` ✅ (60 arquivos, 509 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir Configurações Gerais de IA, alternar “Listar modelos beta”, salvar e confirmar persistência/efeito sem erro novo | Compatibilidade preservada: o wrapper mantém a classe compartilhada `infraAncoraSigla`; o input mantém id `configAI_betamodels`, name, label, classe `infraLinkOrgao` e persistência `setBetaModelsAI`. Foi adicionado somente o hook da feature ao wrapper. Teste estrutural cobre o novo hook e os contratos do controle. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada; smoke manual pendente. Próxima ação: checker revisar A1-147. Revisão checker aprovada: diff focado do commit `8a86a1e` confirma uma única adição do hook no wrapper de modelos beta, teste estrutural e cópia gerada; `src/` é a fonte da verdade, o wrapper preserva `infraAncoraSigla`, e o controle mantém id, name, label, `infraLinkOrgao` e persistência `setBetaModelsAI`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 509 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. Próxima fatia: seleção dinâmica após A1-147. |

| A1-148 | 148 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao wrapper do toggle de configurações avançadas (`seipro-ai-advanced-config-toggle-wrapper`), preservando estilo e controle legados | review_passed | f1598e7 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-147 e inventário do template de configurações em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 88 testes); `npm test` ✅ (60 arquivos, 509 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir Configurações Gerais de IA, alternar “Configurações avançadas”, salvar e confirmar persistência/visibilidade dos campos sem erro novo | Compatibilidade preservada: o wrapper mantém a classe compartilhada `infraAncoraSigla`; o input mantém id `configAI_advancedconfigs`, name, label, classe `infraLinkOrgao`, listener delegado e persistência `setAdvancedConfigs`. Foi adicionado somente o hook da feature ao wrapper. Teste estrutural estende o contrato do toggle. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada; `manifest.base.json` coincide com `dist/manifest.json`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-148. Revisão checker aprovada: diff de `f1598e7` restrito à adição do hook no wrapper, teste estrutural, cópia gerada e board; `src/` é a fonte da verdade, `infraAncoraSigla`, id/name/label, `infraLinkOrgao`, listener delegado e persistência `setAdvancedConfigs` foram preservados. `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0 após o build e `cmp -s manifest.base.json dist/manifest.json` também retornou 0; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 509 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece pendente conforme seção 3. Aprovado. |

| A1-149 | 149 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado à ação de resetar Configurações Gerais (`seipro-ai-config-reset-button`), preservando os defaults legados | review_passed | ff84dac | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-148 e inventário da ação de reset em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 89 testes); `npm test` ✅ (60 arquivos, 510 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir Configurações Gerais de IA, acionar “Resetar configurações”, confirmar os defaults e a continuidade do modal sem erro novo | Compatibilidade preservada: a ação do diálogo mantém o texto, os defaults de modelo/configuração e o fechamento legado; recebeu apenas a classe de feature no botão jQuery UI. Teste estrutural cobre o hook e os resets de modelo OpenAI/configurações avançadas. `src/` é fonte da verdade; somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada. Mapas CSV foram regenerados por mudança de linhas em `sei-pro-ai.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: revisar a próxima fatia dinâmica após A1-149. Revisão checker aprovada: o diff de `ff84dac` foi resolvido pelo símbolo exato e permanece restrito a uma adição de classe no botão, teste estrutural, cópia verbatim e mapas regenerados; `src/` é a fonte da verdade e não houve edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 510 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. |

| A1-150 | 150 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado aos ícones de sugestões padrão (`seipro-ai-suggestion-icon`), preservando estilo e contratos de sugestão | review_passed | 786a215 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-149 e inventário dos produtores de sugestões em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 90 testes); `npm test` ✅ (60 arquivos, 511 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o modal de IA e conferir as sugestões padrão, incluindo resumo, encaminhamento, erros gramaticais no editor e dados sensíveis | Compatibilidade preservada: os quatro ícones mantêm `fas fa-magic azulColor`; receberam somente o hook aditivo da feature. Os cartões conservam os atributos `data-type`, `data-send` e dados de documento/processo, e a delegação continua por `seipro-ai-suggestion-action`. Teste estrutural cobre os quatro produtores e contratos. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-150. Revisão checker aprovada: diff focado do commit `786a215` confirma uma única fatia aditiva nos quatro produtores de ícones de sugestões padrão, teste estrutural e cópia verbatim; `src/` é a fonte da verdade, os contratos `fas fa-magic azulColor`, `data-type`, `data-send`, dados de documento/processo e delegação `seipro-ai-suggestion-action` foram preservados. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 511 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. Próxima fatia: seleção dinâmica após A1-150. |

| A1-151 | 151 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao ícone de sugestões favoritas (`seipro-ai-favorite-suggestion-icon`), preservando cartão e remoção de favoritos | review_passed | faf4bc8 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-150 e inventário do produtor de favoritos em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 91 testes); `npm test` ✅ (60 arquivos, 512 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir o modal de IA, carregar sugestões favoritas e confirmar ícone, remoção e envio do cartão sem erro novo | Compatibilidade preservada: o ícone favorito mantém as classes legadas `fas fa-star cinzaColor`; recebeu somente o hook aditivo `seipro-ai-favorite-suggestion-icon`. O cartão preserva `data-type` personalizado, `data-index`, a ação `seipro-ai-remove-suggestion` e a leitura de `favoritePromptAI`. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-151. Revisão checker aprovada: diff de `faf4bc8` restrito à adição do hook no produtor, teste estrutural e cópia verbatim; `src/` é a fonte da verdade, as classes legadas do cartão, `data-type`, `data-index`, remoção e leitura de `favoritePromptAI` foram preservados. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 512 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; `cmp -s manifest.base.json dist/manifest.json` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. Próxima fatia: seleção dinâmica após A1-151. |

| A1-152 | 152 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao ícone de salvar prompt favorito (`seipro-ai-favorite-prompt-icon`), preservando alternância e armazenamento legados | review_passed | 49653cd | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-151 e inventário do controle `favoritePromptAI` em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 91 testes); `npm test` ✅ (60 arquivos, 512 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir prompt personalizado, salvar/remover favorito e confirmar a alternância do ícone sem erro novo | Compatibilidade preservada: o contêiner mantém id `favoritePromptAI`, o listener delegado e `localStorage` legado; o ícone mantém `far`/`fas fa-star azulColor`, recebe o hook da feature e os dois atualizadores locais passam a selecioná-lo explicitamente. Teste estrutural cobre emissão e as duas atualizações de classe. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-152. Revisão checker aprovada: diff do commit `49653cd` restrito a `src/`, teste estrutural, cópia verbatim e board; `src/` é a fonte da verdade, `favoritePromptAI`, listener, classes legadas, alternância `far`/`fas fa-star azulColor` e armazenamento foram preservados. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 512 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. Próxima fatia: seleção dinâmica após A1-152. |

| A1-153 | 153 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado compartilhado aos ícones de aviso das credenciais (`seipro-ai-token-warning-icon`), preservando os avisos OpenAI/Gemini/Ollama | review_passed | 0c98214 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-152 e inventário dos três templates de credenciais em `src/features/ai/sei-pro-ai.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/ai-css-prefix.test.js` ✅ (1 arquivo, 92 testes); `npm test` ✅ (60 arquivos, 513 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), abrir os avisos de credenciais OpenAI, Gemini e Ollama/LiteLLM e confirmar ícones/conteúdo sem erro novo | Compatibilidade preservada: os três ícones mantêm as classes legadas `fas fa-exclamation-triangle`; receberam somente o hook aditivo da feature. Os avisos, textos e templates de plataforma permanecem iguais. Teste estrutural cobre os três produtores e seus textos distintivos. Somente a cópia verbatim `dist/js/sei-pro-ai.js` foi gerada pelo build; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-153. Revisão checker aprovada: diff do commit `0c98214` confirma uma única fatia aditiva nos três produtores de ícones de aviso em `src/features/ai/sei-pro-ai.js`, com teste estrutural e cópia verbatim correspondente; `src/` é a fonte da verdade, as classes legadas e os avisos/textos OpenAI/Gemini/Ollama foram preservados, sem edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 513 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s src/features/ai/sei-pro-ai.js dist/js/sei-pro-ai.js` retornou código 0; `cmp -s manifest.base.json dist/manifest.json` retornou código 0; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 3. Aprovado. Próxima fatia: seleção dinâmica após A1-153. |
| A1-154 | 154 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao loader da análise de documento-modelo (`seipro-doclote-analysis-loader`), preservando id e spinner legados | review_passed | d47215f | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-153 e produtor de análise em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 11 testes); `npm test` ✅ (60 arquivos, 514 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, selecionar documento-modelo, iniciar análise e confirmar loader/fluxo sem erro novo | Compatibilidade preservada: o loader mantém id `loaderAnalysis`, proteção contra duplicação, spinner legado e remoção pelo fluxo de análise; recebeu apenas o hook próprio da feature. Teste estrutural cobre hook, id, spinner e remoção. Saída gerada correspondente limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-154. Revisão checker aprovada: diff focado do commit `d47215f` confirma uma única adição de hook no loader em `src/features/docs-lote/view.js`, teste estrutural e bundle gerado; o id `loaderAnalysis`, spinner legado, proteção contra duplicação e remoção foram preservados. `src/` é a fonte da verdade; `dist/js/docs-lote.bundle.js` foi reproduzido pelo build, sem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 514 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima fatia: seleção dinâmica após A1-154. |
| A1-155 | 155 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao loader da análise CSV (`seipro-doclote-csv-analysis-loader`), preservando id e spinner legados | review_passed | 8c29560 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-154 e produtor CSV em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 12 testes); `npm test` ✅ (60 arquivos, 515 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, enviar CSV, iniciar análise e confirmar loader/fluxo sem erro novo | Compatibilidade preservada: o loader mantém id `loaderAnalysisCSV`, proteção contra duplicação, spinner legado e remoção no callback de análise; recebeu apenas o hook próprio da feature. Teste estrutural cobre hook, id, spinner e remoção. Saída gerada correspondente limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-155. Revisão checker aprovada: diff focado do commit `8c29560` confirma uma única adição de hook no produtor CSV em `src/features/docs-lote/view.js`, teste estrutural e bundle gerado; `src/` é a fonte da verdade, o id `loaderAnalysisCSV`, proteção contra duplicação, spinner legado e remoção no callback foram preservados, sem edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 515 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` retornou código 0; `dist/js/docs-lote.bundle.js` permaneceu igual ao artefato commitado após o build; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 2. Aprovado. Próxima fatia: seleção dinâmica após A1-155. |
| A1-156 | 156 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao loader de execução (`seipro-doclote-execution-loader`), preservando progresso e spinner legados | review_passed | 869f74b | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-155 e produtor `loaderBox` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 13 testes); `npm test` ✅ (60 arquivos, 516 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), executar Docs em Lote e confirmar loader/progresso de criação, cancelamento e conclusão sem erro novo | Compatibilidade preservada: o contêiner mantém o id `preparingProgressCircular`, spinner legado e remoção por `docsLote_execute`; recebeu somente o hook de feature. O id `preparingProgress` e o fluxo de abertura do modal permanecem iguais. Teste estrutural cobre hook e contratos. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; smoke manual permanece pendente conforme seção 2. Revisão checker aprovada: o commit `869f74b` altera somente o hook emitido em `src/features/docs-lote/templates.js`, o teste estrutural, o bundle gerado e o registro do board; `src/` é a fonte da verdade, o id `preparingProgressCircular`, o id `preparingProgress`, o spinner legado e a remoção por `docsLote_execute` foram preservados, sem edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 516 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` retornou código 0; `dist/js/docs-lote.bundle.js` permaneceu igual ao artefato commitado após o build; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 2. Aprovado. Próxima fatia: seleção dinâmica após A1-156. |

| A1-157 | 157 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao contêiner de progresso da execução (`seipro-doclote-execution-progress`), preservando id e atualizações legados | review_passed | a4c4220 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-156 e produtor `loaderBox` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 14 testes); `npm test` ✅ (60 arquivos, 517 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), executar Docs em Lote e confirmar atualização do progresso, cancelamento e conclusão sem erro novo | Compatibilidade preservada: o contêiner mantém id `progress` e o filho `preparingProgress`; `setProgress` e o fluxo de execução continuam atualizando-o pelos mesmos seletores legados. Foi adicionado somente o hook de feature. Teste estrutural cobre hook, ids e atualizações. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-157. Revisão checker aprovada: o diff de `a4c4220` é uma fatia única e aditiva em `src/features/docs-lote/templates.js`, no teste estrutural e no bundle gerado; `src/` é a fonte da verdade, o id `progress`, o filho `preparingProgress`, `setProgress` e as atualizações via `$('#progress')` foram preservados, sem edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 14 testes); `npm test` ✅ (60 arquivos, 517 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅ após o build; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece pendente conforme seção 2. Aprovado. Próxima fatia: seleção dinâmica após A1-157. |

| A1-158 | 158 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao conteúdo do diálogo de erro de execução (`seipro-doclote-error-dialog`), preservando as mensagens legadas | review_passed | 81b3291 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-157 e produtor `erroBox` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 15 testes); `npm test` ✅ (60 arquivos, 518 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), executar Docs em Lote e confirmar que o diálogo de erro exibe as mensagens e fecha normalmente | Compatibilidade preservada: o wrapper externo continua sendo `dialogBoxDocLote` com `dialogBoxDiv seipro-doclote-dialog`; as mensagens fixas, `textError` e o fluxo `docLoteModalErro` permanecem iguais. Foi adicionado somente o hook da feature ao conteúdo do erro. Teste estrutural cobre hook e mensagens; saída gerada limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-158. Revisão checker aprovada: diff de `81b3291` confirma uma única adição de hook no produtor `erroBox`, teste estrutural e bundle gerado; `src/` é a fonte da verdade, as mensagens fixas, `textError`, `dialogBoxDocLote`, `dialogBoxDiv` e o fluxo `docLoteModalErro` foram preservados. `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` retornou código 0 após o build; `cmp -s manifest.base.json dist/manifest.json` retornou código 0. Gates checker reais: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 518 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme seção 2. Aprovado. |

| A1-159 | 159 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao painel de cruzamento de dados (`seipro-doclote-crossing-panel`), preservando o id legado | review_passed | c845d61 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-158 e produtor `dataCrossingPanel` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 16 testes); `npm test` ✅ (60 arquivos, 519 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, avançar ao cruzamento CSV/modelo e confirmar painel, tabela, seletores e opção de novo processo sem erro novo | Compatibilidade preservada: o contêiner mantém id `divTableDataCrossing`, tabela `tableDataCrossing`, campos e fluxo de cruzamento; recebeu somente o hook aditivo da feature. Teste estrutural cobre o hook e os contratos. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-159. Revisão checker aprovada: diff de `c845d61` confirma uma única adição de hook no produtor `dataCrossingPanel`, teste estrutural e bundle gerado; `src/` é a fonte da verdade, o id `divTableDataCrossing`, a tabela `tableDataCrossing`, os campos e o fluxo de cruzamento foram preservados. Gates checker no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 519 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅ após o build; `cmp -s manifest.base.json dist/manifest.json` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme seção 2. Aprovado. |

| A1-160 | 160 | feature | arvore | `docs-lote` / CSS prefixado | Mover a delegação do toggle de novo processo para `.seipro-doclote-new-process-toggle`, preservando o id legado | review_passed | 8fc364e | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-159 e produtor/consumidor `newProcs` em `src/features/docs-lote/` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 17 testes); `npm test` ✅ (60 arquivos, 520 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), no cruzamento CSV/modelo ativar e desativar “Criar cada documento em um novo processo”, conferir os campos de tipo/especificação e iniciar a execução sem erro novo | Compatibilidade preservada: o input mantém id `newProcs`, name, `data-type`, label, estilo `onoffswitch-checkbox` e leitura interna no payload `createNewProcs`; somente o listener delegado passou ao hook da feature. Teste estrutural cobre emissor, label, delegação e fluxo. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; `dist/js/init_visualizacao.js` sem churn incidental. Próxima ação: checker revisar A1-160. Revisão checker aprovada: diff de `8fc364e` confirma uma única mudança de hook no produtor e no listener delegado, teste estrutural e bundle gerado; `src/` é a fonte da verdade, o id `newProcs`, name, `data-type`, label, leitura de `createNewProcs` e fluxo de campos foram preservados, sem edição manual de `dist/`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 520 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅ após o build; `cmp -s manifest.base.json dist/manifest.json` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme seção 2. Aprovado. |

| A1-161 | 161 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao toggle de forçar nomes (`seipro-doclote-force-names-toggle`), preservando id e leitura de execução legados | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-160 e produtor/consumidor `checkForceNames` em `src/features/docs-lote/` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 18 testes); `npm test` ✅ (60 arquivos, 521 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), no cruzamento CSV/modelo em SEI anterior, ativar/desativar “Forçar atribuição de nomes na Árvore”, iniciar a execução e confirmar que o comportamento de nomes permanece correto | Compatibilidade preservada: o input mantém id `checkForceNames`, `name="onoffswitch"`, `data-type="setdate"`, label e estilo compartilhado `onoffswitch-checkbox`; recebeu somente o hook de feature. A montagem de `forceNames` ainda lê o id legado antes de `docsLote_execute`. Teste estrutural cobre o hook e os contratos de execução. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; `dist/js/init_visualizacao.js` sem churn incidental. Próxima ação: checker revisar A1-161. Revisão checker: commit `4169d68` revisado por diff exato; a fatia é única e aditiva, limitada ao produtor `src/features/docs-lote/templates.js`, teste estrutural, bundle gerado e board. `src/` é a fonte da verdade; `checkForceNames`, id, name, data-type, label, estilo legado e leitura em `docsLote_execute` foram preservados. `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅ após o rebuild; a comparação direta com `templates.js` não se aplica porque o bundle é transformado. Gates checker reais: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 521 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme seção 2. Aprovado. |

| A1-162 | 162 | feature | arvore | `docs-lote` / CSS prefixado | Mover a delegação do seletor de tipo de processo para `.seipro-doclote-process-type-select`, preservando o id legado | review_passed | 280916a | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-161 e produtor/consumidor `tipoProcessoSelect` em `src/features/docs-lote/` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 19 testes); `npm test` ✅ (60 arquivos, 522 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), no cruzamento CSV/modelo ativar novo processo, selecionar tipo de processo e confirmar que a validação/liberação da execução permanece funcional | Compatibilidade preservada: o seletor mantém id `tipoProcessoSelect`, inicialização Chosen, leitura interna para `idTipoProcedimento` e validação existente; somente o listener delegado passou ao hook da feature. Teste estrutural cobre produtor, delegação, Chosen e fluxo. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; build sem churn incidental em `dist/js/init_visualizacao.js`. Revisão checker aprovada: commit `280916a` revisado por diff exato; a fatia é única e limitada ao hook aditivo no produtor, à delegação, ao teste estrutural, ao bundle gerado e ao board. `src/` é a fonte da verdade; o id `tipoProcessoSelect`, inicialização Chosen, leitura de `idTipoProcedimento` e validação `checkTipoProcessoSelect` foram preservados. `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅ após o rebuild; `cmp -s manifest.base.json dist/manifest.json` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 522 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: escolher a próxima fatia dinâmica somente após esta aprovação.

| A1-163 | 163 | feature | arvore | `docs-lote` / CSS prefixado | Mover a delegação do seletor de tipo de documento-modelo para `.seipro-doclote-document-type-select`, preservando o id legado | review_passed | 20a9fff | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-162 e produtor/consumidor `tipoDocumentoSelect` em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 20 testes); `npm test` ✅ (60 arquivos, 523 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), em Docs em Lote selecionar Texto Padrão, escolher tipo de documento e confirmar a liberação/bloqueio de Avançar sem erro novo | Compatibilidade preservada: o seletor mantém id `tipoDocumentoSelect`, inicialização Chosen e leitura interna para atualizar `S.selectedModel.id_tipo_documento`; somente o listener de mudança passou ao hook da feature. Teste estrutural cobre produtor, delegação e atualização do modelo; `funcoes.csv` foi regenerado por deslocamento de linhas. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-163. Revisão checker aprovada: diff focal do commit `20a9fff` confirma uma única fatia de delegação do seletor de tipo de documento-modelo; `src/` é a fonte da verdade, o id `tipoDocumentoSelect`, inicialização Chosen, leitura de `S.selectedModel.id_tipo_documento` e liberação de `btnConfirmAnalysis` foram preservados; `dist/js/docs-lote.bundle.js` foi somente regenerado pelo build. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 523 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅ após o rebuild; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅, sem churn incidental. Smoke manual permanece necessário conforme seção 2. Aprovado. |
| A1-164 | 164 | feature | arvore | `docs-lote` / listener delegado | Mover a delegação da seleção de documento-modelo para `.seipro-doclote-model-select`, preservando o id legado | review_passed | b02ce33 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-163 e produtor/consumidor `docLoteSelect` em `src/features/docs-lote/` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 21 testes); `npm test` ✅ (60 arquivos, 524 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, selecionar documento-modelo, confirmar a limpeza do Texto Padrão e a liberação de Avançar sem erro novo | Compatibilidade preservada: o seletor mantém id `docLoteSelect`, label, inicialização Chosen e preenchimento por `getDocsArvore_fillSelect`; apenas o listener de mudança passou ao hook da feature. O reset de `textoPadraoSelect` e a liberação de `btnSelecaoDoc` continuam no mesmo fluxo. Teste estrutural cobre o hook, ausência do listener direto por id e os efeitos locais. `src/` é fonte da verdade; `dist/js/docs-lote.bundle.js` foi somente regenerado; `funcoes.csv` foi regenerado por deslocamento de linhas em `view.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-164. Revisão checker aprovada: diff focal do commit `b02ce33` confirma uma única migração da delegação de mudança de documento-modelo para `.seipro-doclote-model-select`; o produtor em `templates.js` preserva id `docLoteSelect`, label, Chosen e preenchimento por `getDocsArvore_fillSelect`, enquanto `view.js` preserva o reset de `textoPadraoSelect` e a liberação de `btnSelecaoDoc`. `src/` é a fonte da verdade; `dist/js/docs-lote.bundle.js` reproduziu o build atual, sem edição manual nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 524 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅ após o rebuild; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar a próxima fatia para revisão. |
| A1-165 | 165 | feature | arvore | `docs-lote` / listener delegado | Mover a delegação da seleção de Texto Padrão para `.seipro-doclote-template-select`, preservando o id legado | review_passed | 48ef618 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-164 e produtor/consumidor `textoPadraoSelect` em `src/features/docs-lote/` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 22 testes); `npm test` ✅ (60 arquivos, 525 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, selecionar Texto Padrão, confirmar a limpeza do documento-modelo e a liberação de Avançar sem erro novo | Compatibilidade preservada: o seletor mantém id `textoPadraoSelect`, label, inicialização Chosen e carregamento por `txtPadrao_getList`; somente o listener de mudança passou ao hook já emitido pela feature. O reset de `docLoteSelect` e a liberação de `btnSelecaoDoc` continuam no mesmo fluxo. Teste estrutural cobre hook, ausência do listener direto por id e efeitos locais. `src/` é fonte da verdade; `dist/js/docs-lote.bundle.js` foi somente regenerado; mapas foram regenerados por deslocamento de linhas em `view.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Checker: diff focal de `48ef618` confirma fatia única; `src/` é fonte da verdade, o hook aditivo já emitido, o id legado, Chosen, `txtPadrao_getList`, reset de `docLoteSelect` e liberação de `btnSelecaoDoc` foram preservados. `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅ após o build; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅; gates no checkout atual: `git status --short` limpo, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 525 testes), `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. |

| A1-166 | 166 | feature | arvore | `docs-lote` / listener delegado | Mover a detecção de codificação do upload CSV para `.seipro-doclote-csv-input`, preservando o input legado | review_passed | 5b32e0b | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-165 e produtor/consumidor `inputBD` em `src/features/docs-lote/` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 22 testes); `npm test` ✅ (60 arquivos, 525 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), em Docs em Lote selecionar CSV e confirmar detecção de codificação, habilitação do avanço e análise dos cabeçalhos sem erro novo | Compatibilidade preservada: o input mantém id `inputBD`, `accept` CSV, label e o segundo listener que habilita o botão Avançar; somente o listener de detecção de codificação passou a usar o hook da feature, preservando `FileReader`, `jschardet` e `S.CSVEncoding`. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; `funcoes.csv` regenerado pelo deslocamento de metadados da função. Sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-166. Revisão checker aprovada: diff focal de `5b32e0b` confirma uma fatia única em `src/features/docs-lote/view.js`, teste estrutural, bundle gerado e board; `src/` é a fonte da verdade, o `id="inputBD"`, aceitação CSV, `FileReader`, `jschardet`, `S.CSVEncoding` e o segundo listener que habilita Avançar foram preservados. A delegação usa o hook `.seipro-doclote-csv-input` sem remover o contrato legado interno por id. `dist/js/docs-lote.bundle.js` reproduziu o build atual; não houve edição manual de `dist/` nem churn em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 22 testes); `npm test` ✅ (60 arquivos, 525 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar a próxima fatia para revisão. |

| A1-167 | 167 | feature | arvore | `docs-lote` / listener delegado | Mover a habilitação de Avançar após upload CSV para `.seipro-doclote-csv-input`, preservando o input legado | review_passed | 5531cfc | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-166 e segundo consumidor de `inputBD` em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 22 testes); `npm test` ✅ (60 arquivos, 525 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), em Docs em Lote selecionar CSV, confirmar habilitação de Avançar, detecção de codificação e análise dos cabeçalhos sem erro novo | Compatibilidade preservada: o input mantém id `inputBD`, aceitação CSV, label e o listener de detecção de codificação; somente o listener que habilita `btnEnviaCSV` passou à mesma classe da feature, com namespace `docsLoteAdvance` e limpeza antes da reinstalação. Teste estrutural cobre as duas delegações e ausência do listener direto por id. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; `funcoes.csv` regenerado porque a associação estática da função ganhou uma chamada. Sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-167. Revisão checker aprovada: diff focal do commit `5531cfc` confirma uma única migração da habilitação de Avançar para a delegação namespaced em `.seipro-doclote-csv-input`; `src/` é a fonte da verdade, o id `inputBD`, aceitação CSV, label, listener de detecção de codificação e efeito de habilitar `btnEnviaCSV` foram preservados. `dist/js/docs-lote.bundle.js` reproduziu o build atual; não houve edição manual nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 525 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar a próxima fatia para revisão. |

| A1-168 | 168 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao campo de especificação de novo processo (`seipro-doclote-process-specification-input`), preservando id e payload de execução | review_passed | beed190 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-167 e produtor/consumidor `txtEspecificacaoProcesso` em `src/features/docs-lote/` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 23 testes); `npm test` ✅ (60 arquivos, 526 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), no cruzamento CSV/modelo ativar novo processo, preencher a especificação com campos dinâmicos e confirmar a execução sem erro novo | Compatibilidade preservada: o campo mantém id `txtEspecificacaoProcesso`, classe compartilhada `infraText`, placeholder e leitura para `txtEspecificacaoProcesso` no payload de execução; recebeu somente o hook aditivo da feature. Teste estrutural cobre hook, id, placeholder e fluxo de payload. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; `dist/js/init_visualizacao.js` sem churn incidental. Revisão checker aprovada: diff focal do commit `beed190` confirma uma fatia aditiva única no produtor `templates.js`; `src/` é a fonte da verdade, o hook `seipro-doclote-process-specification-input` foi adicionado sem remover `infraText`, e os contratos `id`, placeholder e payload de execução permanecem preservados. `dist/js/docs-lote.bundle.js` coincide com o build atual; não houve edição manual em `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 526 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar a próxima fatia para revisão. |
| A1-169 | 169 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado compartilhado aos seletores de nome do documento (`seipro-doclote-document-name-select`), preservando opções e Chosen | review_passed | cc8b1f2 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-168 e produtores condicionais `nomesDoc` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 24 testes); `npm test` ✅ (60 arquivos, 527 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), no cruzamento CSV/modelo confirmar o seletor Nome do documento em SEI anterior e SEI novo, busca Chosen e execução sem erro novo | Compatibilidade preservada: os dois produtores condicionais mantêm o id `nomesDoc`, as opções `selectData` e a inicialização Chosen por id; receberam apenas o hook aditivo compartilhado da feature. Teste estrutural exige os dois produtores, as opções e o fluxo Chosen. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; `dist/js/init_visualizacao.js` sem churn incidental. Revisão checker independente: diff focal de `cc8b1f2` confirma fatia única e aditiva nos dois produtores condicionais; `src/` é a fonte da verdade, ids `nomesDoc`, opções `selectData`, inicialização Chosen por id e fluxo em `view.js` foram preservados. `dist/js/docs-lote.bundle.js` reproduziu o build atual; não houve edição manual nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 527 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar a próxima fatia para revisão. |

| A1-170 | 170 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao crédito do Docs em Lote (`seipro-doclote-credit`), preservando atribuição e link externo | review_passed | 25071cb | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-169 e template estático `creditoTcgontijo` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 25 testes); `npm test` ✅ (60 arquivos, 528 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote em contexto que exiba o crédito e confirmar texto/link externo sem erro novo | Compatibilidade preservada: o template mantém o texto de atribuição, link `https://github.com/tcgontijo`, `target="_blank"`, estilo inline e referência a PluriDocs SEI; recebeu somente o hook aditivo da feature. Teste estrutural cobre hook e contratos. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; build não gerou churn em `dist/js/init_visualizacao.js`. Próxima ação: aguardar a próxima fatia para revisão. Revisão checker independente: diff focal do commit `25071cb` confirma uma única fatia aditiva no produtor `src/features/docs-lote/templates.js`, com teste estrutural e bundle gerado; `src/` é a fonte da verdade, o texto de atribuição, link externo, `target="_blank"`, estilo inline e referência a PluriDocs SEI foram preservados. `dist/js/docs-lote.bundle.js` reproduziu o build atual; não houve edição manual em `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 528 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. |

| A1-171 | 171 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao wrapper do switch de novo processo (`seipro-doclote-new-process-switch`), preservando o switch compartilhado | review_passed | d09614f | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-170 e wrapper `onoffswitch` de `newProcs` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 26 testes); `npm test` ✅ (60 arquivos, 529 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), no cruzamento CSV/modelo ativar/desativar Criar cada documento em um novo processo e confirmar aparência, campos dependentes e execução sem erro novo | Compatibilidade preservada: o wrapper mantém a classe compartilhada `onoffswitch`; o input `newProcs`, a classe de toggle, label, delegação e leitura no payload `createNewProcs` não mudam. Foi adicionado somente o hook de feature; teste estrutural cobre o hook e os contratos do switch. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-171. Revisão checker independente: diff focal do commit `d09614f` confirma uma única fatia aditiva no produtor `src/features/docs-lote/templates.js`, teste estrutural e bundle gerado; `src/` é a fonte da verdade, a classe compartilhada `onoffswitch`, input `newProcs`, toggle, label, delegação e leitura no payload foram preservados. `dist/js/docs-lote.bundle.js` reproduziu o build atual; não houve edição manual em `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 529 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima fatia: aguardar nova migração pendente. |

| A1-172 | 172 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao título estático da etapa de análise de documento-modelo (`seipro-doclote-analysis-title`), preservando o texto e o fluxo | review_passed | ef237d8 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-171 e produtor `analiseDocModeloBox` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 27 testes); `npm test` ✅ (60 arquivos, 530 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, iniciar a etapa de análise do documento-modelo e confirmar título, carregamento e avanço sem erro novo | Compatibilidade preservada: a mudança é aditiva no `<p>` emitido por `analiseDocModeloBox`; texto, wrapper legado e fluxo de análise permanecem iguais. `dist/js/docs-lote.bundle.js` foi atualizado somente pelo build; não houve churn incidental em `dist/js/init_visualizacao.js`. Revisão checker independente: diff focal do commit `ef237d8` confirma uma única fatia aditiva em `src/features/docs-lote/templates.js`, com teste estrutural e bundle gerado; `src/` é a fonte da verdade, o texto do título, wrapper legado e fluxo da etapa de análise foram preservados, e não houve edição manual em `dist/`. `dist/js/docs-lote.bundle.js` reproduziu o build atual; não houve churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 530 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente. |

| A1-173 | 173 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado à tabela de resultados (`seipro-doclote-result-table`), preservando id e classes compartilhadas | review_passed | 8cc72d2 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-172 e produtor `resultTable` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 530 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), executar Docs em Lote até a tabela de resultados e confirmar tabela, rolagem e botões Baixar/Copiar sem erro novo | Compatibilidade preservada: `tableDataResult` e as classes compartilhadas `seiProForm tableInfo tableZebra tableFollow` permanecem; foi adicionado somente o hook próprio da feature no produtor da tabela e no bundle gerado. Revisão checker independente: diff focal do commit `8cc72d2` confirma uma única fatia aditiva no produtor `src/features/docs-lote/templates.js`, com teste estrutural e bundle gerado; `src/` é a fonte da verdade, o id `tableDataResult` e as classes compartilhadas `seiProForm tableInfo tableZebra tableFollow` foram preservados. O build atual reproduziu `dist/js/docs-lote.bundle.js` sem diferença em relação ao `HEAD`, e não houve churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 530 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente. |

| A1-174 | 174 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado à ação de cancelar execução (`seipro-doclote-cancel-execution`), preservando id e abort legados | review_passed | a9d04ee | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-173 e ação `cancelExecute` em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 28 testes); `npm test` ✅ (60 arquivos, 531 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), executar Docs em Lote, cancelar a execução e confirmar abortamento/fechamento sem erro novo | Compatibilidade preservada: o botão jQuery UI mantém id `cancelExecute` e chama `docsLote_abortAjax()`; foi adicionado somente `class: 'seipro-doclote-cancel-execution'` como hook próprio. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; mapas regenerados por deslocamento de linhas em `view.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-174. Revisão checker aprovada: diff focal do commit `a9d04ee` confirma uma única fatia aditiva em `src/features/docs-lote/view.js`, com teste estrutural, bundle gerado e mapas; o id `cancelExecute`, a chamada `docsLote_abortAjax()` e os ocultamentos internos legados foram preservados. `src/` permanece fonte da verdade; `dist/js/docs-lote.bundle.js` reproduziu o build atual, sem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 531 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado.

| A1-175 | 175 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao título da etapa de análise CSV (`seipro-doclote-analysis-csv-title`), preservando o texto e o fluxo | review_passed | 9278f05 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-174 e produtor `analiseCsvBox` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 531 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, iniciar a análise CSV e confirmar título, carregamento e avanço sem erro novo | Compatibilidade preservada: o texto “Análise da base de dados” permanece igual; foi adicionado somente o hook próprio da feature no parágrafo emitido por `analiseCsvBox`. Saída gerada correspondente limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: checker revisar A1-175. Revisão checker aprovada: diff focal do commit `9278f05` confirma uma única fatia aditiva em `src/features/docs-lote/templates.js`, com teste estrutural, bundle gerado e board; o texto e o fluxo da análise CSV foram preservados, e não houve edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 531 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --check` do commit ✅; bundle ESM validado pela reconstrução sem diferença no checkout atual. Smoke manual permanece necessário conforme seção 2. Aprovado. |

| A1-176 | 176 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao texto introdutório do painel de cruzamento (`seipro-doclote-crossing-intro`), preservando o texto e o fluxo | review_passed | 2ed3e8d | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-175 e produtor `cruzamentoDadosBox` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 532 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, avançar ao cruzamento CSV/modelo e confirmar o texto introdutório, painel e fluxo sem erro novo | Compatibilidade preservada: apenas o parágrafo introdutório recebeu o hook próprio; texto, wrapper, IDs, tabelas, seletores e fluxo de cruzamento permanecem iguais. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; sem edição manual de `dist/` nem churn incidental. Próxima ação do checker: revisar a emissão do hook e os contratos preservados, repetir os gates e confirmar o bundle gerado. Revisão checker aprovada: o diff focal do commit `2ed3e8d` confirma uma única fatia aditiva em `src/features/docs-lote/templates.js`, com teste estrutural e bundle gerado; o texto introdutório, wrapper, IDs, tabelas, seletores e fluxo de cruzamento foram preservados, e `src/` é a fonte da verdade. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 532 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. |
| A1-177 | 177 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao painel inicial de seleção (`seipro-doclote-selection-panel`), preservando `seiProForm` e os seletores de documento/texto padrão | review_passed | f0527d6 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-176 e inventário de templates em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 29 testes); `npm test` ✅ (60 arquivos, 532 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, confirmar o painel inicial, selecionar documento-modelo/texto padrão e avançar sem erro novo | Compatibilidade preservada: o hook é aditivo no `<table>` inicial; `seiProForm`, `dialogBoxDocLote`, `docLoteSelect`, `textoPadraoSelect`, Chosen e delegações legadas permanecem iguais. Saída gerada somente em `dist/js/docs-lote.bundle.js`; nenhum churn incidental em `dist/js/init_visualizacao.js`. Revisão checker aprovada: diff focal do commit `f0527d6` confirma uma única adição de hook no produtor `selecaoDocBox`, com teste estrutural e bundle gerado; `src/` é a fonte da verdade, `seiProForm`, `dialogBoxDocLote`, ids/selectores `docLoteSelect`/`textoPadraoSelect`, Chosen e delegações legadas foram preservados. `dist/js/docs-lote.bundle.js` reproduziu o build atual; não houve edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais no checkout atual: `git status --short` limpo antes dos gates; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 532 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `cmp -s manifest.base.json dist/manifest.json` retornou código 0. Smoke manual permanece necessário conforme seção 2. Aprovado. |
| A1-178 | 178 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao painel de seleção de CSV (`seipro-doclote-csv-selection-panel`), preservando `seiProForm` e o input legado | review_passed | cd8920b | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-177 e produtor `selecaoBaseDadosBox` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 533 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, selecionar arquivo CSV e confirmar painel de seleção, input, análise e avanço sem erro novo | Compatibilidade preservada: a tabela de upload recebeu somente o hook próprio `seipro-doclote-csv-selection-panel`; `seiProForm`, `inputBD`, `type="file"`, aceitação CSV e os listeners legados permanecem iguais. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; `dist/js/init_visualizacao.js` sem churn incidental. Revisão checker aprovada: diff focal do commit `cd8920b` confirma uma única adição de hook no produtor `src/features/docs-lote/templates.js`, com teste estrutural e bundle gerado; `src/` é a fonte da verdade, `seiProForm`, `inputBD`, `type="file"`, aceitação CSV e listeners legados foram preservados, sem edição manual de `dist/`. Checker rerun no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 533 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 30 testes); `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente. |

| A1-179 | 179 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado à mensagem de conclusão da execução (`seipro-doclote-execution-complete`), preservando texto e tabela de resultados | review_passed | 7f44dd1 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-178 e produtor de conclusão em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 31 testes); `npm test` ✅ (60 arquivos, 534 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), executar Docs em Lote até a conclusão e confirmar mensagem, tabela de resultados, rolagem e botões Baixar/Copiar sem erro novo | Compatibilidade preservada: foi adicionado somente o hook próprio ao `<h4>` de conclusão; o texto, o contêiner `progress`, a tabela `${tableResult}` e o redimensionamento do diálogo permanecem iguais. Saída gerada correspondente limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. O checker deve revisar o diff focado, repetir os gates e confirmar o bundle gerado. Revisão checker aprovada: diff de `7f44dd1` restrito à adição do hook no produtor `src/features/docs-lote/view.js`, teste estrutural, bundle gerado e board; `src/` é a fonte da verdade, o texto, `progress`, `${tableResult}` e redimensionamento do diálogo foram preservados. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 534 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Sem edição manual de `dist/`; smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente. |
| A1-180 | 180 | feature | arvore | `docs-lote` / CSS prefixado | Completar o hook do wrapper da opção de nomes no ramo legado (`seipro-doclote-force-names`), preservando checkbox e fluxo legados | review_passed | 5e2b48c | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-179 e segundo produtor do wrapper em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 31 testes); `npm test` ✅ (60 arquivos, 534 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js` | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), exercitar Docs em Lote no ramo legado/SEI anterior, alternar “Forçar atribuição de nomes” e confirmar execução sem erro novo | Adicionado o mesmo hook prefixado ao wrapper de nomes do ramo `!isNewSEI`, completando os dois produtores sem alterar `checkForceNames`, label, `data-type` ou leitura em `docsLote_execute`. `src/` é a fonte da verdade; `dist/js/docs-lote.bundle.js` foi gerado pelo build. Revisão checker aprovada: diff focal do commit `5e2b48c` confirma uma única adição de hook no segundo produtor, teste estrutural atualizado e bundle gerado; os contratos do checkbox e do fluxo legado foram preservados, sem edição manual de `dist/`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 534 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 31 testes); `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente. |

| A1-181 | 181 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado aos links de documentos gerados (`seipro-doclote-generated-document-link`), preservando `bLink`, URL e abertura em nova aba | review_passed | 83e1454 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-180 e produtor do link de resultado em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 32 testes); `npm test` ✅ (60 arquivos, 535 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js` | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), concluir uma execução de Docs em Lote e abrir os links `link_documento_gerado` em nova aba, confirmando URL e aparência sem erro novo | Hook aditivo no único produtor de links de documentos gerados; classe legada `bLink`, `href`, texto da URL e `target="_blank"` permanecem. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; teste estrutural cobre o hook e contratos preservados. Revisão checker aprovada: diff focal de `83e1454` confirma uma única adição no produtor, teste estrutural e bundle gerado; `src/` é a fonte da verdade, sem edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 535 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. |

| A1-182 | 182 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado à ação de confirmação do diálogo de erro (`seipro-doclote-error-confirm`), preservando botão OK e reset do diálogo | review_passed | 8838846 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-181 e produtor da ação em `src/features/docs-lote/view.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 536 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), provocar erro de execução do Docs em Lote, confirmar botão OK e fechamento/reset do diálogo sem erro novo | Revisão checker aprovada: diff focal do commit `8838846` confirma uma única adição de hook no produtor `src/features/docs-lote/view.js`, com teste estrutural e bundle gerado; `src/` é a fonte da verdade, os contratos `OK`, `ui-state-active` e `resetDialogBoxPro('dialogBoxPro')` foram preservados e não houve edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 536 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. |

| A1-183 | 183 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado à ação de confirmação do cruzamento (`seipro-doclote-crossing-confirm`), preservando classe compartilhada, id e payload de execução | review_passed | 5408a58 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-182 e ação `btnConfirm` em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 34 testes); `npm test` ✅ (60 arquivos, 537 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), executar Docs em Lote até o cruzamento, clicar “Iniciar” e confirmar payload/avanço sem erro novo | Mudança aditiva de uma linha no botão `btnConfirm`: preserva `confirm ui-state-active`, o id e todos os campos de `paramData`; saída gerada limitada a `dist/js/docs-lote.bundle.js`. Revisão checker aprovada: diff focado do commit `5408a58` confirma uma única adição de hook no produtor, teste estrutural e bundle gerado; `src/` é a fonte da verdade, a classe compartilhada, o id `btnConfirm` e o payload de execução foram preservados, sem edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 537 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente. |
| A1-184 | 184 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado à tabela de cruzamento (`seipro-doclote-crossing-table`), preservando id e classes compartilhadas | review_passed | 39aa362 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-183 e produtor `dataCrossingPanel` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 35 testes); `npm test` ✅ (60 arquivos, 538 testes) | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, avançar ao cruzamento CSV/modelo e confirmar tabela, seletores, opção de novo processo e execução sem erro novo | Hook aditivo no `<table id="tableDataCrossing">`; preservados id, `seiProForm`, `tableInfo`, `tableZebra`, `tableFollow`, wrapper/rolagem e fluxo de cruzamento. Saída gerada correspondente limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Revisão checker aprovada: o commit `39aa362` altera somente o produtor em `src/features/docs-lote/templates.js`, o teste estrutural, o bundle gerado e este registro; `src/` é a fonte da verdade, sem edição manual de `dist/`, e os contratos do id, classes compartilhadas, wrapper e fluxo foram preservados. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 538 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente. |

| A1-185 | 185 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado aos cabeçalhos da tabela de cruzamento (`seipro-doclote-crossing-table-header`), preservando `tituloControle` e conteúdo | review_passed | f8023cf | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-184 e produtor `dataCrossingPanel` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 36 testes); `npm test` ✅ (60 arquivos, 539 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, avançar ao cruzamento CSV/modelo e confirmar cabeçalhos, tabela e fluxo sem erro novo | Fatia aditiva única: os três `<th>` preservam a classe compartilhada `tituloControle` e os conteúdos `${csvFileName}`/`${modeloNome}`, recebendo apenas o hook próprio da feature. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; nenhum churn em `dist/js/init_visualizacao.js`. Revisão checker aprovada: diff de `f8023cf` confirma três adições do mesmo hook no produtor, teste estrutural e bundle gerado; `src/` é a fonte da verdade, `tituloControle` e os conteúdos dos cabeçalhos foram preservados, sem edição manual de `dist/`. Gates checker reais no checkout atual: `git status --short` limpo antes e depois do build; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 539 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente. |

| A1-186 | 186 | feature | arvore | `docs-lote` / CSS prefixado | Completar o hook dos cabeçalhos da tabela de resultados (`seipro-doclote-result-table-header`), preservando `tituloControle` e conteúdo dinâmico | review_passed | b7107b2 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-185 e cabeçalhos gerados em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 37 testes); `npm test` ✅ (60 arquivos, 540 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), concluir uma execução de Docs em Lote e confirmar cabeçalhos dinâmicos, tabela de resultados, links e botões Baixar/Copiar sem erro novo | Hook aditivo nos quatro produtores de cabeçalhos gerados em `docsLote_execute`; classe compartilhada `tituloControle`, nomes das colunas e fluxo de tabela permanecem iguais. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação do checker: revisar o diff focado, repetir os gates e confirmar o bundle ESM gerado. Revisão checker aprovada: diff de `b7107b2` confirma uma única fatia aditiva nos quatro produtores de cabeçalhos dinâmicos, teste estrutural e bundle gerado; `src/` é a fonte da verdade, `tituloControle`, nomes das colunas e fluxo da tabela foram preservados, sem edição manual de `dist/`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 540 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. |
| A1-187 | 187 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado à mensagem inicial de preparação (`seipro-doclote-preparing-progress`), preservando id e texto legados | review_passed | 8666bb2 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-186 e produtor `loaderBox` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 38 testes); `npm test` ✅ (60 arquivos, 541 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), iniciar Docs em Lote e confirmar a mensagem de preparação, spinner, atualizações de progresso e execução sem erro novo | Mudança aditiva de uma linha no produtor; `id="preparingProgress"`, texto e atualizações legadas permanecem. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação do checker: revisar a emissão do hook, contratos preservados e bundle gerado. Revisão checker aprovada: diff de `8666bb2` confirma uma única adição aditiva no produtor `loaderBox` em `src/features/docs-lote/templates.js`, teste estrutural e bundle gerado; `id="preparingProgress"`, texto, spinner e atualizações legadas foram preservados, sem edição manual de `dist/`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 541 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. |

| A1-188 | 188 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao ícone do diálogo de erro (`seipro-doclote-error-icon`), preservando ícone legado, mensagem e fluxo | review_passed | 5fc8383 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-187 e produtor `erroBox` em `src/features/docs-lote/templates.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 38 testes); `npm test` ✅ (60 arquivos, 541 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), provocar erro no Docs em Lote e confirmar ícone, mensagem e fechamento/reset sem erro novo | Hook aditivo apenas no `<i>` do produtor `erroBox`; classes legadas `fas fa-exclamation-triangle vermelhoColor`, texto, `textError` e fluxo do diálogo permanecem. Saída gerada correspondente limitada a `dist/js/docs-lote.bundle.js`; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação do checker: revisão aprovada no commit de migração `5fc8383`: a alteração é uma única adição no ícone produzido por `erroBox`, preservando classes legadas, mensagem, `textError` e fluxo do diálogo; `src/` é a fonte da verdade e `dist/js/docs-lote.bundle.js` corresponde ao build reproduzido, sem edição manual de `dist/`. Gates checker reais no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 541 testes); `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. |

| A1-189 | 189 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado à confirmação da seleção inicial (`seipro-doclote-selection-confirm`), preservando id e callback legados | review_passed | 2d9478d | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-188 e botão `btnSelecaoDoc` em `src/features/docs-lote/view.js` | `git diff --check`; `npm test`; `node scripts/engineering-loop-map.mjs --check` | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, selecionar documento-modelo ou Texto Padrão, avançar e confirmar análise sem erro novo | Hook aditivo no botão jQuery UI `btnSelecaoDoc`; classes compartilhadas, id, leitura dos seletores e callback `docLoteModalAnaliseDocModelo` permanecem intactos. Saída gerada esperada em `dist/js/docs-lote.bundle.js`; o checker deve repetir os gates e confirmar o bundle ESM gerado. Revisão checker aprovada: o commit `2d9478d` adiciona apenas o hook ao produtor `btnSelecaoDoc`, preservando id, classes compartilhadas, seletores de documento/texto padrão e callback `docLoteModalAnaliseDocModelo`; `src/` é a fonte da verdade e `dist/js/docs-lote.bundle.js` foi reproduzido pelo build, sem edição manual de `dist/` nem churn incidental em `dist/js/init_visualizacao.js`. Gates checker reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 542 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente. |

| A1-190 | 190 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado à ação de confirmação do upload CSV (`seipro-doclote-csv-confirm`), preservando id, classes compartilhadas e fluxo de análise | review_passed | 7e33a07 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-189 e ação `btnEnviaCSV` em `src/features/docs-lote/view.js` | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 543 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 40 testes); `cmp -s manifest.base.json dist/manifest.json` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` esperado antes do commit (bundle atualizado pela fatia); sem churn incidental em `dist/js/init_visualizacao.js` | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, enviar CSV, confirmar botão Avançar, análise/validação e execução sem erro novo | O botão mantém id `btnEnviaCSV`, classes compartilhadas `confirm ui-state-active`, estado inicial desabilitado, leitura de `inputBD`, validação CSV, mensagem de erro e chamada `docLoteModalAnaliseCSV`; foi adicionado somente o hook próprio. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; `src/` é a fonte da verdade. Próxima ação do checker: revisar o diff focado, repetir os gates e confirmar o bundle ESM gerado. Revisão checker aprovada: o commit `7e33a07` adiciona somente o hook no produtor `btnEnviaCSV`, preservando id, classes compartilhadas, estado inicial, leitura/validação do CSV e callback `docLoteModalAnaliseCSV`; `src/` é a fonte da verdade e `dist/` foi regenerado, sem edição manual. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 543 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente. |

| A1-191 | 191 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado à confirmação da análise de documento-modelo (`seipro-doclote-analysis-confirm`), preservando id, botão e callback | review_passed | 5a470a4 | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-190 e produtor `btnConfirmAnalysis` em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 41 testes); `npm test` ✅ (60 arquivos, 544 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, avançar pela análise do documento-modelo e confirmar que o botão Avançar mantém aparência e callback sem erro novo | Compatibilidade preservada: o botão mantém id `btnConfirmAnalysis`, classes compartilhadas `confirm ui-state-active` e callback `docLoteModalSelecaoBaseDados`; foi adicionado somente o hook próprio `seipro-doclote-analysis-confirm`. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; `src/` é a fonte da verdade, `dist/` não foi editado manualmente. Próxima ação: checker revisar a emissão do hook, contratos preservados e bundle ESM gerado. Revisão checker aprovada: o diff focal do commit `5a470a4` confirma uma única adição no produtor `btnConfirmAnalysis`, teste estrutural e bundle gerado; `src/` é a fonte da verdade, o id, as classes compartilhadas e o callback foram preservados, sem edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 544 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `cmp -s manifest.base.json dist/manifest.json` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅ após o build; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente.
| A1-192 | 192 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado à confirmação da análise CSV (`seipro-doclote-csv-analysis-confirm`), preservando id, botão e callback | review_passed | 06b0bcc | (este commit) | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-191 e produtor `btnConfirmAnalysisCSV` em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 42 testes); `npm test` ✅ (60 arquivos, 545 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir Docs em Lote, avançar pela análise CSV e confirmar que o botão Avançar mantém aparência e callback para o cruzamento sem erro novo | Compatibilidade preservada: o botão mantém id `btnConfirmAnalysisCSV`, classes compartilhadas `confirm ui-state-active` e callback `docLoteModalCruzamentoDados`; foi adicionado somente o hook próprio da feature. Saída gerada limitada a `dist/js/docs-lote.bundle.js`; `src/` é a fonte da verdade e não houve churn incidental em `dist/js/init_visualizacao.js`. Revisão checker aprovada: diff focado do commit `06b0bcc` confirma uma única adição no produtor `btnConfirmAnalysisCSV`, teste estrutural e bundle gerado; `src/` é a fonte da verdade, id, classes compartilhadas e callback foram preservados, sem edição manual de `dist/`. Gates checker no checkout atual: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 545 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `git diff --exit-code HEAD -- dist/js/docs-lote.bundle.js` ✅; `git diff --exit-code HEAD -- dist/js/init_visualizacao.js` ✅. Smoke manual permanece necessário conforme seção 2. Aprovado. Próxima ação: aguardar nova migração pendente.

| A1-193 | 193 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao contêiner dos campos de análise do documento (`seipro-doclote-model-analysis-fields`), preservando id e renderização | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-192 e contêiner `fieldList` em `src/features/docs-lote/view.js` | `git diff --check` ✅; teste focal `npx vitest run tests/structure/docs-lote-css-prefix.test.js` ✅ (1 arquivo, 43 testes); `npm test` ✅ (60 arquivos, 546 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), abrir análise de documento-modelo/texto padrão e confirmar campos detectados, títulos e avanço sem erro novo | Mudança aditiva no produtor de `fieldList`; o id e os seletores/renderização legados permanecem. Checker deve revisar o diff focal, repetir os gates e confirmar o bundle ESM gerado. Próxima ação: checker revisar A1-193. |

## Regras de atualização

- O migration job deve marcar no máximo uma linha por execução como `migration_in_progress` e depois `migrated_pending_review`, ou registrar bloqueio objetivo.
- O verification job deve revisar apenas linhas `migrated_pending_review` e alterar para `review_passed` ou `review_failed_needs_fix`.
- Se um job escolher uma fatia dinâmica que ainda não está no board, deve adicionar a linha antes ou durante a implementação.
- Não remover histórico; adicionar observações em vez de apagar decisões relevantes.
- `Commit migração` e `Commit verificação` podem ser preenchidos com hash curto após commit.
