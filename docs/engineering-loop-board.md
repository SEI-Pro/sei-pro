# Engineering Loop Board — SEI Pro PRF

> Atualize este arquivo a cada execução do loop.  
> Estados válidos: `pending_migration`, `migration_in_progress`, `migrated_pending_review`, `review_in_progress`, `review_failed_needs_fix`, `review_passed`, `blocked`.

## Board

| ID | Prioridade | Tipo | Contexto | Feature/Camada | Fatia | Estado | Commit migração | Commit verificação | Fonte | Gate | Smoke scope | Observações |
|---|---:|---|---|---|---|---|---|---|---|---|---|---|
| L0-001 | 0 | docs | global | loop | Criar `docs/engineering-loop.md` | review_passed | bootstrap-atual | bootstrap-atual | `docs/engineering-loop-architecture-evaluation.md` | `git diff --check`; `npm test` | n/a | Documento canônico criado na implantação inicial do loop. |
| L0-002 | 0 | docs | global | loop | Criar `docs/engineering-loop-board.md` | review_passed | bootstrap-atual | bootstrap-atual | `docs/engineering-loop-architecture-evaluation.md` | `git diff --check`; `npm test` | n/a | Board criado como registro auditável, não fila fechada. |
| L0-003 | 0 | map | global | loop-map | Criar `scripts/engineering-loop-map.mjs` | review_passed | bootstrap-atual | bootstrap-atual | `docs/engineering-loop-architecture-evaluation.md` | `node scripts/engineering-loop-map.mjs --check`; `npm test` | n/a | Script gerador de mapa criado sem depender do repo antigo. |
| L0-004 | 0 | map | global | loop-map | Gerar `docs/engineering-loop-map.md` e CSVs de mapa a partir de `src/` | review_passed | bootstrap-atual | bootstrap-atual | `scripts/engineering-loop-map.mjs` | `node scripts/engineering-loop-map.mjs --check`; `npm test` | n/a | Artefatos gerados pelo script local. |
| L0-005 | 0 | infra | global | hermes-cron | Criar novos cron jobs no repo correto, com migração em `openai-codex/gpt-5.5` e verificação 30min depois | review_passed | bootstrap-atual | bootstrap-atual | `docs/engineering-loop.md` | `cronjob list`; `git diff --check`; `npm test` | n/a | Jobs antigos ficam pausados; novos jobs apontam para `/home/tadeu/repos/sei-pro-prf`. |
| A1-001 | 1 | core | global | `src/core/stack.js` / `monitorados` | Tirar dependência de feature de dentro de `core/stack.js`: instalar `installMonitoradoStore` na ponte transitória `src/content/core-stack.js` | review_passed | e7471a2 | (este commit) | `DEVELOPMENT.md` linhas 291-292 e tabela de violações conhecidas | `git diff --check` ✅; `npm test` ✅ (49 arquivos, 382 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 5 (Todas as páginas); atenção a globais de monitorados antes de `sei-functions-pro`/`init*` | Core não importa mais `../features/monitorados/store.js`; compat legada preservada porque `core-stack.bundle.js` ainda instala os aliases globais antes dos scripts legados. Teste estrutural novo em `tests/structure/no-duplicate-core.test.js` guarda que `src/core/stack.js` não importe `../features/*`. Verificação: diff revisado, gates executados com output real, `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace). Próxima fatia sugerida: mapear e iniciar separação domain/io/view de `src/features/monitorados/` ou próxima dívida explícita do `DEVELOPMENT.md`. |
| A1-002 | 2 | feature | lista-processos | `controlar-prazos` / `gerenciarprazos` | Mover a ponte `aliasGlobal` de `src/features/controlar-prazos/index.js` para `legacy-api.js`, preservando a superfície global legada | review_passed | 4c42c47 | (este commit) | `DEVELOPMENT.md` linhas 123, 156-159 e violação `src/features/*/index.js` com `aliasGlobal` | `git diff --check` ✅; `npm test` ✅ (50 arquivos, 384 testes); `node scripts/engineering-loop-map.mjs --check` ✅ após regenerar `docs/engineering-loop-map.md`; `dist/js/init_visualizacao.js` revertido por mudança incidental do build | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco em `initControlePrazo`, coluna de prazos e handlers de adicionar/alterar prazo | Compatibilidade preservada: `index.js` continua importando `./legacy-api.js`, e o novo arquivo reexpõe todas as funções de `domain/io/view` com os mesmos nomes globais. Saída gerada correspondente incluída em `dist/js/sei-pro-controle-prazo.js`; mapa atualizado para 129 arquivos JS analisados. Teste estrutural novo impede regressão de `aliasGlobal` direto em `features/*/index.js` e verifica a ponte de controlar-prazos. Próxima fatia sugerida: aplicar o mesmo padrão de `legacy-api.js` a outra feature/index com aliases ou reduzir gradualmente handlers inline de `controlar-prazos`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real, `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace). Aprovado. |
| A1-003 | 3 | legacy-bridge | global | `monitorados` / store | Mover os aliases globais do store de `src/features/monitorados/store.js` para a ponte antecipada `store-legacy-api.js`, mantendo instalação via `core-stack` | review_passed | ca70a89 | (este commit) | `DEVELOPMENT.md` linhas 123, 156-159 e violação conhecida de `aliasGlobal` fora de `legacy-api.js`; board A1-001/A1-002 | `git diff --check` ✅; `npm test` ✅ (50 arquivos, 386 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace) | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 5 (Todas as páginas), com foco em globais `getStoreMonitoradoPro`/`saveConfigMonitorado` disponíveis antes dos scripts legados e persistência de monitorados | Compatibilidade preservada: `src/content/core-stack.js` agora chama `installMonitoradoStoreLegacyApi()`, que instala o namespace do store e reexpõe exatamente os mesmos aliases globais antes de `sei-functions-pro`/`init*`; `store.js` fica como IO sem `aliasGlobal`. Saída gerada correspondente incluída apenas em `dist/js/core-stack.bundle.js`; mapa atualizado para 130 arquivos JS analisados. Teste estrutural ampliado garante que chamadas reais a `aliasGlobal()` em `src/features/**` fiquem em pontes `legacy-api` e cobre a ponte antecipada de monitorados. Próxima fatia sugerida: reduzir a exceção transitória de `core-stack` criando entry/contexto específico para os aliases de monitorados ou atacar a próxima dívida explícita do background monolítico. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real, `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace). Aprovado. |
| A1-004 | 4 | background | background | `background.js` / storage | Extrair o roteamento das ações `storageGet`/`storageSet`/`storageRemove` para `src/background/storage-handler.js`, mantendo a API de mensagens legada | review_passed | b0b7c6f | 82ba840 | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e seleção dinâmica após A1-003 | `git diff --check` ✅; `npm test` ✅ (51 arquivos, 387 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace) | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar fluxos que usam storage/background (options, lista de processos/notificação, features com persistência) | Compatibilidade preservada: o service worker clássico continua em `dist/js/background.js` e carrega `storage-handler.js` por `importScripts`; as ações e payloads legados não mudam, apenas delegam para `SeiProBackgroundStorage.handleStorageMessage`. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/storage-handler.js`; `scripts/build.mjs` copia o novo adapter; mapa regenerado para 131 arquivos JS analisados. Teste estrutural novo cobre carregamento/delegação do adapter. Próxima fatia sugerida: extrair uma segunda parte pequena do background monolítico, preferencialmente o roteamento de `fetch`/bug report ou um adapter de notificações, mantendo ações legadas. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado e confirmado idêntico ao build), gates executados com output real, `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace). Aprovado. |
| A1-005 | 5 | background | background | `background.js` / fetch | Extrair o roteamento da ação legada `fetch` para `src/background/fetch-handler.js`, mantendo validação de remetente/host e resposta assíncrona | review_passed | 5e07dbb | (este commit) | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e sugestão do board A1-004 | `git diff --check` ✅; `npm test` ✅ (52 arquivos, 388 testes); `node scripts/engineering-loop-map.mjs --check` ✅; `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace) | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar fluxos que usam background/fetch, especialmente chamada `fetch` delegada ao service worker (Gemini/resolveCaptchaAI) e console sem erros de runtime | Compatibilidade preservada: o service worker clássico continua em `dist/js/background.js` e carrega `storage-handler.js` + `fetch-handler.js` por `importScripts`; a ação `fetch` mantém os mesmos payloads (`url`, `options`) e respostas (`ok`, `status`, `body`/`error`), apenas delegando para `SeiProBackgroundFetch.handleFetchMessage`. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/fetch-handler.js`; `scripts/build.mjs` copia o novo adapter; mapa regenerado para 132 arquivos JS analisados. Teste estrutural novo cobre carregamento/delegação do adapter de fetch, e o teste do storage foi relaxado para aceitar múltiplos scripts carregados no mesmo `importScripts`. Próxima fatia sugerida: extrair uma próxima parte pequena do background monolítico, preferencialmente o envio de `enviarRelatorioBug` ou um adapter de notificações, mantendo ações legadas. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado e confirmado idêntico ao build), gates executados com output real, `dist/js/init_visualizacao.js` revertido (geração incidental de whitespace). Aprovado. |
| A1-006 | 6 | background | background | `background.js` / bug report | Extrair o roteamento da ação legada `enviarRelatorioBug` para `src/background/bug-report-handler.js`, mantendo validação de host e fallback POST→GET | review_passed | 64c1ebf | (este commit) | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e sugestão do board A1-005 | `git diff --check` ✅; `npm test` ✅ (54 arquivos, 390 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar fluxo de envio de relatório de bug/logs via `src/platform/report.js`, verificando console sem erro novo no service worker | Compatibilidade preservada: o service worker clássico continua em `dist/js/background.js` e carrega `storage-handler.js` + `fetch-handler.js` + `bug-report-handler.js` por `importScripts`; a ação `enviarRelatorioBug` mantém os mesmos payloads (`url`, `payload`) e respostas (`ok`/`erro`), apenas delegando para `SeiProBackgroundBugReport.handleBugReportMessage`. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/bug-report-handler.js`; `scripts/build.mjs` copia o novo adapter; mapa regenerado para 133 arquivos JS analisados. Teste estrutural novo cobre carregamento/delegação do adapter de bug report e garante que `isAllowedBugReportSender` saiu do monólito. Próxima fatia sugerida: extrair uma próxima parte pequena do background monolítico, preferencialmente notificações de processos (`syncNotificacaoProcessos`/badge) para um adapter dedicado, mantendo ações legadas. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado e confirmado idêntico ao build atual), gates executados com output real (54 arquivos, 390 testes passaram), `dist/js/init_visualizacao.js` sem churn incidental (árvore limpa após `npm test`). Aprovado. |
| A1-007 | 7 | background | background | `background.js` / notificações de processos | Extrair o roteamento das ações legadas `syncNotificacaoProcessos`/`syncNotificacaoProcessosConfig` para `src/background/process-notification-handler.js`, mantendo badge e notifications | review_passed | 3e98c0e | (este commit) | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e sugestão do board A1-006 | `git diff --check` ✅; `npm test` ✅ (55 arquivos, 391 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar notificações de processos/badge na lista de processos, verificando console sem erro novo no service worker | Compatibilidade preservada: o service worker clássico continua em `dist/js/background.js` e carrega `storage-handler.js` + `fetch-handler.js` + `bug-report-handler.js` + `process-notification-handler.js` por `importScripts`; as ações `syncNotificacaoProcessos` e `syncNotificacaoProcessosConfig` preservam os mesmos payloads/respostas e delegam para `SeiProBackgroundProcessNotification.handleProcessNotificationMessage`. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/process-notification-handler.js`; `scripts/build.mjs` copia o novo adapter; mapa regenerado para 134 arquivos JS analisados. Teste estrutural novo cobre carregamento/delegação do adapter de notificações e garante que as funções de sync/notificação saíram do monólito. Próxima fatia sugerida: checker revisar A1-008; depois, se aprovado, avaliar se ainda resta roteamento no background ou escolher a próxima dívida explícita (`aliasGlobal` em features ou CSS sem prefixo). Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via `diff`), gates executados com output real (55 arquivos, 391 testes passaram), árvore limpa sem churn incidental. Aprovado. |
| A1-008 | 8 | background | background | `background.js` / instalação-atualização | Extrair o listener legado `runtime.onInstalled` para `src/background/install-handler.js`, mantendo abertura da página de boas-vindas e flag `InstallOrUpdate` | review_passed | 4e9add6 | (este commit) | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e sugestão do board A1-007 | `git diff --check` ✅; `npm test` ✅ (56 arquivos, 392 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos; em install/update real, validar página de boas-vindas/flag `InstallOrUpdate` e console sem erro novo no service worker | Compatibilidade preservada: `browser.runtime.onInstalled.addListener(handleInstalled)` continua no service worker clássico, mas `handleInstalled` delega para `SeiProBackgroundInstall.handleInstalled(details, browser)` carregado via `importScripts`; os comportamentos de `install` e `update` foram movidos sem alterar payloads, storage ou URLs. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/install-handler.js`; `scripts/build.mjs` copia o adapter; mapa regenerado para 135 arquivos JS analisados. Teste estrutural novo cobre carregamento/delegação do adapter de instalação. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild), gates executados com output real (56 arquivos, 392 testes passaram), árvore limpa sem churn incidental. A1-009 (router) já submetida como próxima fatia; checker revisar A1-009 na próxima execução. Aprovado. |
| A1-009 | 9 | background | background | `background.js` / router | Extrair o roteamento de mensagens de `background.js` para `src/background/router.js`, mantendo o service worker como fachada fina | review_passed | 5ffc5e9 | (este commit) | `DEVELOPMENT.md` linhas 359-366 (background monolítico) e sugestão do board A1-008 | `git diff --check` ✅; `npm test` ✅ (57 arquivos, 393 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar fluxos de background/storage/fetch/relatório de bug/notificações, verificando console sem erro novo no service worker | Compatibilidade preservada: `background.js` continua clássico e registrado no manifest, carrega todos os adapters e o novo `router.js` por `importScripts`, mantém `runtime.onMessage.addListener(handleMessage)` e delega para `SeiProBackgroundRouter.handleMessage(message, sender, sendResponse, browser)`; as ações legadas e respostas de indisponibilidade permanecem iguais. Saída gerada correspondente incluída em `dist/js/background.js` e novo `dist/js/router.js`; `scripts/build.mjs` copia o adapter; mapa regenerado para 136 arquivos JS analisados. Teste estrutural novo cobre a fachada fina e os testes dos adapters agora verificam a delegação no router. Próxima fatia sugerida: iniciar separação domain/io/view de outra feature (`controlar-prazos`, `monitorados`) ou atacar dívida de `aliasGlobal` em features/CSS sem prefixo. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via `diff`), gates executados com output real (57 arquivos, 393 testes passaram), árvore limpa sem churn incidental. Aprovado. |
| A1-010 | 10 | shared-legacy | global | `shared/ui/prazo-preview` / legacy bridge | Mover os aliases globais `getDatesPreview`/`getProgressPreview`/`configDatesPreview` para `src/shared/ui/prazo-preview-legacy-api.js`, preservando instalação no `core-stack` | review_passed | 4e91d26 | (este commit) | `DEVELOPMENT.md` linhas 123, 156-159 e 287-304; seleção dinâmica após A1-009 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 394 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar seções 1 (Lista de processos) e 5 (Todas as páginas), com foco em etiquetas/prévia de prazo (`getDatesPreview`) e console sem erro novo durante renderização de prazos | Compatibilidade preservada: `installCoreStack()` continua instalando `SeiPro.shared.prazoPreview` e, logo depois, chama `installPrazoPreviewLegacyApi()` para reexpor os mesmos três globais usados por scripts legados; o módulo compartilhado `prazo-preview.js` deixa de importar/chamar `aliasGlobal`. Saída gerada correspondente incluída em `dist/js/core-stack.bundle.js`, `dist/js/db.bundle.js` e `dist/js/login.bundle.js`; mapa regenerado para 137 arquivos JS analisados. Teste estrutural novo garante que os aliases globais de prazo fiquem apenas na ponte `prazo-preview-legacy-api.js`. Próxima fatia sugerida: checker revisar A1-011; depois escolher outra ponte global pequena ou iniciar fatias de CSS prefixado quando houver escopo visível controlado. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild), gates executados com output real (58 arquivos, 394 testes passaram), árvore limpa sem churn incidental. Aprovado. |
| A1-011 | 11 | feature | lista-processos | `controlar-prazos` / CSS prefixado | Prefixar as classes próprias da coluna de prazos (`prazoBoxDisplay`/`addControlePrazo`) para `.seipro-*`, preservando seletores consumidores | review_passed | d090fa3 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de CSS sem prefixo em `src/features/*/*.css`; seleção dinâmica após A1-010 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 394 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar seção 1 (Lista de processos), com foco na coluna “Prazos”, botão “Adicionar prazo”, agrupamento por prazo e espelho de prazo em Monitorados; console sem erro novo | Compatibilidade preservada: a função/global legada `addControlePrazo` e os marcadores `data-seipro-add-prazo` não mudam; apenas as classes DOM/CSS próprias passaram para `seipro-prazo-box-display` e `seipro-add-controle-prazo`. Consumidores em `lista-processos/sei-pro.js` e `monitorados/panel.js` foram atualizados para o novo seletor. Saída gerada correspondente incluída em `dist/css/controlar-prazos.css`, `dist/js/sei-pro-controle-prazo.js`, `dist/js/sei-pro.js` e `dist/js/monitorados.bundle.js`; teste estrutural novo trava o prefixo e ausência das classes antigas em `src/`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 394 testes passaram), sem churn incidental. Resíduos de `addControlePrazo` em `src/` são apenas nomes de função global legado e strings de replace de onclick→data-attr, não classes CSS — preservação correta. Aprovado. Próxima fatia sugerida: continuar CSS prefixado pequeno em outra feature visível ou revisar exceções compartilhadas remanescentes. |
| A1-012 | 12 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar a classe própria da tabela de monitorados (`tableMonitorados`) para `.seipro-table-monitorados`, preservando os seletores consumidores | review_passed | 565bed5 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de CSS sem prefixo em `src/features/*/*.css`; sugestão A1-011 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 395 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: recarregar extensão conforme `SMOKE_TEST.md` pré-requisitos e exercitar seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, edição de etiquetas/cores/ícones e console sem erro novo | Compatibilidade preservada: `panel.js` continua gerando a mesma tabela/IDs legados (`monitoradosProDiv`, `monitoradoTablePro`) e mantém classes compartilhadas (`tableInfo`, `tableZebra`, `infraTable`, `tableFollow`, `tabelaControle`); só a classe própria de styling da feature foi prefixada. O seletor legado em `sei-functions-pro.js` que atualiza ícones de etiquetas agora procura `.seipro-table-monitorados tbody`. Saída gerada correspondente incluída em `dist/css/monitorados.css`, `dist/js/monitorados.bundle.js` e `dist/js/sei-functions-pro.js`; teste estrutural ampliado garante ausência de `.tableMonitorados` em `src/`. Próxima fatia sugerida: continuar CSS prefixado pequeno em outra feature visível (ex.: outra classe própria de monitorados ou de `controlar-prazos`) ou atacar exceção `aliasGlobal`/legacy-bridge remanescente explícita no `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 395 testes passaram), sem churn incidental. Aprovado. |
| A1-013 | 13 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook DOM próprio de remoção em lote (`iconMonitorados_remove`) para `.seipro-monitorados-remove-selected`, preservando o `data-act` legado | review_passed | 42a9cd4 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-012 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 396 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, seleção de linhas e exibição/acionamento da remoção em lote; console sem erro novo | Compatibilidade preservada: o contrato comportamental continua por `data-act="remove-selected"` e pelo listener delegado de `panel-lifecycle.js`; apenas o hook CSS/DOM próprio da feature foi prefixado. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `iconMonitorados_remove` em `panel.js`/`panel-lifecycle.js`. Próxima fatia sugerida: continuar prefixando hooks próprios pequenos de monitorados (`iconMonitorados_update`/`iconMonitorados_config`) ou outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 396 testes passaram), sem churn incidental. Aprovado. |
| A1-014 | 14 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook DOM próprio de atualização (`iconMonitorados_update`) para `.seipro-monitorados-update`, preservando o `data-act` legado | review_passed | 1efb9d2 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-013 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 397 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, botão “Atualizar Informações” e console sem erro novo | Compatibilidade preservada: o contrato comportamental continua por `data-act="update"` e pelo listener delegado existente; apenas o hook CSS/DOM próprio da feature foi prefixado. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `iconMonitorados_update` em `src/`. Próxima fatia sugerida: continuar prefixando o hook próprio remanescente de monitorados (`iconMonitorados_config`) ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 397 testes passaram), sem churn incidental. Aprovado. |
| A1-015 | 15 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook DOM próprio de configuração (`iconMonitorados_config`) para `.seipro-monitorados-config`, preservando o `data-act` legado | review_passed | 6a72612 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-014 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 398 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa após `npm test` sem churn incidental | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, botão “Configurações” e console sem erro novo | Compatibilidade preservada: o contrato comportamental continua por `data-act="config"` e pelo listener delegado existente; apenas o hook CSS/DOM próprio da feature foi prefixado. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `iconMonitorados_config` em `src/`. Próxima fatia sugerida: revisar se restam classes próprias de monitorados sem prefixo ou escolher outra dívida explícita do `DEVELOPMENT.md` (`aliasGlobal` fora de `legacy-api.js` ou CSS sem prefixo em outra feature). Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 398 testes passaram), sem churn incidental. Aprovado. |
| A1-016 | 16 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar o hook DOM próprio de configuração de prazo (`monitoradoConfigDates`) para `.seipro-monitorado-config-dates`, preservando o `data-act` legado | review_passed | a308517 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-015 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 399 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco nas opções de prazo de processos monitorados e console sem erro novo | Compatibilidade preservada: o contrato comportamental continua por `data-act="dates-config"` e pelo listener delegado existente; apenas o hook CSS/DOM próprio da feature foi prefixado e o consumidor em `prazo-row.js` passou a consultar o seletor novo. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `monitoradoConfigDates` em `src/features/monitorados`. Próxima fatia sugerida: revisar outras classes próprias remanescentes de monitorados sem prefixo (`tdmonitorado_*`, `info_*`) com cautela por serem compartilhadas com legado, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real (58 arquivos, 399 testes passaram), árvore limpa sem churn incidental. Aprovado. |
| A1-017 | 17 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar a classe própria das células espelho nativas (`tdmonitorado_native`) para `.seipro-monitorado-native-cell`, preservando o contrato `data-native` | review_passed | 1e9d6c2 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-016 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 400 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, espelhos de prazo/marcador/anotação e console sem erro novo | Compatibilidade preservada: o comportamento continua guiado por `data-native` e pelos IDs/estruturas legadas do painel; apenas a classe DOM própria da feature usada nas células espelho foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `tdmonitorado_native` em `panel.js`. Próxima fatia sugerida: avaliar outras classes próprias remanescentes de monitorados (`tdmonitorado_tipo`, `td_monitorado_category`, `info_category*`) com cautela por consumidores locais/legados, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 400 testes passaram), sem churn incidental. Aprovado. |
| A1-018 | 18 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar a classe própria da célula de tipo (`tdmonitorado_tipo`) para `.seipro-monitorado-type-cell`, preservando o contrato `data-native="tipo"` | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-017 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 401 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, coluna “Tipo de Processo” e console sem erro novo | Compatibilidade preservada: o conteúdo da célula continua guiado por `data-native="tipo"` e pelo mesmo link `data-act="remove-row"`; apenas a classe DOM própria da feature foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `tdmonitorado_tipo` em `src/`. Próxima fatia sugerida: checker revisar A1-018; depois avaliar a classe própria de categoria (`td_monitorado_category`/`info_category*`) com cautela por consumidores locais, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real (58 arquivos, 401 testes passaram), árvore limpa sem churn incidental. Aprovado. |
| A1-019 | 19 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar a classe própria da célula de categoria (`td_monitorado_category`) para `.seipro-monitorado-category-cell`, preservando os hooks internos de edição | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-018 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 402 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, coluna “Categoria”, edição/filtro de categoria e console sem erro novo | Compatibilidade preservada: os hooks locais `info_category_txt`, `info_category` e `data-act="category-edit"` continuam iguais para o dispatcher de categorias; apenas a classe DOM própria da célula foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `td_monitorado_category` em `src/`. Próxima fatia sugerida: checker revisar A1-019; depois avaliar `info_category*` com cautela por serem hooks internos de edição, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real (58 arquivos, 402 testes passaram), árvore limpa sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-020 | 20 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar os hooks internos de edição de categoria (`info_category_txt`/`info_category`) para classes `.seipro-*`, preservando o dispatcher de categoria | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-019 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 402 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, coluna “Categoria”, editar/salvar/nova categoria e console sem erro novo | Compatibilidade preservada: o contrato comportamental segue pelos atributos `data-act` de categoria e pelo dispatcher de `categorias.js`; apenas os hooks DOM internos da célula foram prefixados para `seipro-monitorado-category-text` e `seipro-monitorado-category-editor`. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava os novos hooks e ausência de `info_category*` em `panel.js`/`categorias.js`. Próxima fatia sugerida: avaliar a próxima classe própria remanescente de monitorados (`tdmonitorado_tags`) com cautela por consumidores compartilhados/atividades, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado e confirmado idêntico ao build), gates executados com output real (58 arquivos, 402 testes passaram), árvore limpa sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-021 | 21 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a classe própria da célula de etiquetas de monitorado (`tdmonitorado_tags`) para `.seipro-monitorado-tags-cell`, preservando o modo de etiquetas | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-020 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 403 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco nas etiquetas de processos monitorados, botão “Adicionar etiqueta” e console sem erro novo | Compatibilidade preservada: o contrato comportamental continua pelo atributo `data-etiqueta-mode` e pelo dispatcher de etiquetas existente; apenas a classe DOM/CSS própria da célula de etiquetas em `visualizacao.js` foi prefixada para `seipro-monitorado-tags-cell`. Saída gerada correspondente incluída em `dist/css/monitorados.css` e `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `tdmonitorado_tags` nos arquivos da fatia. Próxima fatia sugerida: checker revisar A1-021; depois avaliar classes próprias remanescentes de monitorados ou atacar outra dívida explícita do `DEVELOPMENT.md`. |
| A1-022 | 22 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a classe própria do box de opções de monitorado (`monitoradosLabelOptions`) para `.seipro-monitorados-label-options`, preservando seleção legada de etiquetas | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-021 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 404 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no checkbox “Manter processo em Processos Monitorados”, edição de categoria/prazo/etiquetas e console sem erro novo | Compatibilidade preservada: o box continua dentro de `divSinAdicionarMonitorados` e mantém `seiProForm`, ids e inputs legados; consumidores em `datas.js` e `sei-functions-pro.js` passaram a consultar a classe prefixada. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js` e `dist/js/sei-functions-pro.js`; teste estrutural ampliado trava o novo hook e ausência dos seletores antigos nos consumidores da fatia. Próxima fatia sugerida: avaliar hooks próprios remanescentes de monitorados, como `monitoradoTagsPro`/`info_dates_*`, com cautela por classes compartilhadas, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 404 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-023 | 23 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a classe própria do input de etiquetas de monitorado (`monitoradoTagsPro`) para `.seipro-monitorado-tags-input`, preservando o `name` legado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-022 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 405 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no input de etiquetas de processos monitorados, checkbox “Manter processo em Processos Monitorados” e console sem erro novo | Compatibilidade preservada: o campo mantém `name="monitoradoTagsPro"` e o fluxo de `createTagsInput`/salvamento de etiquetas; apenas o hook DOM de classe foi prefixado e o reset em `datas.js` passou a consultar a classe nova. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga nos consumidores da fatia. Próxima fatia sugerida: avaliar os hooks de prazo remanescentes (`tdmonitorado_dates`/`info_dates_*`) com cautela por serem compartilhados com `controlar-prazos`/`atividades`, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 405 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-024 | 24 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar a classe própria do contêiner de ícones nativos copiados (`info_icons_monitorado`) para `.seipro-monitorado-native-icons`, preservando os links/ícones clonados | review_passed | 9846d2b | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-023 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 405 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, ícones nativos copiados ao lado do número do processo e console sem erro novo | Compatibilidade preservada: os links/ícones nativos filtrados continuam clonados de `#P{id}` e inseridos no mesmo ponto da célula de processo; apenas a classe DOM própria da feature foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga em `panel.js`. Próxima fatia sugerida: avaliar hooks de prazo remanescentes (`tdmonitorado_dates`/`info_dates_*`) com cautela por classes compartilhadas com `controlar-prazos`/`atividades`, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 405 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-025 | 25 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar o hook interno do editor de prazo de monitorado (`info_dates_monitorado_txt`) para `.seipro-monitorado-dates-editor`, preservando o `name` legado do input | review_passed | 8764b2c | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; sugestão A1-024 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 406 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no editor de prazo de processos monitorados, checkbox “Manter processo em Processos Monitorados” e console sem erro novo | Compatibilidade preservada: o campo mantém `name="monitoradoPrazoSend"`, id `monitoradoPrazoSend`, `data-act="dates-hide-blur"` e classe de input `.seipro-monitorado-dates`; apenas o hook DOM do span editor foi prefixado e consumidores em `datas.js`/`prazo-row.js` passaram a consultar a classe nova. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga nos consumidores da fatia. Próxima fatia sugerida: avaliar `tdmonitorado_dates`/`info_dates_follow_empty` com cautela por acoplamento com CSS legado compartilhado, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 406 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-026 | 26 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook de estado vazio de prazo (`info_dates_follow_empty`) para `.seipro-monitorado-dates-empty`, preservando a prévia compartilhada de prazo | review_passed | 5f1cce1 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-025 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 407 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel “Processos Monitorados”, coluna “Prazo”, hover “Adicionar prazo” em prazo vazio e console sem erro novo | Compatibilidade preservada: a prévia compartilhada `.info_dates_monitorado` e o fluxo `getDatesPreview` continuam iguais; apenas a classe de estado vazia controlada por `prazo-row.js` foi prefixada e o CSS passou a escopar pelo painel `.seipro-table-monitorados`. Saída gerada correspondente incluída em `dist/css/monitorados.css` e `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `tdmonitorado_dates`/`info_dates_follow_empty` nos arquivos da fatia. Próxima fatia sugerida: checker revisar A1-026; depois avaliar outros hooks próprios remanescentes de monitorados com cautela por classes compartilhadas, ou atacar outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 407 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. |
| A1-027 | 27 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar o hook visual de adicionar etiquetas (`followLinkTagsAdd_send`) para `.seipro-monitorado-tags-add`, preservando o dispatcher por `data-act` | review_passed | 8bc1090 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-026 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 407 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco em adicionar etiqueta em processos monitorados e console sem erro novo | Compatibilidade preservada: o clique continua roteado pelo atributo `data-act` de etiquetas e pelo dispatcher legado `showFollowEtiqueta`; apenas a classe DOM própria do link foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `followLinkTagsAdd_send` em `visualizacao.js`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` apenas bundle regerado), gates executados com output real (58 arquivos, 407 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. Próxima fatia sugerida: avaliar `info_tags_follow_empty`/`info_tags_follow*` com cautela por serem compartilhados com atividades/legado, ou atacar outra dívida explícita do `DEVELOPMENT.md`. |
| A1-028 | 28 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook visual de remover linha de monitorado (`followLinkMonitoradoRemove`) para `.seipro-monitorado-remove-row`, preservando o dispatcher por `data-act` | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-027 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 407 testes); `node scripts/engineering-loop-map.mjs --check` ✅; árvore limpa sem churn incidental | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel "Processos Monitorados", botão de remover linha individual e console sem erro novo | Compatibilidade preservada: o clique continua roteado pelo atributo de remover linha e pelo dispatcher delegado do painel; apenas a classe DOM própria do link foi prefixada. Classes compartilhadas `.newLink`, `.followLink`, `.followLinkTags` permanecem intactas. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`, confirmado idêntico ao build via rebuild. Teste estrutural ampliado trava o novo hook e ausência da classe antiga em `panel.js`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (58 arquivos, 407 testes passaram), sem churn incidental, `node scripts/engineering-loop-map.mjs --check` ✅. Aprovado. Próxima fatia sugerida: avaliar hooks próprios remanescentes de monitorados (`info_tags_follow*` compartilhados com atividades/legado) com cautela, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. |
| A1-029 | 29 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook visual de editar categoria (`followLinkMonitoradoCategory`) para `.seipro-monitorado-category-edit`, preservando o dispatcher por `data-act` | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-028 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 407 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel Processos Monitorados, botão de editar categoria e console sem erro novo | Compatibilidade preservada: o clique continua roteado pelo atributo de edição de categoria e pelo dispatcher delegado do painel; apenas a classe DOM própria do link foi prefixada. Classes compartilhadas `.newLink`, `.followLink` e `.followLinkTags` permanecem intactas. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga em `panel.js`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 407 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. Aprovado. Próxima fatia sugerida: avaliar hooks próprios remanescentes de monitorados (`info_tags_follow*` compartilhados com atividades/legado) com cautela, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. |
| A1-030 | 30 | feature | lista-processos | `controlar-prazos` / CSS prefixado | Prefixar a classe remanescente da coluna de prazos (`prazosBoxDisplay`) para `.seipro-prazo-box-display`, alinhando DOM com CSS já prefixado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; seleção dinâmica após A1-029 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 407 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco na coluna Prazos, ordenação/renderização do prazo por marcador e console sem erro novo | Compatibilidade preservada: funções/globais legados de controle de prazo não mudaram; apenas a classe DOM da coluna passou para o prefixo já usado por seletores e CSS da feature. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-controle-prazo.js`; teste estrutural ampliado trava a ausência de `prazosBoxDisplay` em `src/`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 407 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. Ausência de `prazosBoxDisplay`/`prazoBoxDisplay` confirmada em `src/` e `dist/`. Aprovado. Próxima fatia sugerida: avaliar outra classe própria sem prefixo em feature visível ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. |
| A1-031 | 31 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar o hook de estado vazio de etiquetas de monitorado (`info_tags_follow_empty`) para `.seipro-monitorado-tags-empty`, preservando atividades/legado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-030 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 408 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco em etiquetas de processos monitorados sem etiquetas e console sem erro novo | Compatibilidade preservada: `showFollowEtiqueta` continua usando `info_tags_follow_empty` para atividades/legado, mas aplica `seipro-monitorado-tags-empty` quando o alvo é monitorados; `info_tags_follow`/`info_tags_follow_txt` e dispatcher de etiquetas permanecem intactos. Saída gerada correspondente incluída em `dist/css/monitorados.css` e `dist/js/sei-functions-pro.js`; mapa regenerado porque `src/shared/legacy/sei-functions-pro.js` mudou. Teste estrutural ampliado trava o novo hook em CSS/legado e ausência do hook antigo no CSS de monitorados. Próxima fatia sugerida: checker revisar A1-031; depois avaliar outros hooks próprios remanescentes de monitorados com cautela por compartilhamento com atividades/legado, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. |
| A1-032 | 32 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o wrapper de rolagem do painel (`tabelaPanelScroll`) para `.seipro-monitorados-scroll`, preservando o painel legado | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-031 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 409 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel Processos Monitorados, rolagem/redimensionamento da tabela e console sem erro novo | Compatibilidade preservada: ids/containers legados `monitoradosPro` e `monitoradosProDiv` permanecem, assim como as classes compartilhadas `panelHomePro` e `panelHome`; apenas o wrapper DOM próprio do painel foi prefixado. Saída gerada correspondente incluída em `dist/css/monitorados.css` e `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `tabelaPanelScroll` nos arquivos da fatia. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 409 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. `tabelaPanelScroll` confirmada amplamente usada em outras features/shared legacy — apenas a instância emitida por `panel.js` e seus seletores CSS escopados sob `#monitoradosProDiv` foram prefixados. Aprovado. Próxima fatia sugerida: avaliar outros wrappers/classes próprios de monitorados com cautela por compartilhamento com legado, ou atacar `aliasGlobal` fora de `legacy-api.js` em outra feature. |
| A1-033 | 33 | feature | lista-processos | `monitorados` / CSS prefixado | Prefixar o hook visual do link do processo monitorado (`followLinkProcesso`) para `.seipro-monitorado-process-link`, preservando navegação e ícone nativo | review_passed | 2fa6287 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-032 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 409 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 1 (Lista de processos), com foco no painel Processos Monitorados, clique no link do processo e abertura em nova aba; console sem erro novo | Compatibilidade preservada: o href do processo, classe compartilhada `bLink`, ícone/título do tipo de processo e link `followLinkNewtab` permanecem intactos; apenas a classe DOM própria do link principal foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `followLinkProcesso` em `src/`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 409 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. `followLinkProcesso` confirmada ausente em `src/`. Aprovado. |
| A1-034 | 34 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar as linhas de seleção de documento do modal de prazo (`configDates_selectdoc`) para `.seipro-monitorado-dates-selectdoc`, preservando alternância por tipo | review_passed | 4082ccc | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-033 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, alternância “data de assinatura de documento”/“data específica” e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_*`, `data-act`, nomes globais de `legacyApi` e o contrato de alternância por `data-type` não mudaram; apenas a classe DOM própria das linhas de seleção de documento foi prefixada e o seletor dinâmico passou por `configDateClass()`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado porque `src/features/monitorados/datas.js` ganhou o helper `configDateClass`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida. Próxima fatia sugerida: checker revisar A1-034; depois continuar com uma única classe própria remanescente do modal de prazo (`configDates_setdate`, `configDates_countdays`, etc.) ou escolher outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 410 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. `configDates_selectdoc` confirmada ausente em `src/` via busca; demais `configDates_*` legados preservados intocados. Aprovado. |
| A1-035 | 35 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar as linhas de data específica do modal de prazo (`configDates_setdate`) para `.seipro-monitorado-dates-setdate`, preservando alternância por tipo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-034 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, alternância “data específica”/“data de assinatura de documento” e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_*`, atributos `data-act`/`data-type`, `legacyApi` e classes legadas irmãs do modal continuam iguais; apenas a classe DOM própria das linhas de data específica foi prefixada e a ponte dinâmica `configDateClass()` passou a mapear `setdate`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado por mudança de linhas em `src/features/monitorados/datas.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida em `datas.js`. Próxima fatia sugerida: checker revisar A1-035; depois continuar com uma única classe própria remanescente do modal de prazo (`configDates_countdays`, `configDates_newdoc`, etc.) ou escolher outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 410 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental, `dist/js/init_visualizacao.js` limpo. `configDates_setdate` confirmada ausente em `src/features/monitorados/`; remanescente em `controlar-prazos/view.js` é contexto legado fora do slice (mesma regra de A1-034). Aprovado. |
| A1-036 | 36 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a linha de opção de dias úteis do modal de prazo (`configDates_countdays`) para `.seipro-monitorado-dates-countdays`, preservando alternância por tipo | review_passed | 4cca614 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-035 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, alternância “Em número de dias”/“Em tempo relativo”, checkbox “Calcular em dias úteis” e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_*`, atributos `data-act`/`data-type`, `legacyApi` e classes legadas irmãs do modal continuam iguais; apenas a classe DOM própria da linha de dias úteis foi prefixada e a ponte dinâmica `configDateClass()` passou a mapear `countdays`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado por mudança de linhas em `src/features/monitorados/datas.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida em `datas.js`. Próxima fatia sugerida: checker revisar A1-036; depois continuar com uma única classe própria remanescente do modal de prazo (`configDates_newdoc`, `configDates_duedate`, etc.) ou escolher outra dívida explícita do `DEVELOPMENT.md`. Verificação: diff revisado (uma fatia pequena, `src/` fonte da verdade, `dist/` confirmado idêntico ao build via rebuild, árvore limpa após `npm test`), gates executados com output real (`git diff --check` ✅; `npm test` ✅ 58 arquivos, 410 testes; `node scripts/engineering-loop-map.mjs --check` ✅), sem churn incidental. `configDates_countdays` confirmada ausente em `src/`. Contrato `configDatesBox_workday`, `data-act`, `data-type` preservado. Aprovado. |
| A1-037 | 37 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar as linhas de novo documento do modal de prazo (`configDates_newdoc`) para `.seipro-monitorado-dates-newdoc`, preservando alternância por tipo | review_passed | ad93808 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-036 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, opção “Da assinatura de um novo documento”, lista/tipo de documento e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_newdoclist`/`configDatesBox_listnewdoc`, atributos `data-act`/`data-type`, `legacyApi` e classes legadas irmãs do modal continuam iguais; apenas a classe DOM própria das linhas de novo documento foi prefixada e a ponte dinâmica `configDateClass()` passou a mapear `newdoc`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado por mudança de linhas em `src/features/monitorados/datas.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida em `datas.js`. Verificação checker: diff do commit ad93808 revisado; `src/` é fonte da verdade, `dist/js/monitorados.bundle.js` coincide com o build, escopo limitado ao hook `newdoc` e sua ponte dinâmica; `git status --short` limpo antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 454 testes), `node scripts/engineering-loop-map.mjs --check` ✅; nenhum churn incidental em `dist/js/init_visualizacao.js`. Aprovado. Próxima fatia: A1-038. |
| A1-038 | 38 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar as linhas de dias decorridos do modal de prazo (`configDates_duedate`) para `.seipro-monitorado-dates-duedate`, preservando alternância por tipo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-037 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, opção “Do número de dias decorridos”, contador/modo de vencimento e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_duenumber`/`configDatesBox_duecounter`/`configDatesBox_duemode`, atributos `data-act`/`data-type`, `legacyApi` e classes legadas irmãs do modal continuam iguais; apenas a classe DOM própria das linhas de dias decorridos foi prefixada e a ponte dinâmica `configDateClass()` passou a mapear `duedate`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado por mudança de linhas em `src/features/monitorados/datas.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida em `datas.js`. Verificação checker: diff do commit 03facc2 revisado; `src/` é fonte da verdade, `dist/js/monitorados.bundle.js` coincide com rebuild, escopo limitado ao hook `duedate` e sua ponte dinâmica; `git status --short` limpo antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 455 testes; 455 testes passaram), `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. Próxima fatia: A1-039. |
| A1-039 | 39 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a linha de data de vencimento específica do modal de prazo (`configDates_duesetdate`) para `.seipro-monitorado-dates-duesetdate`, preservando alternância por tipo | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-038 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, opção “De uma data de vencimento específica”, campo de data de vencimento e console sem erro novo | Compatibilidade preservada: ids `configDatesBox_duesetdt`, atributos `data-act`/`data-type`, `legacyApi` e classes legadas irmãs fora de Monitorados continuam iguais; apenas a classe DOM própria da linha de data de vencimento específica foi prefixada e a ponte dinâmica `configDateClass()` passou a mapear `duesetdate`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; mapa regenerado por mudança de linhas em `src/features/monitorados/datas.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida em `datas.js`. Revisão aprovada: `a95eabf` altera uma única linha DOM e o mapeamento dinâmico `duesetdate`; `src/` é a fonte da verdade e `dist/js/monitorados.bundle.js` coincide com o build. Gates reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 455 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Próxima fatia: A1-040. |
| A1-040 | 40 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar a tabela de opções avançadas do modal de prazo (`configDates_advanced`) para `.seipro-monitorado-dates-advanced`, preservando alternância avançada | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-039 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 410 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco no modal de opções de prazo de monitorados, botão “Opções avançadas”, visualização de opções avançadas e console sem erro novo | Compatibilidade preservada: o botão continua usando `data-act` advanced e `newLink_active`; apenas a classe DOM própria da tabela avançada foi prefixada e o seletor de `advanced()` passou a consultar `.seipro-monitorado-dates-advanced`. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `configDates_advanced` em `datas.js`. Revisão checker aprovada: commit `6e6d9a8` altera somente a classe da tabela avançada e o seletor consumidor, mantendo `data-act="advanced"`, `newLink_active` e demais contratos; `src/` é a fonte da verdade e `dist/js/monitorados.bundle.js` foi reproduzido pelo build. Gates reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 455 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Próxima fatia: A1-041. |

| A1-041 | 41 | feature | visualizacao | `monitorados` / CSS prefixado | Prefixar o hook visual da mensagem de sucesso ao adicionar monitorado (`iframeSucessPro`) para `.seipro-monitorado-success-message`, preservando o modal de opções | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-040 | `git diff --check` ✅; `npm test` ✅ (58 arquivos, 411 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seções 1 (Lista de processos) e 4 (Visualização de documento), com foco em adicionar processo aos Monitorados pela visualização/processo e abertura do modal de opções sem erro novo | Compatibilidade preservada: o modal `openModal` continua com o mesmo título, conteúdo textual, ícone e `monitoradosLabelOptions`; apenas a classe DOM própria do `<strong>` de sucesso foi prefixada. Saída gerada correspondente incluída em `dist/js/monitorados.bundle.js`; teste estrutural ampliado trava o novo hook e ausência de `iframeSucessPro` em `commands.js`. Revisão checker aprovada: `fb697f4` altera somente o hook visual emitido em `commands.js` e o bundle correspondente; `src/` é a fonte da verdade, a classe antiga permanece ausente no produtor e o modal/texto/ícone/`monitoradosLabelOptions` foram preservados. `dist/js/monitorados.bundle.js` coincidiu com a saída reproduzida pelo build. Gates reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 456 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Smoke manual permanece necessário conforme escopo acima. Próxima fatia: A1-042. |

| A1-042 | 42 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar as linhas de tipo/especificação de processo do painel de cruzamento (`containerTipoProcessoSelect`) para `.seipro-doclote-process-type-fields`, preservando checkbox e campos legados | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; seleção dinâmica após A1-041 | `git status --short` ✅ (limpo antes/depois); `git diff --check` ✅; `npm test` ✅ (60 arquivos, 457 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, modal de cruzamento CSV/modelo, checkbox “Criar cada documento em um novo processo”, seleção de tipo/especificação e console sem erro novo | Revisão aprovada: o commit `15654e8` altera uma única classe própria nas duas linhas e o seletor show/hide correspondente; ids `newProcs`, `tipoProcessoSelect` e `txtEspecificacaoProcesso` permanecem iguais. `src/` é a fonte da verdade; `dist/js/docs-lote.bundle.js` foi confirmado coincidente com a saída do build, sem churn incidental em `dist/js/init_visualizacao.js`; teste estrutural cobre o novo hook e a ausência do antigo em `templates.js`/`view.js`. Próxima fatia: A1-043. |
| A1-043 | 43 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar a classe de texto de análise do wizard (`textAnalysis`) para `.seipro-doclote-analysis-text`, preservando campos e fluxo do modal | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-042 | `git diff --check` ✅; `npm test` ✅ (59 arquivos, 413 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, etapas de análise de documento modelo/texto padrão e CSV, seleção de tipo de documento e console sem erro novo | Compatibilidade preservada: ids `fieldList`, `fieldListCSV`, `tipoDocumentoSelect`, `btnConfirmAnalysis` e `btnConfirmAnalysisCSV` continuam iguais; apenas a classe DOM própria aplicada aos textos/listas de análise foi prefixada. Saída gerada correspondente incluída em `dist/js/docs-lote.bundle.js`; teste estrutural ampliado trava o hook prefixado e ausência da classe antiga emitida em `view.js`. Revisão checker aprovada: commit `64ceb5e` limita-se ao produtor `src/features/docs-lote/view.js`, ao teste estrutural, ao board e ao bundle gerado; todos os produtores de análise foram atualizados, IDs/fluxo foram preservados e não há `textAnalysis` remanescente em `src/`. `dist/js/docs-lote.bundle.js` foi confirmado como saída do build. Gates checker: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 458 testes); `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Aprovado. Próxima fatia: A1-044. |
| A1-044 | 44 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar o wrapper da opção “Forçar atribuição de nomes” (`divInputForceNames`) para `.seipro-doclote-force-names`, preservando checkbox legado | review_passed | c594942 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-043 | `git status --short` ✅ (limpo); `git diff --check` ✅; `npm test` ✅ (60 arquivos, 459 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, painel de cruzamento em SEI 4, opção “Forçar atribuição de nomes na Árvore” e console sem erro novo | Revisão aprovada: o commit c594942 altera apenas o wrapper da opção em `src/features/docs-lote/templates.js`, o teste estrutural, o bundle gerado e o registro do board; preserva `checkForceNames`, `name`, `data-type`, label e comportamento do checkbox. `src/` é a fonte da verdade; `dist/js/docs-lote.bundle.js` foi confirmado como saída do build, sem churn incidental em `dist/js/init_visualizacao.js`. Gates checker executados com output real. Próxima fatia: A1-045. |
| A1-045 | 45 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar as mensagens de validação do wizard (`noFieldsError`) para `.seipro-doclote-field-error`, preservando o legado compartilhado | review_passed | 85ab95e | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-044 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 460 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, validação de campos dinâmicos/CSV inválido e mensagens de erro do cruzamento | Aprovado pelo checker: revisão do commit 85ab95e confirmou escopo restrito aos quatro produtores de mensagens em `src/features/docs-lote/view.js`, teste estrutural, bundle gerado e board; a ocorrência compartilhada `noFieldsError` em `src/shared/legacy/sei-functions-pro.js` foi preservada. `src/` permanece fonte da verdade e `dist/js/docs-lote.bundle.js` coincide com a saída do build; não houve churn incidental em `dist/js/init_visualizacao.js`. Gates reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 460 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Próxima ação: revisar A1-046. |
| A1-046 | 46 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar o hook de título dos campos detectados do wizard (`dFielTitle`) para `.seipro-doclote-field-title`, preservando o texto de análise | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-045 | `git diff --check` ✅; `npm test` ✅ (59 arquivos, 415 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, etapa de análise de documento/CSV e títulos de campos detectados | Compatibilidade preservada: a classe base `seipro-doclote-analysis-text`, ids `fieldList`/`fieldListCSV` e textos do wizard continuam iguais; apenas o hook visual específico dos títulos dos campos detectados foi prefixado. Saída gerada correspondente incluída em `dist/js/docs-lote.bundle.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga em `view.js`. Verificação checker aprovada: o commit `2efa0f8` altera somente os três produtores de títulos em `src/features/docs-lote/view.js`, o teste estrutural, o bundle gerado e o board; preserva `seipro-doclote-analysis-text`, ids `fieldList`/`fieldListCSV` e os textos do wizard. `src/` é a fonte da verdade, e a saída em `dist/js/docs-lote.bundle.js` corresponde ao build atual; não houve churn em `dist/js/init_visualizacao.js`. Gates reais: `git status --short` limpo antes/depois; `git diff --check` ✅; `npm test` ✅ (60 arquivos, 461 testes); `node scripts/engineering-loop-map.mjs --check` ✅. Smoke manual permanece necessário conforme escopo acima. Próxima ação: checker revisar A1-047. |
| A1-047 | 47 | feature | arvore | `docs-lote` / CSS prefixado | Prefixar o wrapper da barra de ações do resultado (`filterTablePro`) para `.seipro-doclote-result-actions`, preservando botões de baixar/copiar | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-046 | `git diff --check` ✅; `npm test` ✅ (59 arquivos, 416 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, conclusão da geração em lote e botões Baixar/Copiar da tabela de resultados | Compatibilidade preservada: os botões continuam usando classes `seipro-doclote-download`/`seipro-doclote-copy` e a delegação existente; apenas o wrapper visual emitido por `templates.js` deixou de reutilizar a classe legada compartilhada `filterTablePro`. Usos de `filterTablePro` em `shared/legacy`, atividades e lista-processos permanecem intocados fora desta fatia. Saída gerada correspondente incluída em `dist/js/docs-lote.bundle.js`; teste estrutural ampliado trava o novo hook e ausência do wrapper antigo em `templates.js`. Revisão checker aprovada: o commit `63c6e85` limita-se ao wrapper em `src/features/docs-lote/templates.js`, teste estrutural, bundle gerado e board; preserva classes dos botões, `data-value`, `data-icon`, `tableDataResult` e delegação, sem renomear usos compartilhados de `filterTablePro`. `dist/js/docs-lote.bundle.js` coincide com a saída reproduzida pelo build atual. Gates checker executados com output real: árvore limpa antes/depois, `git diff --check` ✅, `npm test` ✅ (60 arquivos, 462 testes), `node scripts/engineering-loop-map.mjs --check` ✅; sem churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: revisar A1-048. |

| A1-048 | 48 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao wrapper do diálogo (`seipro-doclote-dialog`), preservando a classe compartilhada `dialogBoxDiv` | review_passed | (este commit) | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-047 | `git status --short` ✅ (limpo antes/depois); `git diff --check` ✅; `npm test` ✅ (60 arquivos, 463 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, abertura dos diálogos do wizard e manutenção da aparência/modal legado | Compatibilidade preservada: o wrapper continua com id `dialogBoxDocLote` e classe compartilhada `dialogBoxDiv`; foi adicionado apenas o hook próprio `seipro-doclote-dialog` para a feature sem alterar ids, botões, campos ou fluxo do wizard. Saída gerada correspondente incluída em `dist/js/docs-lote.bundle.js`; teste estrutural ampliado trava o novo hook e a preservação da classe compartilhada. Revisão checker aprovada: b20562b altera somente o wrapper-fonte, o teste estrutural, o bundle gerado e o registro do board; `src/` é a fonte da verdade, `dist/` coincide com o build e não houve churn incidental em `dist/js/init_visualizacao.js`. Próxima ação: revisar A1-049. |

| A1-049 | 49 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao wrapper de rolagem da tabela de resultado (`seipro-doclote-result-scroll`), preservando a tabela de resultado legada | review_passed | 322df79 | (este commit) | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-048 | `git status --short` ✅ (limpo antes/depois); `git diff --check` ✅; `npm test` ✅ (60 arquivos, 464 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, conclusão da geração em lote, rolagem da tabela de resultados e botões Baixar/Copiar | Revisão checker aprovada: o commit 322df79 altera somente o wrapper em `src/features/docs-lote/templates.js`, o teste estrutural, o bundle gerado e o registro do board; preserva `tableDataResult`, as classes compartilhadas da tabela, botões e delegação. `src/` permanece fonte da verdade; `dist/js/docs-lote.bundle.js` coincide com a saída do build, sem churn incidental em `dist/js/init_visualizacao.js`. Gates reais executados após o commit de migração. Próxima ação: revisar A1-050. |

| A1-050 | 50 | feature | arvore | `docs-lote` / CSS prefixado | Adicionar hook prefixado ao wrapper de rolagem da tabela de cruzamento (`seipro-doclote-crossing-scroll`), preservando a tabela de cruzamento legada | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-049 | `git diff --check` ✅; `npm test` ✅ (59 arquivos, 418 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 2 (Árvore de documentos), com foco em Docs em Lote, etapa de cruzamento CSV/modelo, rolagem da tabela de campos relacionados e manutenção dos campos/seletores do painel | Compatibilidade preservada: a tabela continua com id `tableDataCrossing` e classes compartilhadas `seiProForm tableInfo tableZebra tableFollow`; foi adicionado apenas o hook próprio `seipro-doclote-crossing-scroll` ao wrapper de rolagem, sem alterar ids, selects, checkbox de novo processo ou fluxo do wizard. Saída gerada correspondente incluída em `dist/js/docs-lote.bundle.js`; teste estrutural ampliado trava o novo hook e a preservação da tabela legada. Próxima ação: checker revisar A1-050. |
| A1-051 | 51 | feature | editor | `ai` / CSS prefixado | Prefixar as linhas de configurações avançadas do modal de IA (`configAI_advancedconfigs`) para `.seipro-ai-advanced-config-row`, preservando checkbox/config legados | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e violação conhecida de classes de feature sem prefixo; seleção dinâmica após A1-050 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 419 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de configurações de IA, alternância “Configurações avançadas” e console sem erro novo | Compatibilidade preservada: ids `configAI_advancedconfigs`, `configAI_system_instruction`, `configAI_temperature`, `configAI_max_tokens`, `configAI_top_p`, `configAI_frequency_penalty` e `configAI_presence_penalty` continuam iguais; apenas a classe DOM própria das linhas avançadas foi prefixada e o seletor show/hide passou a consultar `.seipro-ai-advanced-config-row`. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural novo trava o hook prefixado e ausência da classe antiga como classe emitida/seletor. Próxima ação: checker revisar A1-051. |

| A1-052 | 52 | feature | editor | `ai` / CSS prefixado | Prefixar o hook dos chips de seleção múltipla de documentos (`doc-ai-tag`) para `.seipro-ai-doc-tag`, preservando remoção dos chips | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-051 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 420 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, seleção múltipla de documentos, remoção de chips e envio do prompt sem erro novo | Compatibilidade preservada: o container `docAIMultiList`, dados do chip e o ícone de remoção `doc-ai-tag-remove` continuam iguais; apenas a classe DOM própria do chip foi prefixada e os seletores locais passaram a consultar `.seipro-ai-doc-tag`. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida/seletor. Próxima ação: checker revisar A1-052. |

| A1-053 | 53 | feature | editor | `ai` / CSS prefixado | Prefixar o hook das respostas do usuário no chat de IA (`response_user`) para `.seipro-ai-user-response`, preservando restauração de histórico | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-052 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 421 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, envio/restauração de prompts no chat e console sem erro novo | Compatibilidade preservada: o histórico continua salvo com `getHistoryDialog` e restaurado antes de `initFunctionsChat`; apenas a classe DOM própria da mensagem do usuário foi prefixada e o seletor de limpeza em `#response_ai` passou a usar `.seipro-ai-user-response`, mantendo `.response_bot` para fatia futura. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida/seletor. Próxima ação: checker revisar A1-053. |

| A1-054 | 54 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado às respostas do bot no chat de IA (`seipro-ai-bot-response`), preservando a classe legada de estilo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-053 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 422 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração/restauração de respostas do bot, copiar resposta e leitura em voz alta | Compatibilidade preservada: as caixas de resposta do bot mantêm a classe legada `response_bot` para o CSS legado de `dist/css/sei-pro.css`, mas recebem o hook próprio `seipro-ai-bot-response`; os seletores locais de restauração e leitura em voz alta passaram a usar o hook prefixado, e `response_bot_content` continua intacto para copiar texto/html. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novos seletores locais pela classe legada. Próxima ação: checker revisar A1-054. |

| A1-055 | 55 | feature | editor | `ai` / CSS prefixado | Prefixar o hook de adicionar resposta da IA ao editor (`add_doc_response_ai`) para `.seipro-ai-add-doc-response`, preservando o `data-response` | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-054 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 423 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta e ação “Adicionar texto no documento” no editor CKEditor | Compatibilidade preservada: o wrapper da ação mantém `data-response` e chama `addDocResponseAI(this)` via delegação; apenas a classe DOM própria do botão foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida/seletor. Próxima ação: checker revisar A1-055. |

| A1-056 | 56 | feature | editor | `ai` / CSS prefixado | Prefixar o hook de criar documento SEI a partir da resposta da IA (`create_doc_response_ai`) para `.seipro-ai-create-doc-response`, preservando o `data-response` | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-055 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 424 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA fora do editor CKEditor, geração de resposta e ação “Criar documento SEI” | Compatibilidade preservada: o wrapper da ação mantém `data-response` e chama `createDocResponseAI(this)` por delegação; apenas a classe DOM própria do botão foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida/seletor. Próxima ação: checker revisar A1-056. |

| A1-057 | 57 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado à ação de copiar texto simples da resposta da IA (`seipro-ai-copy-response`), preservando a classe legada de estilo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-056 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 425 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta, ação “Copiar texto simples” e duplo clique para cancelar leitura em voz alta | Compatibilidade preservada: o wrapper da ação mantém `copy_response_ai` para o CSS legado de `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-copy-response`; os seletores locais de clique e duplo clique passaram a usar o hook prefixado, mantendo `data-response` e `copyResponseAI(this)`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novos seletores locais pela classe legada. Próxima ação: checker revisar A1-057. |

| A1-058 | 58 | feature | editor | `ai` / CSS prefixado | Prefixar o hook de copiar texto formatado da resposta da IA (`copy_html_response_ai`) para `.seipro-ai-copy-html-response`, preservando o `data-response` | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-057 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 426 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta e ação “Copiar texto formatado” para colar em documentos | Compatibilidade preservada: o wrapper da ação mantém `data-response` e chama `copyHtmlResponseAI(this)` por delegação; apenas a classe DOM própria do botão foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga como classe emitida/seletor. Próxima ação: checker revisar A1-058. |

| A1-059 | 59 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado à ação de leitura em voz alta da resposta da IA (`seipro-ai-speech-response`), preservando a classe legada de estilo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-058 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 427 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta, ação “Ouvir em voz alta” e interrupção/convivência com copiar texto simples | Compatibilidade preservada: o wrapper da ação mantém a classe legada `speech_response_ai` para o CSS legado de `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-speech-response`; o seletor local de clique passou a usar o hook prefixado, mantendo `data-response` e leitura a partir da resposta do bot. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novo seletor local pela classe legada. Próxima ação: checker revisar A1-059. |

| A1-060 | 60 | feature | editor | `ai` / CSS prefixado | Adicionar hooks prefixados à área/cartões de sugestões do chat de IA (`seipro-ai-suggestions`/`seipro-ai-suggestion-action`), preservando classes legadas de estilo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-059 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 428 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, exibição de sugestões iniciais, clique em sugestões padrão e favoritos de prompt | Compatibilidade preservada: as classes legadas `suggestions` e `suggestion_actions` permanecem para o CSS legado de `dist/css/sei-pro.css`, mas os seletores locais passaram a usar os hooks próprios `seipro-ai-suggestions` e `seipro-ai-suggestion-action`; atributos `data-send`, `data-type` e dados de documento/processo continuam iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava os novos hooks e evita nova delegação pela classe legada. Próxima ação: checker revisar A1-060. |

| A1-061 | 61 | feature | editor | `ai` / CSS prefixado | Prefixar o hook de remover sugestão favorita da IA (`suggestion_actions_remove`) para `.seipro-ai-remove-suggestion`, preservando índice do favorito | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-060 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 429 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, sugestões favoritas/personalizadas, remoção pelo ícone de lixeira e persistência da lista | Compatibilidade preservada: cartões de sugestão continuam com `suggestion_actions` para CSS legado e `seipro-ai-suggestion-action` para a delegação principal; apenas o hook local do ícone de remoção de favorito foi prefixado. O atributo `data-index` e a chamada `removeFavoritePromptByIndex` permanecem iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência do seletor antigo. Próxima ação: checker revisar A1-061. |
| A1-062 | 62 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado às referências de documento do prompt (`seipro-ai-prompt-ref-doc`), preservando classe legada de estilo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-061 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 430 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, prompt personalizado com referências de documento/processo, seleção de documentos e envio do prompt | Compatibilidade preservada: spans de referência continuam com `prompt_ref_doc` para o CSS legado em `dist/css/sei-pro.css`, mas recebem o hook próprio `seipro-ai-prompt-ref-doc`; o seletor local em `makeFooterPrompt` passou a usar o hook prefixado. Atributos `data-id_documento`/`data-id_procedimento`/`data-num_processo`/`contenteditable` e classes dinâmicas `doc_*` permanecem iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novo seletor local pela classe legada. Próxima ação: checker revisar A1-062. |

| A1-063 | 63 | feature | editor | `ai` / CSS prefixado | Prefixar o hook de remoção dos chips de documento (`doc-ai-tag-remove`) para `.seipro-ai-doc-tag-remove`, preservando o chip selecionado | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-062 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 430 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, seleção múltipla de documentos, remoção de chips e envio do prompt | Compatibilidade preservada: os chips continuam usando `seipro-ai-doc-tag`, dados jQuery (`id_documento`, `num_processo`, `id_procedimento`, `name`) e container `docAIMultiList`; apenas o ícone/delegação de remoção foi prefixado para `seipro-ai-doc-tag-remove`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência do seletor antigo. Próxima ação: checker revisar A1-063. |
| A1-064 | 64 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao conteúdo textual das respostas da IA (`seipro-ai-response-content`), preservando a classe legada de estilo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-063 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 431 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta, copiar texto simples/formatado, adicionar ao editor e criar documento SEI | Compatibilidade preservada: spans de conteúdo continuam com `response_bot_content` para o CSS legado em `dist/css/sei-pro.css`, mas recebem o hook próprio `seipro-ai-response-content`; seletores locais de copiar/adicionar/criar documento passaram a consultar o hook prefixado. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novos seletores locais pela classe legada. Próxima ação: checker revisar A1-064. |

| A1-065 | 65 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao alvo da animação BotPro (`seipro-ai-icon`), preservando a classe legada de estilo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-064 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 432 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, abertura do chat e animação/ícone BotPro inicial | Compatibilidade preservada: o contêiner continua com `icon_ia` para o CSS legado em `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-icon`; chamadas de `adicionarVideoWebM` passaram a mirar o hook prefixado. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novos alvos pela classe legada. Próxima ação: checker revisar A1-065. |

| A1-066 | 66 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao wrapper do diálogo de IA (`seipro-ai-dialog`), preservando a classe legada de estilo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-065 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 433 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA e manutenção da aparência/fluxo do diálogo | Compatibilidade preservada: o wrapper continua com `dialogBoxAI` para o CSS legado de `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-dialog`; ids, botões, seletores internos e fluxo do chat não mudaram. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e a preservação da classe legada. Próxima ação: checker revisar A1-066. |
| A1-067 | 67 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao bloco de boas-vindas do chat de IA (`seipro-ai-welcome`), preservando a classe legada de estilo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-066 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 434 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA e visualização da mensagem inicial/ícone BotPro | Compatibilidade preservada: o bloco inicial continua com a classe legada `welcome` para o CSS legado em `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-welcome`; ícone `icon_ia seipro-ai-icon`, ids e fluxo do chat não mudaram. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e a preservação da classe legada. Próxima ação: checker revisar A1-067. |

| A1-068 | 68 | feature | editor | `ai` / CSS prefixado | Prefixar o wrapper da barra de prompt do chat de IA (`input_prompt`) para `.seipro-ai-prompt-bar`, preservando campos de prompt/documento | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-067 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 435 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA, seleção de prompt/documento e digitação de prompt personalizado | Compatibilidade preservada: os ids `promptAISelect`, `docAISelect`, `docAIMultiList`, `promptAIPersonal` e o fluxo de envio do prompt continuam iguais; apenas a classe DOM própria do wrapper da barra de prompt foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência de `input_prompt` como classe emitida. Próxima ação: checker revisar A1-068. |

| A1-069 | 69 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do seletor de prompt do chat de IA (`prompt_type`) para `.seipro-ai-prompt-select`, preservando o id do select | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-068 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 436 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA, seleção de prompts padrão/personalizados e envio do prompt | Compatibilidade preservada: o select continua com id `promptAISelect`, opções de `buildPromptOptions()` e fluxo Chosen/delegação por id; apenas a classe DOM própria do seletor de prompt foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência de `prompt_type` como classe emitida. Próxima ação: checker revisar A1-069. |
| A1-070 | 70 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do seletor de documentos do chat de IA (`prompt_doc`) para `.seipro-ai-doc-select`, preservando o id do select | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-069 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 437 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA, seleção de documento/processo e envio do prompt | Compatibilidade preservada: o select continua com id `docAISelect`, carregamento via `getDocsArvore_fillSelect()` e fluxos Chosen/delegação por id; apenas a classe DOM própria do seletor de documentos foi prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência de `prompt_doc` como classe emitida. Próxima ação: checker revisar A1-070. |
| A1-071 | 71 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao modo de prompt personalizado do seletor de documento (`seipro-ai-personal-prompt-mode`), preservando a classe legada de CSS | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-070 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 438 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA, prompt personalizado, seleção de documento para inserir referência no prompt e retorno ao seletor padrão | Compatibilidade preservada: o estado escolhido do `docAISelect_chosen` mantém `prompt_personal` para o CSS legado de `dist/css/sei-pro.css`, mas passa a receber também o hook próprio `seipro-ai-personal-prompt-mode`; ids `promptAIPersonal`, `docAISelect`, fluxo Chosen e opções de seleção permanecem iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e a preservação da classe legada. Próxima ação: checker revisar A1-071. |

| A1-072 | 72 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao modo de seletor de documentos no editor (`seipro-ai-editor-doc-mode`), preservando a classe legada de CSS | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-071 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 439 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco na abertura do modal de IA no editor CKEditor, seletor de documento/processo e envio de prompt | Compatibilidade preservada: o estado Chosen de `docAISelect_chosen` mantém `prompt_editor_sei` para o CSS legado de `dist/css/sei-pro.css`, mas recebe também o hook próprio `seipro-ai-editor-doc-mode`; ids, opções, Chosen e fluxo de prompt/documento permanecem iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e a preservação da classe legada. Próxima ação: checker revisar A1-072. |

| A1-073 | 73 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do botão de salvar credencial Gemini (`gemini_token`) para `.seipro-ai-gemini-token`, preservando detecção da plataforma | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-072 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 440 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações de IA, selecionar Gemini, salvar credencial e confirmar que a plataforma detectada continua Gemini | Compatibilidade preservada: o botão mantém id `plataformAI_uiElement`, rótulo/inputs e classes compartilhadas do CKEditor; apenas o hook próprio `gemini_token` foi prefixado e a detecção em `saveTokenOpenAI` passou a consultar `.seipro-ai-gemini-token`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência do seletor antigo. Próxima ação: checker revisar A1-073. |

| A1-074 | 74 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do botão de salvar credencial OpenAI (`openai_token`) para `.seipro-ai-openai-token`, preservando detecção da plataforma | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-073 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 441 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações de IA, selecionar OpenAI, salvar credencial e confirmar que a plataforma detectada continua OpenAI | Compatibilidade preservada: o botão mantém id `plataformAI_uiElement`, rótulo/inputs e classes compartilhadas do CKEditor; apenas o hook próprio `openai_token` foi prefixado e a detecção em `saveTokenOpenAI` passou a consultar `.seipro-ai-openai-token`, mantendo fallback OpenAI. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência do seletor antigo. Próxima ação: checker revisar A1-074. |

| A1-075 | 75 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do botão de salvar credencial Ollama (`ollama_token`) para `.seipro-ai-ollama-token`, preservando detecção da plataforma | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-074 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 442 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações de IA, selecionar/configurar Ollama ou LiteLLM, salvar credencial/URL/modelo e confirmar que a plataforma detectada continua Ollama | Compatibilidade preservada: o botão mantém id `plataformAI_uiElement`, rótulo, campos `cke_inputOllamaUrl_textInput`/`cke_inputOllamaKey_textInput`/`cke_inputOllamaModel_textInput` e classes compartilhadas do CKEditor; apenas o hook próprio `ollama_token` foi prefixado e a detecção em `saveTokenOpenAI` passou a consultar `.seipro-ai-ollama-token`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência do seletor antigo. Próxima ação: checker revisar A1-075. |

| A1-076 | 76 | feature | editor | `ai` / CSS prefixado | Prefixar o hook do checkbox de animação de digitação (`resume_doc`) para `.seipro-ai-typing-toggle`, preservando id/config e estilo legado do switch | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-075 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 443 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, alternar “Animação de texto digitando”, salvar e confirmar que a opção continua persistindo e afetando a digitação | Compatibilidade preservada: o checkbox mantém id `configAI_typing_box`, `name="infraAncoraSigla"`, label e persistência via `setTypingAI`; apenas a classe própria `resume_doc` foi substituída por `seipro-ai-typing-toggle`, preservando a classe compartilhada `infraLinkOrgao`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e ausência da classe antiga. Próxima ação: checker revisar A1-076. |

| A1-077 | 77 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao checkbox de modelos beta (`seipro-ai-beta-models-toggle`), preservando id/config e estilo legado do switch | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-076 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 444 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, alternar “Listar modelos beta”, salvar e confirmar que a lista/modelos beta continuam persistindo | Compatibilidade preservada: o checkbox mantém id `configAI_betamodels`, `name` legado, label e persistência via `setBetaModelsAI`; apenas foi adicionado o hook próprio `seipro-ai-beta-models-toggle`, preservando a classe compartilhada `infraLinkOrgao`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-077. |

| A1-078 | 78 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao checkbox de configurações avançadas (`seipro-ai-advanced-config-toggle`), preservando id/config e estilo legado do switch | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-077 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 445 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, alternar “Configurações avançadas”, salvar e confirmar exibição/persistência dos campos avançados | Compatibilidade preservada: o checkbox mantém id `configAI_advancedconfigs`, `name` legado, label e persistência via `setAdvancedConfigs`; apenas foi adicionado o hook próprio `seipro-ai-advanced-config-toggle`, preservando a classe compartilhada `infraLinkOrgao`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-078. |

| A1-079 | 79 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado à linha de modelo manual Ollama (`seipro-ai-manual-model-row`), preservando id/campo de modelo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-078 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 446 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA na plataforma Ollama/LiteLLM, informar modelo manual, salvar e confirmar persistência/uso do modelo | Compatibilidade preservada: a linha mantém id `configAI_ollama_model_row`, campo `configAI_manual_model`, label e persistência via `setModelOllama`; apenas foi adicionado o hook próprio `seipro-ai-manual-model-row`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-079. |
| A1-080 | 80 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao seletor de modelo da IA (`seipro-ai-model-select`), preservando id e persistência | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-079 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 447 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, escolher modelo por plataforma, salvar e confirmar persistência/uso do modelo | Compatibilidade preservada: o select mantém id `configAI_model`, opções de `optionsModels()` e persistência por `setModelOpenAI`/`setModelGemini`/fallback Ollama; apenas foi adicionado o hook próprio `seipro-ai-model-select`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-080. |

| A1-081 | 81 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao aviso de consentimento de IA (`seipro-ai-consent-disclaimer`), preservando a classe legada de estilo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-080 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 448 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir o modal de IA, visualizar o aviso de consentimento e marcar o checkbox antes de prosseguir | Compatibilidade preservada: o bloco de consentimento mantém `editorTextDisclaimer` para o CSS legado em `dist/css/sei-slim.css`, mas recebe o hook próprio `seipro-ai-consent-disclaimer`; id `cke_382_uiElement`, checkbox `ciente_disclaimer`, label e fluxo de consentimento permanecem iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/checkbox. Próxima ação: checker revisar A1-081. |

| A1-082 | 82 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao link de restauração do histórico da IA (`seipro-ai-history-link`), preservando o id legado | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-081 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 449 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir o modal de IA, gerar/restaurar histórico do diálogo e confirmar que o link Ver Histórico continua funcionando | Compatibilidade preservada: o link mantém o id `historyDialogAI` e o storage `historyDialogAI`; apenas recebe o hook próprio `seipro-ai-history-link`, e a restauração/click delegado passam a usar a classe prefixada. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e evita novos seletores locais pelo id legado. Próxima ação: checker revisar A1-082. |

| A1-083 | 83 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de instrução de sistema da IA (`seipro-ai-system-instruction`), preservando id/config e persistência | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-082 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 450 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar instrução de sistema, salvar e confirmar persistência/uso no chat | Compatibilidade preservada: o textarea mantém id `configAI_system_instruction`, label e persistência via `setSystemInstructionAI`; apenas recebeu o hook próprio `seipro-ai-system-instruction`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-083. |

| A1-084 | 84 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de temperatura da IA (`seipro-ai-temperature-input`), preservando id/config e persistência | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-083 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 451 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar temperatura, salvar e confirmar persistência/uso na geração | Compatibilidade preservada: o input mantém id `configAI_temperature`, label e persistência via `setTemperatureAI`; apenas recebeu o hook próprio `seipro-ai-temperature-input`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-084. |

| A1-085 | 85 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de comprimento máximo da IA (`seipro-ai-max-tokens-input`), preservando id/config e persistência | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-084 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 452 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar comprimento máximo, salvar e confirmar persistência/uso na geração | Compatibilidade preservada: o input mantém id `configAI_max_tokens`, label e persistência via `setMaxTokenAI`; apenas recebeu o hook próprio `seipro-ai-max-tokens-input`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-085. |

| A1-086 | 86 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo Top P da IA (`seipro-ai-top-p-input`), preservando id/config e persistência | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-085 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 453 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar Parte superior P/Top P, salvar e confirmar persistência/uso na geração | Compatibilidade preservada: o input mantém id `configAI_top_p`, label e persistência via `setTopPAI`; apenas recebeu o hook próprio `seipro-ai-top-p-input`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-086. |

| A1-087 | 87 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de penalidade de frequência da IA (`seipro-ai-frequency-penalty-input`), preservando id/config e persistência | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126 e sugestão A1-086 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 454 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar penalidade de frequência, salvar e confirmar persistência/uso na geração | Compatibilidade preservada: o input mantém id `configAI_frequency_penalty`, label e persistência via `setFrequencyPenaltyAI`; apenas recebeu o hook próprio `seipro-ai-frequency-penalty-input`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-087. |

| A1-088 | 88 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de penalidade de presença da IA (`seipro-ai-presence-penalty-input`), preservando id/config e persistência | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-087 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 455 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir configurações gerais de IA, editar penalidade de presença, salvar e confirmar persistência/uso na geração | Compatibilidade preservada: o input mantém id `configAI_presence_penalty`, label, tipo/range/step e persistência via `setPresencePenaltyAI`; apenas recebeu o hook próprio `seipro-ai-presence-penalty-input`. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de id/persistência. Próxima ação: checker revisar A1-088. |

| A1-089 | 89 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de modelo manual Ollama (`seipro-ai-manual-model-input`), preservando id/config e persistência | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-088 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 456 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em configurar modelo manual Ollama/LiteLLM, salvar e confirmar persistência/uso | Compatibilidade preservada: o input mantém id `configAI_manual_model`, placeholder, valor e persistência via `setModelOllama`; apenas recebe o hook próprio da feature. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o hook e os contratos de id/persistência. Próxima ação: checker revisar A1-089. |

| A1-090 | 90 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao cursor de digitação da resposta (`seipro-ai-typing-cursor`), preservando a classe legada de estilo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-089 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 457 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, geração de resposta com animação de digitação e cursor piscante | Compatibilidade preservada: o cursor mantém `blinker` para o CSS legado em `dist/css/sei-pro.css`, mas recebe o hook próprio `seipro-ai-typing-cursor`; o fluxo de renderização incremental e a resposta final permanecem iguais. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava os dois hooks. Próxima ação: checker revisar A1-090. |

| A1-091 | 91 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao botão de adicionar documento (`seipro-ai-add-document`), preservando o id legado | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-090 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 458 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, adicionar documento à seleção múltipla, remover chip e enviar prompt | Compatibilidade preservada: o link mantém id `btnAddDocAI`, classe compartilhada `newLink`, tooltip e chamada `addDocToMultiList`; o listener delegado local usa o novo hook da feature. Saída gerada correspondente incluída apenas em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava hook, id e delegação. Próxima ação: checker revisar A1-091. |

| A1-092 | 92 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao botão de configurações de IA (`seipro-ai-config-button`), preservando o id legado | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-091 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 459 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em abrir o modal de IA, acionar Configurações Gerais e salvar/fechar sem erro novo | Compatibilidade preservada: o link mantém id `btnConfigAI`, classe compartilhada `newLink`, tooltip e chamada `configAI(this)`; somente a delegação local passou para o novo hook da feature. Saída gerada correspondente: `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava hook, id e delegação. Próxima ação: checker revisar A1-092. |

| A1-093 | 93 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao botão de envio de prompt (`seipro-ai-send-button`), preservando o id legado | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-092 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 460 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA, envio de prompt por botão e atalho, e ausência de erro novo | Compatibilidade preservada: o link mantém id `btnSendAI`, classe compartilhada `newLink`, ícone/texto e chamada `initAI(this)`; somente a delegação local usa o novo hook da feature. Saída gerada esperada: `dist/js/sei-pro-ai.js`; teste estrutural cobre hook, id e delegação. Próxima ação: checker revisar A1-093. |
| A1-094 | 94 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao botão de alternar plataforma da IA (`seipro-ai-switch-platform`), preservando o id legado | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-093 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 461 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco no modal de IA com duas ou mais plataformas configuradas, alternância de plataforma e persistência da plataforma ativa | Compatibilidade preservada: o link mantém id `btnChangeSelectedAI`, classe compartilhada `newLink`, tooltip e persistência por `setOptionsPro`; somente a delegação local passa a usar o novo hook da feature. Saída gerada correspondente: `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava hook, id, delegação e persistência. Próxima ação: checker revisar A1-094. |

| A1-095 | 95 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de chave Ollama (`seipro-ai-ollama-key-input`), preservando o comportamento legado de senha | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-094 | `git diff --check` ✅; `npm test` ✅; `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em configurações de IA para Ollama/LiteLLM, preenchimento da chave e salvamento da credencial | Compatibilidade preservada: o input mantém id `cke_inputOllamaKey_textInput`, tipo `password` e classe legada `passReveal`; foi adicionado apenas o hook próprio `seipro-ai-ollama-key-input`. Saída gerada correspondente incluída em `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o novo hook e contratos de senha. Próxima ação: checker revisar A1-047 e, depois, esta fatia na ordem do board. |
| A1-096 | 96 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de URL do servidor Ollama (`seipro-ai-ollama-url-input`), preservando id e endpoint padrão | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-095 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 463 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em configurações de IA para Ollama/LiteLLM, editar a URL do servidor, salvar e confirmar persistência/conexão | Compatibilidade preservada: o input mantém id `cke_inputOllamaUrl_textInput`, tipo `text`, endpoint padrão e leitura por `saveTokenOpenAI`; foi adicionado apenas o hook próprio `seipro-ai-ollama-url-input`. Saída gerada esperada: `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o hook e os contratos de id/endpoint. Próxima ação: checker revisar A1-096. |

| A1-097 | 97 | feature | editor | `ai` / CSS prefixado | Adicionar hook prefixado ao campo de modelo do servidor Ollama (`seipro-ai-ollama-model-input`), preservando id e fluxo de modelo | migrated_pending_review | (este commit) |  | `DEVELOPMENT.md` linhas 123-126; seleção dinâmica após A1-096 | `git diff --check` ✅; `npm test` ✅ (60 arquivos, 464 testes); `node scripts/engineering-loop-map.mjs --check` ✅ | Smoke manual necessário: `SMOKE_TEST.md` seção 3 (Editor de documentos), com foco em configurações de IA para Ollama/LiteLLM, informar o modelo padrão, salvar e confirmar persistência/uso | Compatibilidade preservada: o input mantém id `cke_inputOllamaModel_textInput`, tipo `text`, placeholder e leitura por `saveTokenOpenAI`; foi adicionado apenas o hook próprio `seipro-ai-ollama-model-input`. Saída gerada correspondente esperada: `dist/js/sei-pro-ai.js`; teste estrutural ampliado trava o hook e os contratos de id/modelo. Próxima ação: checker revisar A1-097. |

## Regras de atualização

- O migration job deve marcar no máximo uma linha por execução como `migration_in_progress` e depois `migrated_pending_review`, ou registrar bloqueio objetivo.
- O verification job deve revisar apenas linhas `migrated_pending_review` e alterar para `review_passed` ou `review_failed_needs_fix`.
- Se um job escolher uma fatia dinâmica que ainda não está no board, deve adicionar a linha antes ou durante a implementação.
- Não remover histórico; adicionar observações em vez de apagar decisões relevantes.
- `Commit migração` e `Commit verificação` podem ser preenchidos com hash curto após commit.
