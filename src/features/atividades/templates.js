/**
 * Atividades — HTML/template factories (seed; more strings move here over time).
 */
export function atividadesPanelRootHtml() {
    return '<div id="atividadesPro" class="seipro-atividades-root"></div>';
}

export function atividadesPanelDivHtml() {
    return '<div id="atividadesProDiv" class="seipro-atividades-panel"></div>';
}
