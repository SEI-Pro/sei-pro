/**
 * Stub (ADR-0007) — hoje em `activity-actions.ts`, `activity-work.ts`,
 * `activity-form.ts`, `panel.ts`, `kanban.ts`. Ver docs/capabilities-map.md.
 */
export function installAtividadesRegistroFeature() {
    // no-op until extraction
}
