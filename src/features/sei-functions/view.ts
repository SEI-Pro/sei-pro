// @ts-nocheck — ADR-0014: dívida até tipagem; remover ao editar o arquivo.
/**
 * Sei Functions Pro — view facade.
 * Concrete UI still lives in cluster modules (jQuery legado).
 */
export function installSeiFunctionsView() {
    // Cluster modules register behavior via boot.js (fnJqueryPro / loadScriptPro).
}
